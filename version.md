## 2026-07-15
- Added `File > Check for Updates` for alpha-review builds. The updater discovers matching prereleases from a configured public GitHub repository, verifies the full Inno installer by size and SHA-256, downloads atomically in the background, and performs an in-place update without removing user settings.
- Added a one-command local GitHub prerelease publisher that builds and smoke-tests the PyInstaller bundle, creates a versioned Inno installer and strict update manifest, and uploads both release assets.
- Added a metadata-seeded Gaia WCS fallback for asteroid/comet images that contain pointing and optical metadata but no celestial WCS. The fallback solves rotation and parity from detected/Gaia star patterns before trying astrometry.net.

## 2026-06-17
- Fixed asteroid/comet blink playback performance for large FITS sequences such as `E:\Davida\aligned` (100 frames, 4176×6248, ~99.5 MiB each). Blink is now scoped to asteroid/comet mode only; Sky Explorer and other modes are unchanged.
- Added a blink-active fast path in `photometry_app/ui/main_window.py`: during playback, `_set_active_asteroid_frame()` now calls `_refresh_asteroid_blink_frame_view()` (image swap only) instead of the full `_handle_asteroid_result_selection_changed()` path that rebuilt the details panel and refreshed the asteroid Sky View tab every frame (~0.13–0.31 s/frame on Davida). Sky View and details refresh once when blink stops.
- Fixed blink timer interval not honoring settings on startup. The `QTimer` was created before `_settings` existed and stayed at the hardcoded fallback `_ASTEROID_BLINK_INTERVAL_MS = 350` until the user saved Settings. `_sync_asteroid_blink_timing_from_settings()` now runs after settings load in `MainWindow.__init__` and in `_load_settings_for_root()`, so the configured value (default 50 ms) applies immediately.
- Hardened blink preload before playback. `_start_asteroid_blink_playback()` now requires `_asteroid_blink_preload_ready()` and triggers `_prepare_asteroid_blink_preload()` when the QImage cache is incomplete. Preload completion no longer auto-starts playback unless every frame has a cached rendered `QImage`; incomplete preloads report `loaded/total` in the workflow note.
- Stopping blink on any asteroid/comet image display-control change (stretch, levels, curves, invert), not just invert, so playback does not continue with a cleared QImage cache after display tweaks.
- Davida benchmark results (offscreen): with warm preload and blink active, per-frame advance ~0.007–0.011 s (~17 FPS effective at 50 ms interval); previously ~0.17–0.31 s inactive path plus 350 ms timer capped playback around ~2.8 FPS. Uncached frames on Davida cost ~0.55–10 s each via full FITS processing.
- Added diagnostic scripts `scripts/diagnose_asteroid_blink_davida.py`, `scripts/benchmark_asteroid_blink_davida.py`, and `scripts/benchmark_asteroid_blink_mainwindow_davida.py`. Focused blink regressions added/updated in `tests/test_main_window.py` (18 blink-related tests passing).

## 2026-06-01
- Added a menu-bar About action that identifies the app as developed by Ogetay, points reviewers to ogetay.com/citizen-astronomy-cast, and clearly marks the build as alpha-reviewer-only/non-distributable. Added a dedicated PyInstaller alpha-review spec that keeps the debug spec intact while explicitly bundling app data, the curated 32k Milky Way runtime assets, Moon visual assets, the icon, and required Astroquery/Photutils package data for local reviewer builds, including Simbad's packaged query-criteria JSON so frozen startup does not fall back to Astropy data downloads.
- Hardened settings/state JSON loading to accept UTF-8 files with a BOM, which prevents alpha-review packaged startup from failing on existing Windows-side `settings.json` files written or edited with a BOM.
- Switched Sky View Milky Way runtime asset selection to the 32k TIFF-LZW pyramid only, removing the 16k manifest from resolver fallbacks and deleting the local 16k tile tree/source EXR from the workspace. Documentation and resolver tests now treat `textures/milky_way_tiles_32k_padded_lzw_benchmark/manifest.json` as the default packaged Milky Way asset.
- Optimized only the Sky View Milky Way runtime shader tile request construction path while preserving tiled rendering, visual output, diagnostics, and the projected mesh debug path. Normal shader-mode requests now use a per-manifest/level static tile metadata cache plus a bounded shader request LRU keyed by manifest identity, selected level, viewport, camera/FOV, and debug-relevant settings, so unchanged frames skip tile visibility sampling, per-tile signal/bounds/path/cache-key reconstruction, and per-tile filesystem checks. Added focused request-cache regressions for identical-frame hits, camera/LOD/manifest invalidation, missing-tile state, shader-mode mesh avoidance, and steady-frame filesystem behavior.

## 2026-05-31
- Optimized only the Sky View Milky Way OpenGL renderer's tiled preparation path without changing shader appearance, tiled rendering, the global/base fallback, diagnostics, or debug modes. Missing prepared tiles are now scheduled onto bounded background futures, completed futures are harvested per frame with a cap, `warm_prepared_tile_cache()` schedules instead of decoding/padding synchronously, tile texture resolution only uploads already prepared images by default, and base/global alpha-mask plus upload-image mirroring is prepared off the paint path before GL upload. Runtime diagnostics now report prepared-tile future submit/harvest/pending/error state plus deferred base preparation timing.
- Optimized the Sky View Moon tiled-texture preparation path without changing Moon shader appearance or disabling tiled rendering. `MoonTiledTextureManager` now builds tile-generation/index/registration snapshots off the render path, keeps live asset watching opt-in, defers live equivalence probes, normal-statistics diagnostics, and polar-cap image loading behind cached background futures, uses global Moon fallback while tiled diagnostics or assets are pending, and limits completed tile-future harvesting per paint frame. Focused Moon tests now cover metadata-pending fallback, deferred diagnostics, non-per-frame generation refresh, budget fallback, and nonblocking polar-cap binding. Focused benchmark outputs under `benchmark_results_moon_optimized/` and `benchmark_results_moon_optimized_matrix2/` show the prior hundred-ms Moon `prepare_tiles` spikes removed from the targeted measured scenarios.
- Extended the gated Sky View benchmark into a refined bottleneck-isolation pass without changing normal rendering output. The recorder now captures 50 ms slow-frame context, GC deltas, tracked-object counts for slow frames, and recent correlated events; Sky View instrumentation now splits Milky Way visibility/runtime input/LOD/tile selection/cache/draw/eviction work, grid cache/generation/projection/drawing work, constellation cache/culling/clipping/projection/batching/label-cache/drawing work, Moon route/projection/texture-bind/shader/uniform/draw work, background clear timing, and time-simulation tick/repaint metadata. `scripts/run_sky_view_benchmark.py` now writes the refined report set under `benchmark_results_refined/`, including expanded JSON/CSV data, `slow_frames.md`, `layer_toggle_matrix.csv`, `layer_toggle_matrix.md`, and `gc_diagnostic.md`, while preserving the original `benchmark_results/` first-pass outputs.
- Added gated Sky View benchmark instrumentation behind `CITIZEN_PHOTOMETRY_BENCHMARK=1` without changing normal rendering behavior. The new recorder captures nested `perf_counter()` sections, per-frame totals, interaction state, slow-frame context, and summary stats, while `scripts/run_sky_view_benchmark.py` drives repeatable startup/load, idle, Milky Way, dense star, horizon/ground, pole, constellation, Moon, grid, time-simulation, pan, zoom, stress, and minimal-layer scenarios and writes Markdown/JSON/CSV reports under `benchmark_results/`. Instrumentation now covers the main `paintGL()` frame boundary, QPainter render stages, Milky Way tile decode/prepare/upload/draw paths, Moon ephemeris/tile/texture/draw paths, and constellation load/projection/draw paths.
- Switched the default interactive Sky View Milky Way tiled asset to the new padded TIFF-LZW 16k pyramid at `textures/milky_way_tiles_16k_padded_lzw_benchmark/manifest.json`, while keeping the 32k padded TIFF-LZW tree available as a dedicated `High Detail Milky Way (32k)` selector option. The default Advanced path now prewarms the OpenGL layer's CPU prepared-tile cache for the visible 16k tile set before GPU drawing, preserving the seam-safe tiled renderer while moving decode/prepare work out of the native GL draw. Added focused resolver, render-mode normalization, and prepared-tile warmup regressions.

## 2026-05-27
- Converted Sky View Milky Way rendering from a full-sky opacity overlay toward a Milky-Way-only masked overlay. `photometry_app/core/milky_way_mask.py` now centralizes the broad luminance alpha-mask helper and empty-tile classifier, `photometry_app/core/milky_way_tile_generator.py` and `scripts/generate_milky_way_tiles.py` now write RGBA masked tiles plus backward-compatible manifest signal metadata by default, and `photometry_app/core/milky_way_assets.py` parses that optional metadata. The OpenGL renderer now preserves source alpha, adds upload-time mask alpha for legacy RGB assets, removes the shader's full-sky opacity floor, and skips manifest-known empty tiles before resolving/uploading them; Sky View diagnostics now report empty tile skips. Focused Milky Way asset, GL helper, and Sky View integration tests cover the new manifest metadata, generator output, alpha-preserving decode/upload path, and empty-tile request culling.
- Finalized the Sky View horizon-lock camera repair after the accidental `main_window.py` splice damage. `photometry_app/ui/main_window.py` now restores the corrupted star-sprite and GL helper bands, keeps a separate continuous drag-right axis during near-pole mouse gestures, preserves the full projected previous right vector inside a small pole continuity band, and only snaps the stored render basis back to the canonical horizon frame once the camera is visibly off the pole. `tests/test_main_window.py` now keeps the pole-exit regression tied to a measurable angular departure from the initial near-zenith forward vector instead of an over-tight hardcoded `z` cutoff. Focused validation passed for the full 11-test Sky View camera/projection slice.
- Added a fast vector-based Sky View constellation overlay from local d3-celestial data. `photometry_app/ui/constellation_overlay.py` now lazy-loads packaged `constellations.lines.json` and `constellations.json`, precomputes RA/Dec endpoints and label anchors into unit vectors, projects them through the existing Sky View camera math with a retained projected-geometry cache, draws batched anti-aliased stick-figure lines, renders cached labels with wide-view fading, and reports constellation line/label/cache/timing metrics through the existing Sky View timing output. `photometry_app/ui/main_window.py` now replaces the old raster constellation image draw path with the vector overlay and adds Sky View options for constellation on/off, lines, labels, color, line width, line opacity, label opacity, and label size. Added focused loader/render/UI regressions in `tests/test_constellation_overlay.py` and `tests/test_main_window.py`.
- Fixed Sky View's near-pole mouse-drag camera instability by keeping drag motion vector-based on the unit sphere with a persistent camera up basis instead of rebuilding orientation from pole-singular horizon axes each frame. `photometry_app/ui/main_window.py` now preserves the supplied camera basis in orientation state, re-orthonormalizes the camera frame after drag updates, and falls back to the last valid tangent up direction when the world-up projection collapses near zenith or nadir. Added a focused near-zenith drag continuity regression in `tests/test_main_window.py` alongside the existing drag-direction, horizon-level, and pole-crossing checks.
- Revised the Sky View pole fix to remove the special world-up yaw cone and keep one continuous forward/up/right camera basis through drag. `photometry_app/ui/main_window.py` now persists camera right alongside forward/up, re-orthonormalizes with previous-frame up/right sign continuity so the basis cannot spontaneously flip handedness near zenith or nadir, and threads that basis into the render scene and GL uniform builders instead of reconstructing roll from derived alt/az during drag. `tests/test_main_window.py` now replaces the old horizon-level and azimuth-only near-pole checks with stronger near-zenith horizontal, vertical, and mixed drag regressions that assert forward-step bounds, positive up/right continuity, and continuous projected motion of a near-center test star.
- Revised the Sky View camera again to keep the forward-vector drag update pole-stable while making the final render basis horizon-locked. `photometry_app/ui/main_window.py` now rebuilds the final camera right/up from the local horizon frame using `cross(forward, world_up)` with sign continuity from the last valid horizon-right basis near zenith and nadir, so the ground remains screen-parallel without reintroducing the old pole jump. `tests/test_main_window.py` now restores normal-altitude horizon-alignment checks and adds pole-exit plus no-accumulated-roll regressions on top of the near-zenith continuity coverage.

## 2026-05-26
- Added Moon Phase 5H tiled projection correctness, tile coverage stability, and normal-tile sanity. `scripts/build_moon_tiles.py` now generates synthetic UV/grid, flat-normal, and known-bump Moon source maps; validates source-vs-tile UV sampling across random samples, tile boundaries, `u = 0/1` seam, poles, and multiple LODs; reports LOD luminance consistency; and validates tiled normal-map neutrality/sanity. `photometry_app/ui/moon_system.py` now smooths and conservatively scales displacement-derived normal maps, expands the fixed GLSL tiled path to six samplers, selects lower-LOD parent tiles for broad stable coverage when high-LOD tiles exceed the sampler budget, and reports visible/selected/bound tile counts plus visible/fallback coverage estimates. `photometry_app/ui/main_window.py` now adds synthetic global/tiled UV-grid debug render modes and a `Disable tiled normals` fallback switch so tiled albedo can run with the global normal map while normal tiles are being validated. `scripts/moon_visual_smoke.py`, `tests/test_build_moon_tiles.py`, `tests/test_moon_system.py`, `tests/test_main_window.py`, and `tests/test_moon_visual_smoke.py` now cover synthetic tile builds, UV equivalence, seam/pole/v-orientation checks, LOD luminance stability, coverage estimates, zoom-step binding stability, normal generation sanity, tiled-normal fallback, and the new smoke diagnostics.

- Added Moon Phase 5G real lunar tile asset generation and validation. `scripts/build_moon_tiles.py` now builds the runtime Moon tile pyramid directly from the real `textures/moon_lroc_color_16bit_srgb_8k.tif` albedo source plus either a supplied normal map or normals derived from `textures/moon_ldem_16.tif`, writes a build manifest, and validates tile counts, readable images, matching normal pairs, tile sizes, UV-layout metadata, and `MoonTiledTextureManager` compatibility. `scripts/moon_visual_smoke.py` now reports whether real Moon tile assets are present, whether the tiled shader path activates at high zoom with those assets, the resident real albedo/normal tile counts, the selected real tile keys, and the high-zoom global-texture warning state for the real asset tree. Added focused synthetic tests for the builder, validator, seam-edge preservation, and the real-tile smoke activation report, and documented the real build/validate workflow in `assets/moon_tiles/README.md`.

- Added Moon Phase 5F high-zoom source, tone, and background correction. `photometry_app/ui/main_window.py` now computes Moon background suppression from the final rendered Moon diameter instead of only the field-width helper, reports Moon diameter/threshold/focus/suppression diagnostics in the developer overlay, and warns in debug mode when high Moon inspection zoom is still using only the global Moon texture. `photometry_app/ui/moon_system.py` now remaps Moon albedo before lighting, applies a bounded lighting scalar so `lighting_only` and normal rendering no longer flatten into a pure white hemisphere, and hardens UV seam routing for wrapped tile bounds while keeping longitude texture wrap on `GL_REPEAT`. `tests/test_main_window.py`, `tests/test_moon_system.py`, `scripts/moon_visual_smoke.py`, and `assets/moon_tiles/README.md` now cover high-zoom suppression activation, debug-mode suppression persistence, bounded albedo/lighting helpers, seam-stable UV routing, the high-zoom global-texture fallback warning, and the updated tile seam guidance.

- Added Moon Phase 5E texture/lighting isolation and final high-zoom tuning. `photometry_app/ui/main_window.py` now exposes a developer-only Moon debug section in the Sky View options popup with live render-mode switching, diagnostics overlay toggling, global-only versus tiled-only isolation controls, tone-map/normal/earthshine bypass toggles, and a `Ctrl+Shift+M` render-mode cycle shortcut; the Moon diagnostics snapshot now reports split terrain-strength, terminator-band, source-mix, and tone-map state from the active draw path. `photometry_app/ui/moon_system.py` now separates global and terminator terrain-normal strength in the shader, adds directional earthshine, optional tone-mapping bypass, tiled-texture-only isolation when a tile is present, and tighter high-zoom tone/color defaults to reduce chalky highlights and make terminator relief read more clearly. `tests/test_main_window.py`, `tests/test_moon_system.py`, and `scripts/moon_visual_smoke.py` now cover Phase 5E debug isolation/state propagation, split terrain-strength bounds, color-preservation constants, and the bright-ray smear isolation sequence.

## 2026-05-25
- Added Moon Phase 5D high-zoom visual correction and overlay alignment. `photometry_app/ui/main_window.py` now suppresses the stale point-source Moon marker/ring when the extended rendered Moon disc is active, reanchors Moon hit-testing to the rendered disc center/radius, draws a clean disc-edge Moon label in that mode, adds stricter Moon-only high-zoom background suppression, and exposes developer-only Moon debug controls for render mode plus tiled-texture bypass/global-texture-only fallback. `photometry_app/ui/moon_system.py` now supports developer render modes (`albedo_only`, `lighting_only`, `normal_visualization`, and `uv_debug`) in the GLSL 120 Moon shader, reports the active debug mode through the uniform path, and retunes high-zoom tone mapping, contrast, earthshine, terminator softness, and terrain-normal strength to reduce chalky highlights and make the dark side/terminator read more naturally. `scripts/moon_visual_smoke.py` now reports Phase 5D overlay-alignment/debug/tone diagnostics, and focused regressions cover the extended-body overlay path, debug-mode normalization/plumbing, debug render-mode shader uniform values, and the updated tone/shader constants.
- Stabilized the Moon Phase 5C focused-zoom floor after live Moon drift. Sky View now latches Moon inspection zoom once a Moon-centered view legitimately enters sub-`2°` FOV, so later time flow, Moon drift, or off-center panning does not let the normal all-sky floor snap the camera back to `2°x1°`; the latch releases after returning to the normal zoom range or explicitly targeting a non-Moon object. Added regressions for no-selected-Moon drift, panning after the Moon leaves center, and latch release back to normal all-sky behavior, plus a drift-without-selected-Moon sequence in `scripts/moon_visual_smoke.py`.
- Fixed the Moon Phase 5C focused-zoom camera/FOV clamp so Sky View keeps one authoritative horizontal field-width scalar and derives the vertical FOV from the final viewport aspect ratio. Moon-focused zoom now clamps the effective minimum once before applying the camera state, allowing inspection zoom below the normal 2-degree floor without pinning the vertical axis at 1 degree or snapping back to `2°x1°`; normal all-sky minimum zoom remains unchanged when Moon focus is inactive. Added focused regressions for aspect-preserving Moon FOV pairs, repeated zoom-in no-wrap behavior, footer FOV formatting, aspect-ratio changes after resize, and a smoke-report Moon-focused zoom sequence in `scripts/moon_visual_smoke.py`.
- Added Moon Phase 5C visual tuning and high-zoom presentation polish. `photometry_app/ui/moon_system.py` now uses more conservative Moon-specific exposure plus highlight compression to protect bright highlands and ray systems, stronger but still bounded earthshine floor/phase response to avoid a pure-black dark side when earthshine is enabled, and a tighter terminator-weighted terrain-normal profile with an additional high-zoom terrain boost that stays concentrated near the terminator. Tile diagnostics now report whether the global albedo path, global normal path, tiled albedo path, and tiled normal path are active, along with bound tile counts and fallback reasons. `photometry_app/ui/main_window.py` now adds Moon-focused zoom limits so the Moon can fill or exceed most of the viewport when selected or centered, while applying Moon-only background star/deep-sky fade and sprite-size clamps at extreme inspection zoom without changing normal all-sky behavior. `scripts/moon_visual_smoke.py` now includes the richer render-path diagnostics in its scenario and mock-tile reports, and focused tests cover visual-setting bounds, highlight compression boundedness, render-path diagnostics, Moon-focused background presentation, and Moon-specific zoom activation. Crater labels, mesh displacement, and full 3D lunar terrain remain future work.
- Added Moon Phase 5B high-zoom tiled shader sampling. `photometry_app/ui/moon_system.py` now binds a deterministic fixed set of resident high-zoom Moon albedo tiles through GLSL 120 samplers, pairs matching normal tiles when available, passes per-tile UV bounds into the shader, and blends tile samples back toward the global albedo/normal path near tile edges. Missing, delayed, incomplete, or out-of-active-set tiles keep falling back to the existing global Moon texture path without flicker; missing normal tiles still allow tiled albedo while terrain normals fall back to the global normal map or sphere geometry. Sky View now passes prepared resident bindings into the renderer, `scripts/moon_visual_smoke.py` reports active mock tiled-shader and missing-normal fallback cases, and focused tests cover sampler limits, deterministic binding, UV route metadata, normal fallback, and current-frame texture protection. Crater labels, mesh displacement, and full 3D lunar terrain remain future work.
- Added Moon Phase 5A high-zoom lunar tile streaming infrastructure while preserving the existing global Moon texture renderer as the visible fallback. `photometry_app/ui/moon_system.py` now includes a `MoonTiledTextureManager` with documented 2:1 equirectangular tile layout, async albedo/normal tile discovery and decoding, tile activation threshold plus hysteresis, deterministic visible-disc tile selection using the same UV convention as the GLSL global texture path, lower-resolution/resident tile fallback accounting, current-frame tile cache eviction protection, and tile diagnostics for requested/resident/missing/pending tiles and memory estimate. Sky View prepares tile selections before native painting and never performs tile IO or decoding in the draw path; `MoonRendererGL` remains on the compatible global texture path in this Phase 5A pass, with full multi-tile GLSL sampling deferred to Phase 5B. Added `assets/moon_tiles/README.md` and high-zoom tile diagnostics/scenarios to `scripts/moon_visual_smoke.py`. Crater labels, mesh displacement, and full 3D lunar terrain remain future work.
- Added Moon Phase 4B SPICE setup, diagnostics, and validation tooling. The project now documents the optional `assets/spice` kernel layout, expected NAIF leapseconds/ephemeris/lunar orientation/frame kernels, and Windows plus Linux/macOS environment-variable setup in `assets/spice/README.md`. Added `scripts/check_moon_spice.py` to report `spiceypy` importability, selected provider mode, discovered and loaded kernels, active provider, fallback reasons, representative finite/orthonormal orientation checks, and SPICE-vs-approx comparison summaries when SPICE is active. Sky View now logs a single concise Moon orientation provider line during Moon system initialization, `scripts/moon_visual_smoke.py` keeps its existing report while adding flattened orientation provider fields, and provider diagnostics now distinguish invalid kernel directories from ordinary missing-kernel fallback. `MoonRendererGL` remains unchanged, safe approximate fallback remains the default for unconfigured users, and high-zoom tiled lunar terrain remains future work.
- Added Moon Phase 4A SPICE/NAIF lunar orientation provider plumbing without changing `MoonRendererGL`. `photometry_app/ui/moon_system.py` now preserves the existing `LunarOrientationProvider` interface while adding `LunarOrientationSpiceProvider`, `select_lunar_orientation_provider(...)`, `auto` / `approx` / `spice` selection modes, kernel-path discovery from explicit environment configuration, and clear diagnostics when `spiceypy` or kernels are unavailable. `MoonEphemeris` now uses this provider selection by default, falls back to `LunarOrientationApprox` safely when SPICE cannot be activated, and continues to compute expensive orientation samples only through the existing MoonState cache/interpolation path rather than paintGL. `scripts/moon_visual_smoke.py` now reports the active orientation provider and emits SPICE-vs-approx comparison diagnostics when SPICE is active. High-zoom tiled lunar terrain remains out of scope for this pass.
- Added Moon Phase 3.5 hardening and calibration for the Sky View Moon renderer. The terrain/earthshine/atmosphere path now uses centralized conservative `MoonVisualSettings` values in `photometry_app/ui/moon_system.py`, with the GLSL 120 shader generated from the same constants used by CPU-side atmosphere, earthshine, and terrain influence helpers. Missing albedo files, missing normal maps, missing `moon_ldem_16.tif`, unreadable image files, delayed normal LOD readiness, and partially prepared normal LODs are covered by focused tests and fail without crashing; missing normals keep the renderer in albedo-only mode with `u_has_moon_normal_map` false. Added one-month time-scrub finite-state coverage, repeated zoom/LOD threshold checks, current-frame albedo+normal texture eviction protection tests, stricter debug environment flag parsing, near-horizon draw-state stability checks, and `scripts/moon_visual_smoke.py` for lightweight visual/stress scenario diagnostics. Apparent refracted horizon Moon rendering and high-zoom tiled lunar terrain/displacement remain future work.
- Sky View search now includes a live Moon target. Searching for `Moon` or `Luna` in `photometry_app/ui/main_window.py` builds a dynamic topocentric `SkyAtlasObject` from the current observer/time ephemeris, recenters the camera on the Moon immediately, and updates the object panel/status text without requiring the Moon to already be inside the current field of view or present in the static star/deep-sky catalog.
- Fixed a Moon GL startup regression on drivers that optimize unused uniforms away. `photometry_app/ui/moon_system.py` no longer requires the stale `u_viewport_size_px` uniform, so the Sky View Moon renderer stays enabled during manual runs instead of disabling itself for the session before the first draw.
- Added Moon Phase 3 terrain-aware visual realism while keeping the Sky View renderer fast and draw-state driven. `photometry_app/ui/moon_system.py` now prepares optional lunar normal-map LODs asynchronously, deriving them from the NASA CGI Moon Kit LDEM height texture when no separate normal map is supplied, and falls back cleanly to albedo-only shading when normal data is missing or still loading. `MoonRendererGL` remains GLSL 120 and renderer-dumb: it receives precomputed draw/orientation/light/atmosphere inputs, samples an optional normal texture only for local terminator lighting detail, keeps the analytic sphere normal authoritative for disc/limb/phase geometry, uses directional Moon-local Earth vectors for conservative earthshine, and applies cheap altitude-based extinction/reddening. `photometry_app/ui/main_window.py` now requests normal LODs without blocking Moon draws and exposes default-off developer flags for exaggerated terrain normals, disabling terrain normals, disabling earthshine, and disabling atmosphere effects. SPICE/NAIF physical orientation and high-zoom tiled lunar terrain/displacement remain future work.
- Added Phase 2 lunar orientation for the Sky View Moon renderer. `photometry_app/ui/moon_system.py` now exposes physically meaningful lunar libration longitude/latitude, lunar north position angle, parallactic angle, Moon body-frame matrices, and Moon-local Sun/Earth/observer vectors through a replaceable `LunarOrientationProvider` seam with the current `LunarOrientationApprox` backend ready for a future SPICE/NAIF implementation. Cached Moon samples now interpolate orientation with wrap-safe scalar angles and quaternion slerp matrices, so fast time scrubbing stays smooth without per-frame orientation recomputation. `photometry_app/ui/main_window.py` now applies the final active-camera `view_to_moon_matrix` to the shader path and includes a disabled-by-default `moon_debug_orientation_overlay` / `CITIZEN_PHOTOMETRY_SKY_VIEW_MOON_DEBUG_ORIENTATION` validation overlay showing lunar north, equator, central meridian, subsolar point, and sub-Earth point. High-zoom terrain relief from the LDEM texture remains future work.
- Added the first high-performance Sky View Moon renderer milestone. `photometry_app/ui/moon_system.py` now separates cached topocentric Moon ephemerides, async NASA CGI Moon Kit texture LOD preparation, runtime ephemeris validation, and a GLSL 120 screen-space sphere impostor that reconstructs normals, samples the lunar albedo texture, applies a softened phase terminator plus earthshine floor, and protects the current Moon texture from GPU cache eviction. `photometry_app/ui/main_window.py` now builds a Moon draw state from the active observer/time/camera scene and draws it through the OpenGL path without blocking UI construction or paint on high-resolution texture loading. Added focused Moon system tests plus a Sky View integration regression for projected Moon position/size in `tests/test_moon_system.py` and `tests/test_main_window.py`.
- Added continuous live time to Sky View in `photometry_app/ui/main_window.py`. Sky View now starts with time flowing by default, advances the selected UTC once per second while the mode is active so the horizon sky moves naturally, and exposes a compact play/pause toolbar button to stop or resume that flow. The Date/Time popup was simplified to just the UTC editor and `-1 d / -1 h / Now / +1 h / +1 d` controls; the explanatory Local/UTC/site text block below those buttons was removed for a cleaner in-canvas workflow. Added focused regressions for default live-time advancement, pause behavior, date editor syncing, and the cleaned popup in `tests/test_main_window.py`.
- Finished the Sky View immersive chrome/footer polish in `photometry_app/ui/main_window.py`. The current-page stacked host is now frameless so it no longer paints the thin host strips above or below Sky View, and fullscreen still hides the main window menu/status chrome through the window-state sync path. The Sky View footer now draws fixed-width bottom-left segments so changing FOV/coordinate digits do not shove later fields around; the saved site name appears by itself first, followed by compact `FOV`, `RA/Dec`, and `Alt/Az` columns, and the old `Sky atlas ... shown` segment stays removed. Added focused regressions for the frameless stack and the new footer segment output in `tests/test_main_window.py`.
- Improved the Sky View Observer Location workflow. In fullscreen, the Observer Location dialog now opens inside the Sky View overlay instead of as a separate native modal window, avoiding the fullscreen compositor flash. The latitude/longitude/elevation fields in `photometry_app/ui/sky_view_location.py` are now manual numeric inputs with spin arrows removed, and the map view has brighter tile fallback/overlay treatment plus a stronger selected-site marker and coordinate pill so the chosen location is easier to see without leaving the app's dark Sky View design. Added focused regressions for the no-arrow inputs and fullscreen embedded dialog host in `tests/test_main_window.py`.
- Replaced the Sky View fullscreen popup display path in `photometry_app/ui/main_window.py` with an in-window overlay host for the embedded search/date-time/location/options cards. Fullscreen no longer depends on a separate `QMenu` window painting above the `QOpenGLWidget`; the existing popup card is reparented into the immersive overlay layer, positioned above the bottom dock, and restored to the menu for windowed mode. Updated the fullscreen regression in `tests/test_main_window.py` to assert that the options card is visible inside the overlay host rather than merely active as an off-window menu.
- Continued the damaged `photometry_app/ui/main_window.py` recovery by reconstructing the missing startup/HR helper block that the stale backup had dropped, including `_apply_startup_theme`, inline progress-bar styling, and the HR prepare-button width/style/busy-state helpers. This unblocks `MainWindow` construction again without reintroducing the Sky View fullscreen popup or spinbox-arrow regressions.
- Restored the live `photometry_app/ui/main_window.py` from the intact local backup after the on-disk file was truncated again mid-function, then reapplied the Sky View popup hardening so the recovered file still keeps fullscreen popups visible and both `Max mag` / `Bloom` spinbox arrows clickable. This also explains the editor overwrite/compare prompts: the on-disk file had been changed outside the current editor buffer.
- Re-landed the active Sky View popup fix into the current `photometry_app/ui/main_window.py` runtime after the code and log drifted apart. The fullscreen popup helper now keeps the search/date-time/location/options menus on their original overlay host instead of reparening them into the fullscreen window, and the popup-local `Max mag` and `Bloom` spinboxes now reserve an explicit button gutter so both increment and decrement arrows stay clickable.
- Hardened the Sky View options popup spinboxes for `Max mag` and `Bloom`. The popup now resolves the options-card width at show time, gives those two spinboxes a guaranteed live width/button gutter, and styles the popup-local spinbox button column explicitly so the increment hit area is no longer lost on some window/layout combinations. Focused validation passed with `py_compile` on `photometry_app/ui/main_window.py` and the updated `test_sky_view_ground_and_appearance_controls_sync_to_canvas` slice.
- Restored the runnable `MainWindow` source after a local file-state mismatch left the on-disk `photometry_app/ui/main_window.py` truncated behind the editor working copy. The recovery now rehydrates the full class body again, bringing back the missing HR callbacks such as `_browse_for_hr_source_image` and `_sync_hr_prepare_button_width`, so `MainWindow.__init__` wires the HR controls cleanly and Photometry launches normally again.
- Fixed Sky View's immersive toolbar regression after the grid/overlay split. The fullscreen popup helper now anchors search/date-time/options menus against the active overlay host and clamps them onto the active screen, so those popups remain visible in fullscreen instead of disappearing off-host. The old single-grid popup wiring was removed from `photometry_app/ui/main_window.py`, the toolbar now drives separate Equatorial Grid, Alt-Az Grid, and Constellation Lines toggle buttons with distinct icons, and the new constellation TIFF overlay is wired through the existing projected celestial-texture path.
- Raised `Fully Milky Way` high-resolution mode to use the 16k stitched manifest level when available, while still refusing the unsafe 32k one-piece upload. The default `Atlas` Milky Way presentation is now brighter, higher contrast, more saturated, and uses a stronger stable opacity so the fully loaded Advanced/Full view keeps the richer look that previously appeared only during transient lazy-loading blends.
- Corrected the follow-up Milky Way mode behavior after live review. `Basic Milky Way` now uses the same manifest-stitched base source as the Advanced underlay when a tiled manifest is available, instead of falling back to the old preview PNG with the wrong orientation. `Fully Milky Way` now falls back to that manifest base if the larger one-piece stitched source cannot be built, so it no longer drops back to the wrong simple preview path. The tiled OpenGL draw path now masks resident tile footprints out of the manifest base with the stencil buffer before drawing the tile color pass, so Advanced streaming tiles replace their base area instead of being alpha-blended on top of it and temporarily looking brighter than the always-on layer.
- Fixed the plain-app Milky Way mode selector so it now has a tiled asset to operate on without requiring a manual environment variable. `photometry_app/core/milky_way_assets.py` now prefers the current 32k manifest-backed assets in `textures/milky_way_tiles_32k_padded_lzw_benchmark`, then the padded PNG 32k tree, then the legacy paths, instead of falling straight through to the single preview PNG on a normal workspace launch. That was the reason `Basic`, `Advanced`, and `Fully` appeared identical in the running app: the resolver was choosing `preview_fallback`, so all three modes collapsed to the same simple texture path.
- Added user-selectable Sky View Milky Way render modes in the options popup. `Basic Milky Way` now uses the simple single-texture overlay and never requests manifest tiles, `Advanced Milky Way` keeps the existing simple-underlay plus tiled-detail behavior, and `Fully Milky Way` stitches one capped high-resolution manifest level into a single GL texture so it loads once and draws without tile streaming. The new full-texture path preserves the manifest tile upload-orientation parity fix, reports `render_mode` in the Milky Way runtime diagnostics, and refuses levels above the configured one-texture safety cap instead of attempting an unsafe full 32k upload.
- Fixed the live 32k TIFF-LZW tiled OpenGL Milky Way duplicate-layer and zoom-crash blockers. `photometry_app/ui/sky_view_milky_way_gl.py` now keeps the manifest base underlay only while requested detail tiles are still loading, then intentionally skips it in normal mode once all requested tiles are resident so the low-resolution base cannot remain visible as a second Milky Way beneath the high-resolution tile layer. The GL tile cache eviction path also now protects every texture requested for the current frame, preventing zoom-driven LRU eviction from destroying a `QOpenGLTexture` that the same frame is about to bind/draw, which matches the observed native access-violation crash during zoom.
- Corrected the remaining tiled-layer orientation mismatch when the Milky Way display is mirrored horizontally. The visible-tile picker in `photometry_app/ui/main_window.py` now stays in source-space tile coordinates instead of flipping the manifest selection grid, and the shader-tile ownership/local-UV path in `photometry_app/ui/sky_view_milky_way_gl.py` now explicitly converts the display-flipped screen sample back into source-space before deciding which tile owns the pixel and where to sample inside that tile. That keeps the stitched manifest base and the high-resolution tiled layer on the same celestial orientation instead of letting the tiled pass cross the base/background in the opposite direction.
- Corrected the manifest-base loading underlay orientation for the tiled OpenGL Milky Way path. The L0 stitched `manifest_base` preview is now vertically pre-flipped before the shared GL texture-upload helper applies its upload transform, matching the net orientation of prepared detail tile textures while tiles are still loading. Fresh focused `galactic_center_wide` visual captures now show `base_only`, `tiles_only`, and settled `normal` aligned instead of crossing, and a new asymmetric synthetic-tile regression locks the upload-parity contract.
- Updated the TIFF-LZW visual smoke authority rules so `base_skipped_reason=all_tiles_resident` is considered authoritative only with full tile residency and zero missing/deferred tiles. The refreshed `_tmp_sky_view_milky_way_lzw_validation/validation_report.txt` now marks `galactic_center_detail` authoritative with `renderer=opengl`, `asset_mode=tiled_manifest`, `base_drawn=0`, `base_skipped_reason=all_tiles_resident`, `tile_resident=48`, `missing=0`, and `deferred=0`, and the fresh `galactic_center_detail_normal.png` no longer shows the persistent low-resolution underlay under the final tile layer.
- Focused validation for this renderer fix completed cleanly: `py_compile` passed on the touched GL/runtime harness/test files, `pytest tests/test_sky_view_milky_way_gl.py tests/test_sky_view_lzw_visual_smoke.py -q` passed (`25 passed`, existing `QImage.mirrored` deprecation warnings only), the added flipped-orientation regression plus the existing tiled-runtime-input test passed (`pytest tests/test_main_window.py -k "milky_way_visible_tile_coordinates_stay_in_source_space_when_flipped or milky_way_gl_runtime_inputs_build_manifest_tile_requests" tests/test_sky_view_milky_way_gl.py -q` -> `2 passed`), the one-view TIFF-LZW visual wrapper rerun exited cleanly on the patched shader path, editor diagnostics reported no issues in the touched GL file, and `_tmp_app_startup_smoke_result.json` reports `startup_ok`.
- Finished the harness-only `polar_medium` reliability pass in `_tmp_sky_view_lzw_visual_smoke.py` without changing renderer/runtime behavior. The broad wrapper now persists partial polar settle progress as it changes, caps isolated polar settle on an explicit `8.0 s` wall-clock timeout, captures the polar `normal` artifact immediately after that bounded settle window through the live widget surface, skips the optional debug variants for polar, and skips the post-loop manifest-activation probe on isolated polar reruns so the wrapper exits cleanly after the polar report is flushed and cleanup completes.
- Final isolated polar rerun now completes successfully and writes a durable polar entry into `_tmp_sky_view_milky_way_lzw_validation/validation_report.json` plus `_tmp_sky_view_milky_way_lzw_validation/validation_report.txt`, with screenshot `_tmp_sky_view_milky_way_lzw_validation/screenshots/polar_medium_normal.png` and `last_marker=polar_cleanup_complete`. The polar artifact is intentionally marked non-authoritative rather than being dropped: the final report shows `renderer=opengl`, `asset_mode=tiled_manifest`, `base_drawn=1`, `missing=0`, `selected_level=L4`, but still `deferred=59` and `normal_authoritative=False`. The recorded failed authority conditions are `requested_equals_resident`, `deferred_zero`, `capture_after_full_residency`, and `capture_after_settled_paint`, which matches the observed live settle progression where the polar view kept requesting about `95` tiles and only reached partial residency before the bounded capture.
- Focused validation for this polar harness pass completed cleanly: `pytest tests/test_sky_view_lzw_visual_smoke.py -q` passed (`6 passed`), `py_compile` passed on `_tmp_sky_view_lzw_visual_smoke.py` and `tests/test_sky_view_lzw_visual_smoke.py`, and the final isolated wrapper rerun printed the report paths and exited cleanly. No Photometry startup smoke was rerun for this pass because only temporary harness/test files changed.

## 2026-05-24
- Made the broad `_tmp_sky_view_lzw_visual_smoke.py` wrapper authoritative for the final wide-view normal artifact by moving authority decisions onto the actual captured frame and persisting per-variant checkpoints as each screenshot is written. The wrapper report now records `authoritative`, `authoritative_reason`, `failed_authoritative_conditions`, `renderer`, `asset_mode`, `source_level`, `requested/resident/missing/deferred`, `base_drawn`, `base_source`, `tile_decode_failures`, `capture_after_full_residency`, `capture_after_settled_paint`, plus manifest-base metadata showing the `content_region`-cropped base-stitch contract from `textures/milky_way_tiles_32k_padded_lzw_benchmark/manifest.json`. After rerunning the wrapper in fresh one-view task contexts, `_tmp_sky_view_milky_way_lzw_validation/validation_report.json` now marks `galactic_center_wide_normal.png` authoritative with `requested: 28`, `resident: 28`, `missing: 0`, `deferred: 0`, `base_drawn: true`, `base_source: "manifest_base"`, `tile_decode_failures: 0`, `capture_after_full_residency: true`, `capture_after_settled_paint: true`, and `base_stitch_mode: "content_region_cropped"`. The broad-wrapper wide normal screenshot visually matches the focused helper's clean wide capture with no dark polygon. The same wrapper path also refreshed authoritative normal captures for `cygnus_medium`, `orion_medium`, `ra_seam_medium`, `wide_field`, and `galactic_center_detail`; the remaining caveat is that an isolated `polar_medium` wrapper run still exits before persisting a captured report entry, so that one view is still the only incomplete broad-wrapper artifact in this pass.
- Finished the 32k TIFF-LZW Milky Way release-hardening fix for the remaining wide-view base-underlay artifact. `photometry_app/ui/main_window.py` now crops each padded manifest base-level tile to the manifest `content_region` before stitching the low-resolution `manifest_base` image, instead of painting the full padded tile into the stitched base. That removed the false dark polygon that only appeared when the base underlay was present. Focused validation through `_tmp_sky_view_milky_way_detail_validate.py` now shows clean `galactic_center_wide` `base_only`, `normal_with_base_forced`, and settled `normal` captures on the real `textures/milky_way_tiles_32k_padded_lzw_benchmark/manifest.json` runtime, with the wide debug banner confirming `requested 28 resident 28 missing 0 deferred 0` at the settled state.
- Cleaned up the release-hardening documentation for the current Milky Way asset contract. `README.md` now calls out the manifest-backed NASA SVS 32k tiled Milky Way path and the production `tiff_lzw` default, and `DOCUMENTATION.md` now documents the tiled-asset generation/runtime contract: `scripts/generate_milky_way_tiles.py` defaulting to `--tile-format tiff_lzw`, continued `png` and `tiff_deflate` support, recommended `--pad-gutter 1`, the invariant that normal rendering keeps the manifest base under translucent detail tiles, the `CITIZEN_PHOTOMETRY_SKY_VIEW_MILKY_WAY_DEV_PANEL=1` developer panel, the Qt image-format smoke commands, and the runtime TIFF decode failure diagnostic string.
- Final executable validation for this hardening pass completed cleanly: `pytest tests/test_milky_way_assets.py tests/test_qt_image_formats.py tests/test_sky_view_milky_way_gl.py tests/test_main_window.py::SkyViewMilkyWayTextureTest::test_milky_way_runtime_format_reports_tile_renderer -q` passed (`42 passed`, existing deprecation warnings only), `py_compile` passed on the touched runtime and validation files, and `_tmp_startup_smoke.py` rewrote `_tmp_app_startup_smoke_result.json` with `status: "startup_ok"`. The broad `_tmp_sky_view_lzw_visual_smoke.py` wrapper was also hardened so `--views ...` reruns replace old selected entries instead of silently reusing stale report rows, but the focused detail helper remains the authoritative visual proof for the single `galactic_center_wide` release-blocker screenshot because that broader wrapper still does not consistently produce a trustworthy final-wide `normal` artifact in the same way.
- Built the exact Windows shipping-style bundle from `CitizenPhotometryDebug.spec` with `.venv\Scripts\python.exe -m PyInstaller --noconfirm --clean --distpath _tmp_shipping_dist --workpath _tmp_shipping_build CitizenPhotometryDebug.spec` and verified the real artifact under `_tmp_shipping_dist/CitizenPhotometryDebug`. The finished one-dir bundle is about `394152670` bytes in total, the executable `_tmp_shipping_dist/CitizenPhotometryDebug/CitizenPhotometryDebug.exe` is about `36000052` bytes, and the packaged Qt imageformats directory exists at `_tmp_shipping_dist/CitizenPhotometryDebug/_internal/PySide6/plugins/imageformats` with `qtiff.dll`, `qwebp.dll`, `qjpeg.dll`, plus the other standard imageformat plugins present. No spec change was needed for TIFF support on the exact shipping path.
- Added a packaged-app smoke mode to `photometry_app/main.py` backed by the shared helper `photometry_app/core/qt_image_format_smoke.py`, so the shipping executable can self-report Qt image-format capability without entering the normal UI path. The exact shipping executable wrote `_tmp_qt_image_format_smoke_result_shipping.json` with `success: true`, `has_tiff: true`, `has_png: true`, `has_webp: true`, plugin root `_tmp_shipping_dist/CitizenPhotometryDebug/_internal/PySide6/plugins`, bundled `imageformats` plugin directory containing `qgif.dll`, `qico.dll`, `qjpeg.dll`, `qtiff.dll`, `qwebp.dll`, and successful embedded TIFF-LZW decode with exact RGB match, no orientation flip, and no channel swap.
- With the exact shipping bundle now verified, switched the Milky Way tile-generation default to TIFF-LZW in `photometry_app/core/milky_way_tile_generator.py` and `scripts/generate_milky_way_tiles.py`. PNG support remains intact as an explicit supported output format and backward-compatible runtime manifest type, TIFF-deflate remains intact as the alternate TIFF-family output, and the runtime renderer/fallback/shader path was not changed in this pass. The script help text now states that production generation defaults to `tiff_lzw` while `png` and `tiff_deflate` remain supported.
- Reran focused validation after the default switch: `pytest tests/test_milky_way_assets.py tests/test_qt_image_formats.py -q` (`24 passed`), `pytest tests/test_sky_view_milky_way_gl.py tests/test_main_window.py::SkyViewMilkyWayTextureTest::test_milky_way_runtime_format_reports_tile_renderer -q` (`18 passed`, existing deprecation warnings only), `py_compile` on the touched runtime/generator files, the source app smoke mode (`_tmp_qt_image_format_smoke_result_source_cli.json`), the exact shipping smoke mode (`_tmp_qt_image_format_smoke_result_shipping.json`), and startup smoke (`_tmp_app_startup_smoke_result.json`: `startup_ok`). A live one-view GUI validation against `textures/milky_way_tiles_32k_padded_lzw_benchmark/manifest.json` also finished cleanly in `_tmp_sky_view_milky_way_validation/validation_report.json`: `galactic_center_detail` stayed on `renderer:opengl`, `asset:tiled_manifest`, `tile_format:tiff`, `tile_compression:tiff_lzw`, `cpu_fallback:0`, `tile_decode_failures:0`, and reached `all_requested_tiles_resident:1`; the prior warm/cold trend remains consistent with the earlier LZW benchmark summary (`first_visit` about `24.9 ms` layer vs `11.35 ms` warm second visit for the detail case).
- Verified Qt TIFF image-plugin availability for the padded 32k Milky Way TIFF-LZW path in both the development environment and a release-like PyInstaller bundle, without switching the production default away from PNG. Added `photometry_app/core/qt_image_formats.py` as a shared runtime capability helper for `QImageReader.supportedImageFormats()`, Qt plugin/library search paths, and discovered `imageformats` plugins; `photometry_app/ui/sky_view_milky_way_gl.py` now records a clearer tile decode failure reason (`TIFF tile decode failed; Qt TIFF image plugin unavailable or decode error.`), and `photometry_app/ui/main_window.py` now includes that failure reason in the Milky Way runtime string while logging one additional Qt image-format support line only when the Milky Way dev panel is enabled.
- Added the self-contained TIFF-LZW smoke harness `_tmp_qt_image_format_smoke.py`, which embeds a known 2x2 TIFF-LZW tile, checks Qt supported formats, decodes the sample through Qt, verifies dimensions and exact RGB values, and writes `_tmp_qt_image_format_smoke_result.json`. On this development machine, Qt reports TIFF/PNG/WebP support with plugin root `C:/Users/Kay/Desktop/Projects/Photometry/.venv/Lib/site-packages/PySide6/plugins`, `imageformats` directory `.../PySide6/plugins/imageformats`, and plugins including `qjpeg.dll`, `qgif.dll`, `qico.dll`, `qtiff.dll`, and `qwebp.dll`; the smoke passed with exact pixel match, no orientation flip, and no channel swap.
- Built a release-like PyInstaller smoke bundle under `_tmp_qt_smoke_dist/QtImageFormatSmoke` using the same `hook-PySide6.py` path that the current Windows packaging flow uses. The packaged bundle contains `_internal/PySide6/plugins/imageformats/qtiff.dll` alongside the other imageformat plugins, and the packaged executable wrote `_tmp_qt_image_format_smoke_result_packaged.json` with `success: true`, `decode_success: true`, `has_tiff: true`, exact RGB match, no orientation flip, and no channel swap. No packaging configuration change was needed in this pass: the current PyInstaller/PySide6 hook path already includes TIFF support in the verified release-like bundle.
- Added focused validation for the new packaging/runtime support path in `tests/test_qt_image_formats.py`, `tests/test_sky_view_milky_way_gl.py`, and `tests/test_main_window.py`, and reran the relevant checks: `py_compile` on the touched runtime/smoke files, `pytest tests/test_qt_image_formats.py tests/test_sky_view_milky_way_gl.py tests/test_main_window.py::SkyViewMilkyWayTextureTest::test_milky_way_runtime_format_reports_tile_renderer -q` (`21 passed`), `pytest tests/test_milky_way_assets.py -q` (`19 passed`), development TIFF smoke passed, packaged TIFF smoke passed, and `_tmp_app_startup_smoke_result.json` stayed `startup_ok`. Updated recommendation: it is now verified that the current release-like PyInstaller environment can decode TIFF-LZW, but keep PNG as the generic default until the actual shipping app bundle/install path is verified with the same check rather than assuming every future packaging variant will match this smoke bundle exactly.
- Finished the padded 32k Milky Way production-format experiment for TIFF-LZW without changing renderer behavior, shader logic, overlays, picking, CPU fallback, or the existing PNG support path. `photometry_app/core/milky_way_assets.py` now carries backward-compatible manifest tile-format metadata (`tile_format`, `compression`, `file_extension`, `codec`), `photometry_app/core/milky_way_tile_generator.py` plus `scripts/generate_milky_way_tiles.py` can generate `png`, `tiff_lzw`, or `tiff_deflate` pyramids, and `photometry_app/ui/sky_view_milky_way_gl.py` / `photometry_app/ui/main_window.py` now report manifest format plus tile decode success/failure diagnostics while keeping the runtime decode path extension-agnostic through `QImage.fromData(...)`.
- Built the real full-tree TIFF-LZW variant at `textures/milky_way_tiles_32k_padded_lzw_benchmark`, validated it against the padded PNG benchmark tree with `_tmp_tile_format_lzw_validation.json`, and benchmarked it live against the PNG-backed padded tree with `_tmp_sky_view_lzw_vs_png_benchmark.json` / `_tmp_sky_view_lzw_vs_png_summary_utf8.tsv`. Full-tree validation was clean for all `682/682` tiles with exact RGB equality, `0` pixel mismatches, `0` max channel delta, matching padded-content metadata, no orientation flip, and no channel swaps; the TIFF-LZW tree is about `1304.97 MB` versus `1241.60 MB` for padded PNG (about `+63.4 MB`, `+5.1%`). In the real GUI warm-region pass, the meaningful cold first visits improved clearly: `galactic_center_wide` dropped from about `39.9 ms` to `30.7 ms` layer time, `29.2 ms` to `19.0 ms` prepare, and `26.8 ms` to `16.0 ms` decode, while `galactic_center_medium` dropped from about `45.6 ms` to `35.3 ms` layer time, `30.9 ms` to `20.6 ms` prepare, and `28.2 ms` to `17.3 ms` decode. Warm second visits and GPU-cache-cleared revisits stayed broadly similar to PNG, which means the gain is in the cold decode/prepare path rather than the already-warm path.
- Added focused TIFF-LZW coverage in `tests/test_milky_way_assets.py`, `tests/test_sky_view_milky_way_gl.py`, and `tests/test_main_window.py`, reran the focused Milky Way test set (`41 passed`), compiled the touched production/experiment files with `py_compile`, and reran the app startup smoke (`_tmp_app_startup_smoke_result.json`: `startup_ok`). The dedicated LZW visual validation under `_tmp_sky_view_milky_way_lzw_validation` confirmed the requested runtime invariants on the captured views: OpenGL renderer, tiled-manifest asset mode, zero CPU fallback, zero decode failures, and zero missing tiles on the settled/captured runtime lines for `galactic_center_wide`, `cygnus_medium`, `orion_medium`, `ra_seam_medium`, `polar_medium`, `wide_field`, and `galactic_center_detail`. The helper still exits late in extended multi-view sweeps, so the final three views were completed as isolated screenshot-based smokes using the same wrapper.
- Decision for this pass: keep PNG as the generic default generator/runtime baseline for now, but treat TIFF-LZW support as production-ready in code and as the current best cold-path candidate for the padded 32k tree on this machine. The performance improvement is clear and the visual/runtime validation is clean, but switching the default asset format globally is still gated on packaged-build confidence that the Qt TIFF plugin/runtime support is present wherever Photometry ships. PNG remains the safe fallback, TIFF-deflate remains the smaller TIFF-family alternative, and the next change should only be a default switch once packaged-build TIFF availability is explicitly verified.
- Ran the production-format decision pass for the padded 32k Milky Way tile pyramid without changing normal renderer behavior or switching production assets yet. Added `photometry_app/core/milky_way_tile_format_benchmark.py` plus focused tests and temporary benchmark drivers to build representative sample copies, verify lossless RGB equality, benchmark Qt decode/convert time, benchmark runtime-like prepared-tile misses, and write the large-sample artifacts `_tmp_tile_format_large_sample_benchmark.json` and `_tmp_tile_format_large_sample_summary_utf8.tsv`.
- The large-sample benchmark covered `141` representative padded tiles across six requested sky regions and levels `L2`/`L3`/`L4`, comparing `png_current`, `tiff_deflate`, `tiff_lzw`, `tiff_raw`, and `webp_lossless` through the real runtime decode path (`QImage.fromData` + `convertToFormat(QImage.Format.Format_RGB888)`) and the runtime-like prepared-tile miss path. All tested variants were lossless on the sample (`141/141` pixel-equal, no orientation tags written, no RGB mismatches), and Qt reported native support for TIFF and WebP on this machine.
- Representative all-sample results: PNG stayed at about `12.90 ms` median decode / `14.53 ms` median prepare with a projected full padded tree around `1184.1 MB`; TIFF-deflate improved that to about `9.43 ms` median decode / `11.26 ms` median prepare while projecting to about `1103.9 MB`; TIFF-LZW improved further to about `8.34 ms` median decode / `10.19 ms` median prepare while projecting to about `1203.9 MB`; TIFF-raw was much faster still (`1.22 ms` decode / `2.88 ms` prepare) but projected to about `2117.4 MB`; WebP lossless was much smaller (about `757.8 MB`) but not materially faster than PNG (`12.37 ms` decode / `13.90 ms` prepare). Recommendation for the next implementation pass: prefer a TIFF-family production experiment over PNG, with TIFF-LZW as the decode-first winner and TIFF-deflate as the more size-balanced fallback; both carry the same Qt TIFF plugin/runtime dependency risk, while TIFF-raw is too large and WebP lossless does not solve the current decode bottleneck.
- Reduced the cold first-visit CPU prepare cost for the tiled OpenGL Milky Way path without changing visual output, shader ownership, overlays, or CPU fallback behavior. `photometry_app/core/milky_way_assets.py` now accepts backward-compatible padded-tile manifest metadata (`tile_width`, `tile_height`, `gutter_pixels`, `content_region`, `padded_tile`), `photometry_app/core/milky_way_tile_generator.py` plus `scripts/generate_milky_way_tiles.py` can now bake a 1-pixel offline gutter with `--pad-gutter 1`, and `photometry_app/ui/main_window.py` / `photometry_app/ui/sky_view_milky_way_gl.py` now thread that manifest contract into the runtime cache key and tile requests so pre-padded tiles skip runtime neighbor decode and border-copy work entirely. The old-manifest path still works, and it now also keeps a bounded decoded-tile LRU so adjacent cold prepares reuse already-decoded neighbor tiles instead of repeatedly reloading them.
- Added focused coverage for the new manifest/generator/runtime contract in `tests/test_milky_way_assets.py`, `tests/test_sky_view_milky_way_gl.py`, and `tests/test_main_window.py`. Validation passed for the new padded metadata, offline gutter generation with RA wrap and pole clamp behavior, the runtime fast path that skips padding for pre-padded tiles, decoded-neighbor reuse across adjacent old-manifest prepares, and the updated cache-key identity.
- Captured new cold-path benchmark artifacts for the real 32k tile set under `_tmp_tile_prepare_compare.json` and `_tmp_tile_format_decode_benchmark.json`, plus a real padded benchmark tile tree under `textures/milky_way_tiles_32k_padded_benchmark`. On the exact requested-tile sets from the existing warm-region run, cold prepared-tile time dropped from about `519.7 ms` to `391.8 ms` for the wide case (`-24.6%`), from about `1123.9 ms` to `645.7 ms` for the medium case (`-42.5%`), and from about `594.4 ms` to `280.6 ms` for the detail case (`-52.8%`), with `tile_padding_ms` falling from `442.0 / 1077.1 / 567.6 ms` to `0.0 ms` in those cases while warm revisits stayed at `0 ms` prepare/read/decode/convert/padding. The padded benchmark copy increased disk usage only slightly, from about `1179.4 MB` to `1184.1 MB` across the full 32k tree, while representative single-tile format sampling showed TIFF-deflate decoding faster than PNG/WebP on this stack (`~11.2 ms` decode for a padded L4 tile vs `~14.5 ms` current PNG and `~15.6 ms` lossless WebP) but was not switched in production during this pass.
- Added the bounded CPU-side prepared-tile cache pass for Sky View's tiled OpenGL Milky Way renderer instead of changing the shader architecture or adding async upload. `photometry_app/ui/sky_view_milky_way_gl.py` now keeps an LRU cache of decoded, format-converted, neighbor-padded upload-ready tile images keyed by manifest/tile/padding identity, reuses those prepared images on later GPU misses and reuploads, tracks CPU cache hits/misses plus prepare/padding time, and evicts against the new `CITIZEN_PHOTOMETRY_SKY_VIEW_MILKY_WAY_CPU_TILE_CACHE_MB` budget (`512 MB` default). `photometry_app/ui/main_window.py` now builds one authoritative manifest-aware tile cache key, threads the CPU-cache budget into the GL draw path, and surfaces CPU/GPU cache metrics in the runtime/detail strings; focused tests were added for LRU eviction, cache-key identity, CPU-hit reuse, and GPU reupload from the prepared cache.
- Added a clean warm-region benchmark/report path for the prepared-tile cache under `_tmp_sky_view_warm_region_benchmark.json` and `_tmp_sky_view_warm_region_benchmark_summary_utf8.tsv`, and hardened the temporary validation teardown so those runs finish cleanly without the earlier `QOpenGLTexturePrivate::destroy()` no-current-context warning flood. The captured warm revisits show the intended behavior: `galactic_center_wide` dropped from about `253.5 ms` layer / `238.0 ms` prepare on first visit to about `6.6 ms` layer with `0 ms` prepare/read/decode/convert/padding on second visit, `galactic_center_detail` dropped from about `140.7 ms` layer / `134.0 ms` prepare to about `4.4 ms` layer warm, and GPU-cache-cleared revisits still stayed at `0 ms` prepare while reusing CPU-prepared tiles (`cpu_hits:2` and about `1.4 ms` upload in the wider cases, `cpu_hits:0` / `gpu_hits:19` in the fully resident detail case). The broad `_tmp_sky_view_stutter_matrix.json` / `_tmp_sky_view_stutter_matrix_summary_utf8.tsv` artifacts were also regenerated on the new build; cold tiled-manifest cases remain roughly `303-313 ms`, which confirms this pass removes repeated CPU tile preparation on warm revisits/reuploads but does not change the unavoidable first-visit cold-load cost.
- Added an instrumentation-first Sky View stutter pass instead of another renderer change. The live timing window now reports update frequency, viewport/DPR, visible-star and drawn-label counts, label candidate/measure/layout/draw splits, overlay-misc vs star-overlay time, collect sub-breakdowns (bin selection, candidate gather/sort, magnitude filtering, projection/cull, visible-list assembly), and tiled-GL Milky Way asset/tile-select/cache-lookup/read/decode/convert/copy/upload timings. A clean seven-case matrix was captured into `_tmp_sky_view_stutter_matrix.json` / `_tmp_sky_view_stutter_matrix_summary_utf8.tsv` for `disabled`, `single_global_gl`, `tiled_manifest_gl_atlas`, `tiled_manifest_gl_source_like`, `tiled_manifest_gl_dark_sky`, `tiled_manifest_gl_dev_panel`, and `cpu_fallback` at the full-catalog `45 deg` view. Representative steady-state figures were about `17.9 ms` paint with Milky Way disabled, `24.9 ms` with single-global GL, `39.9 ms` with CPU fallback, and about `298-334 ms` for the tiled-manifest GL cases with pan worst frames around `312-359 ms`. In those tiled cases, `collect` stayed only about `14-18 ms`, star draw about `1.6-3.2 ms`, overlay about `2.2-4.3 ms`, label work stayed under `0.7 ms`, GL upload stayed only about `2.8-3.9 ms`, GL draw stayed near `0.1 ms`, while tile read/decode/convert/copy dominated (`14-16 ms` read, `245-271 ms` decode, `5-6 ms` convert, `240-263 ms` border-copy/padding). Classification for the current remaining live stutter is therefore category `1. tile disk/decode/convert bound`, not GL upload, GL draw, collect, star draw, labels/overlays, repaint frequency, or dev-panel overhead. Minimal recommendation only: keep the current renderer behavior unchanged and target any future optimization specifically at CPU-side tile image decode/padding work, ideally by avoiding repeated neighbor-border image rebuilds or introducing a bounded decoded-tile CPU cache before considering async GL upload.
- Tuned the Sky View Milky Way tone presets for cleaner separation in the live tiled-GL view: `Atlas` is now a slightly less pushed default, the old `Photographic` preset was replaced by a flatter `Source-like` preset while keeping `photographic` as a backward-compatible alias for saved settings, and `Dark sky` now leans more contrast-forward without crushing the base underlay. The Sky View options popup now exposes `Atlas`, `Source-like`, `Dark sky`, and `Custom`.
- Added a simple developer-visible Milky Way runtime panel in normal rendering behind `CITIZEN_PHOTOMETRY_SKY_VIEW_MILKY_WAY_DEV_PANEL=1`. It overlays the active renderer, asset mode, source level, tile counts, cache MB, and fallback count directly on the Sky View canvas for both the GL path and CPU fallback.
- Captured clean documentation screenshots for the tuned Milky Way view under `docs/screenshots`: `sky_view_milky_way_atlas_before.png`, `sky_view_milky_way_atlas_after.png`, `sky_view_milky_way_source_like.png`, `sky_view_milky_way_dark_sky.png`, and `sky_view_milky_way_dev_panel.png`.
- Ran the normal full-catalog live-use Sky View benchmark cases on the tuned build. The refreshed tiled-manifest GL cases stayed on the shader renderer with zero fallback; representative steady-state figures were about `241.5 ms` paint / `234.7 ms` Milky Way layer at the `220 deg` wide view (`L2`, max `5.6 ms` tile upload over the pan sweep), about `308.7 ms` paint / `293.5 ms` layer at the `45 deg` medium view (`L4`, max `4.0 ms` tile upload), and about `228.7 ms` paint / `148.7 ms` layer at the fully resident `12 deg` zoomed view (`L4`, max `1.7 ms` tile upload in steady-state, `11.7 ms` pan worst frame once warm). Based on that profile, the current visible stutter is not dominated by synchronous GL tile upload itself, so no async tile-loading change was made in this pass.
- Fixed the remaining Sky View Milky Way tiled-GL wedge/void artifact in normal rendering without changing stars, overlays, the CPU fallback, or the shader tile ownership route. The GL layer now keeps the manifest base underlay in normal mode even when all requested translucent detail tiles are resident, while retaining `all_requested_tiles_resident` as a diagnostic flag; targeted validation for `galactic_center_detail` now reports `L4`, shader tiles, `19/19` requested/resident tiles, zero missing/deferred tiles, `base_drawn: True`, and `base_skipped_reason: none`. The tile diagnostics were also extended with coverage/solid-ID/raw-RGB/alpha modes, tile uploads now use a one-pixel neighbor gutter to remove linear-filter edge bleed, and the selected-pixel probe's CPU model was aligned to that padded upload path. A full non-benchmark seven-view validation refreshed all normal captures with `renderer: shader`, `base_drawn: True`, `base_skipped_reason: none`, and zero missing tiles. Focused verification: selected manual edge samples remained `case6_all_match`, and `pytest tests/test_sky_view_milky_way_gl.py tests/test_main_window.py::SkyViewMilkyWayTextureTest::test_milky_way_runtime_format_reports_tile_renderer -q` passed.
- Hardened `_tmp_sky_view_milky_way_validate.py` for the post-blend-fix Milky Way validation sweep by writing each per-view report entry immediately after screenshot capture and settle metrics, before the optional pan-sweep work. This keeps finished captures in `validation_report.json` even when the Qt/OpenGL process exits later during the longer sweep/teardown path, and it let the seven requested view cases plus the five requested benchmark cases be recovered incrementally in isolated runs.
- Finished the clean post-fix Milky Way visual validation pass against the real `textures/milky_way_tiles_32k/manifest.json` runtime. All seven requested views now report `renderer:opengl`, `cpu_fallback:0`, and `mesh_build_ms:0.00`; `galactic_center_detail` reaches full `L4` residency with `base_drawn:0`, while the wider/polar/seam views still draw the manifest base with partial tile residency during their first settled frame. The visible wedge/fan artifact remains in normal tiled rendering after the packed-debug blending fix.
- Narrowed the remaining Milky Way artifact with clean source/base/tile separation instead of another renderer probe. In the wide all-resident follow-up capture, `tiles_only` still shows the hard void/wedge, `base_only` is visually continuous, and `missing_debug` can reproduce the wedge with all requested `L2` tiles resident, so the issue is not the packed debug path and not a simple transient deferred-residency hole. Direct source-tile checks on representative resident tiles such as `textures/milky_way_tiles_32k/L2/5_2.png`, `L2/6_2.png`, and `L2/6_3.png` also show clean edge content, which pushes the remaining defect toward tiled render coverage/compositing rather than bad tile imagery.
- Ran the requested post-fix performance sanity pass. The merged validation report now contains `disabled`, `single_global_gl`, `tiled_manifest_gl_zoomed_out`, `tiled_manifest_gl_medium`, and `tiled_manifest_gl_zoomed_in`; all five cases stayed at `fallbacks:0` and `mesh_build_ms:0.00`, with representative steady-state Milky Way layer costs of about `0.012 ms`, `0.314 ms`, `34.649 ms`, `35.872 ms`, and `1.599 ms` respectively.
- Resolved the Milky Way selected-pixel `case2_uv_mismatch` without changing normal sky/tile conventions. The real bug was that exact packed debug outputs, especially `local_uv_packed` and `global_uv_packed`, were still drawn with blending enabled, so their bytes were alpha-blended into the framebuffer before readback because the fourth packed channel landed in fragment alpha. `photometry_app/ui/sky_view_milky_way_gl.py` now disables blending for exact debug/exact-ID packed modes, `_tmp_sky_view_milky_way_pixel_probe.py` records exact live tile bounds plus coordinate-variant diagnostics and uses nontrivial synthetic UV override bytes, and the regenerated normal-mode probe now returns `dominant_mismatch_case: case6_all_match`, `uv_mismatch_persists_after_internal_capture: false`, and `synthetic_packed_validation.all_passed: true`.
- Moved the Milky Way selected-pixel diagnostic into the live Sky View OpenGL draw path instead of relying on external harness-side `glReadPixels`. The Sky View widget now accepts per-frame diagnostic capture requests, resolves selected pixels out of the multisampled `QOpenGLWidget` framebuffer through a scratch single-sample FBO, and returns JSON-serializable per-sample RGBA plus explicit coordinate-space records. The GL layer also gained debug-only synthetic override uniforms so packed tile ID / UV / RGB / alpha outputs can be validated against known values without changing normal rendering.
- Updated `_tmp_sky_view_milky_way_pixel_probe.py` to use that internal capture API for every debug mode, keep `grabFramebuffer()` only as a fallback, record capture status/error summaries, and report direct shader-vs-CPU UV comparisons plus UV-mismatch subcases. The regenerated normal-mode probe now confirms `internal_capture_available: true`, `final_capture_backend: internal_gl_readback`, and `synthetic_packed_validation.all_passed: true`, while the dominant mismatch remains `case2_uv_mismatch` with every sample falling into the more specific subcase `captured_shader_global_outside_decoded_tile_bounds`, which means the UV issue is real and no longer attributable to external readback or packed decode artifacts.
- Added focused coverage for the new coordinate helpers and widget diagnostic-capture request/result API, and tightened Sky View shutdown so GPU star textures and Milky Way GL textures are released while the widget context is current.
- Replaced the unstable full-frame multi-pass Milky Way wedge diagnostic with a new minimal selected-pixel probe path instead of changing the renderer. The new `_tmp_sky_view_milky_way_pixel_probe.py` harness settles the real `galactic_center_detail` `tiles_only` OpenGL view in `sample_mode=normal`, takes one final framebuffer grab, selects a compact 16-24 pixel wedge/control set, reads only those pixels back through PyOpenGL `glReadPixels` for packed shader-truth outputs (tile ID / local UV / global UV / raw tile RGB / toned RGB / alpha / owner mask), and writes incremental progress plus a partially valid JSON report throughout capture and CPU comparison so native exits are easier to localize.
- Added a pure probe-support module and focused tests for the new selected-pixel path. `photometry_app/ui/sky_view_milky_way_pixel_probe_support.py` now owns packed decode helpers, default wedge/control sample selection, CPU-side tile sampling model comparisons, and atomic incremental JSON report writes, while new unit coverage validates decode round-trips, sample-set generation, JSON sample loading, sampling-model matching, and incremental report aggregation without launching the full Qt/OpenGL stage.
- Extended the Sky View Milky Way tiled-GL diagnostic pass with the next precision stage instead of changing the renderer. The OpenGL layer now supports debug-only tile sample modes (`normal`, `nearest`, `linear_no_mip`, `linear_mip`, `texel_fetch_debug`, `manual_bilinear_debug`), packed shader-truth outputs for exact tile ID / local UV / global UV / raw tile RGB / toned RGB / alpha under the current GLSL 120 profile, and representative live GL texture-state reporting for a resident tile. The Sky View widget now normalizes and reports the active tile sample mode, threads it through the draw call, and includes it in runtime/debug snapshot state; focused main-window tests cover that new normalization and plumbing.
- Reworked `_tmp_sky_view_milky_way_pixel_path_validate.py` around those new diagnostics so it no longer relies on PNGs as the only truth source. The harness now reads framebuffer bytes directly from `grabFramebuffer()`, decodes packed 16-bit shader outputs, evaluates multiple explicit CPU sampling models against the shader-captured local UV, estimates the real widget background before framebuffer comparison, and prepares per-sample / per-mode summaries for the forced sampler modes. During live end-to-end validation, the old multi-pass capture set exposed two native exit boundaries in the Qt/GL diagnostic path (`final_preblend_packed_lo` and the per-mode snapshot fetch), so those redundant passes were removed or bypassed and the remaining harness now isolates the unresolved blocker to an immediate process exit right after the reduced normal-mode capture loop completes, before Python-side report generation begins.
- Added the direct Sky View Milky Way pixel-path diagnostic pass requested for the remaining tiled-GL wedge/source-blend investigation. The GL path now exposes debug-only shader outputs for tile ID, local UV, global UV, raw tile RGB, toned RGB, alpha, and owner-mask, and the new temporary `_tmp_sky_view_milky_way_pixel_path_validate.py` harness settles the real `galactic_center_detail` `tiles_only` view, captures those framebuffer variants, disables catalog objects for clean sampling, and writes a structured JSON report plus screenshots under `_tmp_sky_view_milky_way_pixel_path_validation`.
- Used that pixel-path report to tighten the current hypothesis before any renderer fix: the first comparator over-attributed the mismatch to UVs because its CPU screen-to-global reconstruction was offset from the shader-captured `global_uv`; the refined report now keys expected samples off the shader-captured tile ID and local UV, mirrors the CPU tile sample the same way as the GL upload path, and shows the remaining dominant mismatch as raw tile RGB sampling rather than tile ownership, tone math, or alpha. The same report also shows framebuffer-vs-black discrepancies disappear when the non-black widget background is taken into account, so the current unresolved defect is narrowed to the raw texture sampling stage rather than base blending or post-alpha composition.
- Narrowed the remaining Sky View Milky Way tiled-GL wedge diagnosis with focused live checks instead of more renderer churn. Source-tile seam checks on the fully resident `L4` detail set came back clean, sampled detail-view coverage checks showed no missing or overlapping shader-tile ownership across the viewport, and a temporary per-tile mipmap suppression probe was tried and then reverted after it produced no visual change in the refreshed detail/wide screenshots.
- Added a narrower Milky Way tiled-GL diagnostic pass to separate base blending from tile coverage defects before changing shader behavior. The GL layer now reports whether the low-resolution base actually drew, which base source was active (`manifest_base` vs fallback modes), whether all requested tiles were resident, and why the base was skipped; the debug render-mode normalization also accepts explicit `normal_without_base` and `normal_with_base_forced` probes so temporary validators can compare fully resident tile-only frames against forced-base composites without reviving the projected mesh path.
- Followed up the Sky View manifest-tile shader pass with a narrower composition fix: the OpenGL Milky Way layer now suppresses the low-resolution base preview draw when a manifest-tiled frame is already fully resident, so fully loaded shader-tile views no longer keep blending the preview texture underneath the manifest tiles. A more conservative shader tile-selection probe was tried during validation but reverted after it only increased deferred residency without changing the observed tile UV footprint.
- Reworked the tiled Sky View Milky Way mesh generation path to target the remaining wedge/fan artifacts directly instead of changing asset selection or GL draw ownership. Tile requests now build adaptive per-tile meshes with extra subdivision for zoomed, pole-adjacent, and highly curved views, reject degenerate/discontinuous/oversized projected triangles before they reach GL, carry per-tile mesh diagnostics through the draw request and validation harness, and keep existing lightweight widget/scene test fixtures working by defaulting optional debug and camera fields safely.
- Added a diagnostic-only tiled Milky Way artifact localization pass without changing the normal renderer output. The Sky View OpenGL Milky Way path now exposes exact requested/resident/deferred/missing tile IDs, developer-only tile debug overlays and labels, optional tile UV debug, and debug render modes (`normal`, `base_only`, `tiles_only`, `tiles_over_flat`), while `_tmp_sky_view_milky_way_validate.py` now captures paired normal/tile/missing/mesh screenshots and per-view tile diagnostics. Live real-manifest validation localized the remaining wedge/seam artifact to the projected tile mesh itself: the clean `galactic_center_detail` failure reproduces with `18/18/18` full residency and no wrapped or missing tiles, the visible wedge edges coincide with the rendered tile triangles, the polar case collapses into a pole-centered fan of tile sectors, and the RA-seam case still shows deferred/wrap stress near seam-adjacent tiles rather than a pure missing-tile gap.
- Added a real 32k Milky Way validation pass around the manifest-backed tiled OpenGL renderer. The offline tile generator now prefers `OpenEXR` for EXR sources before falling back to `imageio`, which unblocked generation of the real `textures/milky_way_tiles_32k` pyramid on this machine, and the temporary `_tmp_sky_view_milky_way_validate.py` harness now writes incremental JSON/text reports plus captured comparison screenshots even if a later benchmark case fails. Live validation against the generated 32k manifest confirmed `asset:tiled_manifest`, `renderer:opengl`, and zero CPU fallback on the real dataset, while also surfacing remaining visual wedge/seam artifacts in the current tiled runtime path; unsuccessful local renderer probes from this pass were reverted instead of being left in the production path.
- Added the next staged Sky View Milky Way OpenGL pass for manifest-backed tiled rendering. The GL helper now keeps the existing fullscreen single-texture base path, adds a structure-preserving alpha mode by default with a developer luminance-alpha override, supports a bounded per-frame synchronous tile upload path plus an explicit LRU GPU tile cache for visible manifest tiles, and reports active tiled runtime diagnostics including selected source level, visible/resident tile counts, cache hits/misses, upload counts, cache size, and alpha mode. The Sky View widget now prepares a practical low-resolution manifest base image when available, selects an active manifest level with hysteresis, builds visible tile requests from the current camera footprint, projects tile meshes through the existing sky projector, and falls back cleanly to the old single-global/CPU behavior when tiled GL cannot draw anything valid.

## 2026-05-23
- Added the first staged foundation pass for the Sky View Milky Way 32k pipeline without changing the live single-texture draw behavior yet. Milky Way GL is now the default preferred renderer when available, with explicit developer overrides to force CPU or disable GL, runtime diagnostics now report renderer/asset selection and fallback state, the app has a new Milky Way asset resolver plus tile-manifest model/parser, pure RA/Dec tile-selection and zoom-level helpers with focused tests, and a new offline `scripts/generate_milky_way_tiles.py` utility can validate image sources, fail loudly on broken EXR decoding, and generate a manifest-backed tile pyramid for future runtime tiled rendering.
- Extended the Sky View Milky Way pass so the real visible Sky View now reports runtime renderer diagnostics, shares one tone-control pipeline across the CPU and experimental OpenGL Milky Way paths, exposes Atlas/Dark sky/Photographic tone presets in the options popup, and prefers the GL Milky Way layer for `High` mode whenever `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_MILKY_WAY=1` is enabled and GL remains healthy. Full-catalog real-scene benchmarking with the cached `61719`-object atlas confirmed that CPU `High` motion still spends about `27.9 ms` per frame in the Milky Way layer while GL `High` keeps that layer around `0.75 ms` with zero fallback, and warmed GL frames now keep correct mipmap diagnostics instead of falsely reporting `mipmaps:0` after the initial upload.
- Removed the Sky View Milky Way `Low quality while dragging` mode and its Milky Way-specific drag/wheel settle swap. Pan and zoom now keep one consistent Milky Way quality path: the normal CPU projected-image renderer or the experimental OpenGL renderer when enabled and successful. The obsolete interactive-low-quality UI/control and internal flag are gone, CPU projected-image cache keys no longer split on interaction state, obsolete config keys are ignored safely, and the experimental GL helper now uses the correct PySide6 per-axis texture-wrap enums so the GL path stays active on this build.
- Added an experimental Sky View OpenGL Milky Way renderer behind `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_MILKY_WAY=1`. The new path uploads the Milky Way source texture once, applies brightness/opacity/flip in a fullscreen shader below stars and labels, keeps the existing CPU projected-image renderer as the fallback/reference path, disables the GL layer for the session on any initialization or draw failure, and adds focused tests for GL short-circuiting, fallback, and cache-preserving renderer-mode updates.
- Followed up the Sky View Milky Way performance pass with a second idle-regeneration optimization focused on the high-quality CPU image path. The renderer now builds cached float32 source mip levels once per texture load, selects the smallest acceptable source level for the current field size and resolution mode, and reuses projection-grid basis components instead of rebuilding extra horizon-space intermediates on each miss. Earlier warmed offscreen measurements improved the changing-camera high-quality CPU path from roughly `51.5 ms` to roughly `45.0 ms` on this machine while keeping stable cache hits around `2.2 ms`.
- Optimized the Sky View Milky Way texture layer after timing showed the slowdown came from per-frame CPU projection, bilinear sampling, alpha assembly, and `QImage` copying during camera-changing frames rather than repeated PNG loading. The Milky Way path now reports detailed timing through the existing Sky View timing log, keeps a small LRU of projected background images, caches the reusable projection grid, applies opacity at painter draw time so opacity changes do not rebuild the projected image, and keeps Milky Way controls for on/off, opacity, brightness, resolution mode, and horizontal flip while leaving the layer below catalog stars and labels.

## 2026-05-22
- Sky View now supports a diffuse Milky Way background layer sourced from the NASA SVS 2020 Deep Star Maps EXR master. A new preprocessing script converts `textures/milkyway_2020_4k.exr` into the runtime `textures/milkyway_2020_4k_preview.png`, while the renderer samples that PNG through the existing screen-ray to RA/Dec sky geometry instead of drawing it as a flat wallpaper. The layer is cached per camera/view state, sits behind catalog stars and overlays, and the Sky View options popup now includes opacity plus horizontal texture-flip controls for alignment checks.
- Sky View star rendering has been retuned after the first deeper-field pass. The visibility curve now sits between the original sparse sky and the overly dense last pass, trims the mid-zoom scan cutoff for smoother interaction, restores the default scientific field depth to `9.5`, makes bright stars separate more clearly from faint fast-path points through stronger nonlinear sprite/radius scaling, increases B-V color saturation so warm stars such as Betelgeuse read less white, and replaces the sine-like twinkle with a Stellarium-shaped random luminance shimmer that is strongest near the horizon and subtle near the zenith.
- Sky View stars now use a more atlas-like three-layer render: a crisp near-white core, a softer nonlinear bloom, and a faint wide aura for the brightest stars instead of mostly uniform blurred discs. Bright stars separate more intentionally from medium and threshold stars, faint fast-path stars stay closer to subliminal points, the scientific Hipparcos field now defaults deeper with safe fallback to the best lower cached limit, color temperature is smoother and slightly more legible for bright stars without becoming saturated, hover/selection rings stay quieter, and labels now use a premium atlas treatment with stronger hierarchy plus a ranked multi-position placement solver that can move labels around stars near edges or crowded regions instead of relying on one fixed offset.
- Sky View's bottom-dock date/time control is now a real observation-time selector instead of a read-only clock summary. The popup now includes a UTC date/time editor plus quick step buttons for `-1 d`, `-1 h`, `Now`, `+1 h`, and `+1 d`; Sky View preserves the selected observation time across location/context refreshes, updates the local/UTC summary labels, and repaints the sky so altitude/azimuth and the visible sky respond to both the chosen time and the active observing site.
- Removed the last source of Sky View location-window flashing by replacing the WebEngine/Leaflet map surface with a pure Qt raster slippy-map widget. The location manager still draws exact Carto Earth tiles with the same dark Sky View styling, click-to-place selection, drag panning, zoom controls, marker centering, saved sites, and reverse-geocoded name autofill, but opening it no longer creates a browser/GPU surface over the live Sky View `QOpenGLWidget`.
- Sky View's observer-location dock button now opens the full location manager directly instead of first going through the quick-switch popup menu. That removes the extra intermediate surface/prompt from the bottom-dock click path on Windows and keeps the interaction aligned with the exact-map workflow.
- Reduced the remaining Sky View location-window flash on Windows by moving the observer-location dialog off the blocking `exec()` path. The exact-map window now opens as a single-instance window-modal dialog with an async finished callback instead of forcing a nested modal event loop over the live Sky View `QOpenGLWidget`, which avoids the harder activation/repaint jump when the map window appears.
- Fixed a Sky View Windows compositor artifact triggered by the new exact observer-location map. App startup now enables Qt shared OpenGL contexts before `QApplication` is created, which is required when the Sky View `QOpenGLWidget` and the WebEngine-backed map dialog coexist; the earlier overlay-suspension workaround was removed so opening the location window no longer flashes the main view or shows a reverted duplicate surface.
- Sky View now has a dedicated observer-location control in the bottom dock backed by an exact Earth map instead of the earlier stylized atlas placeholder. The location popup now supports quick switching between multiple saved observing sites from the same dock button, the full management dialog uses a dark-styled tiled map with zoom controls and click-to-place site selection, reverse geocoding can auto-fill the site name from picked coordinates, and saved selections write back into the existing observing-site settings so Sky View immediately refreshes its local-horizon context.
- Fixed overlapping Sky View labels for very close named objects. The Alpha Centauri pair exposed that multiple nearby named stars could both pass the label gate and then draw text directly on top of each other. Sky View now resolves object-label candidates through a small collision filter before painting, prioritizing the selected object first and then brighter labels, so close pairs like Toliman and Rigil Kentaurus no longer render stacked label text.
- Followed up the Sky View bright-star layering pass so the same stagger now applies to labels for searchable/named stars instead of only to star visibility. Searchable star labels now use a brightness-dependent wide-field cutoff, which lets very bright labels like Sirius appear earlier than mid-bright labels like Arneb, and the rich cached diffraction spikes were enlarged by another 20% in extent while keeping their tip-fade gradient treatment.
- Tuned Sky View bright-star layering and spike rendering. Mid-bright stars now enter later than the very brightest stars during zoom-in by applying a small magnitude-dependent visibility offset in the star fade curve, bright diffraction spikes now fade toward their tips instead of reading as a flat-opacity `+`, and spike extent/thickness scale more naturally with brightness while staying on the cached sprite path so steady-state draw cost remains effectively unchanged.
- Reworked Sky View atlas naming and star-name search. The app now bundles a SIMBAD-derived HIP proper-name alias map for named stars, promotes actual/common star names like Alnitak, Alnilam, and Mintaka above raw HIP/HD identifiers, keeps alternate identifiers as searchable aliases, canonicalizes atlas object display names with the priority `actual/common name > Messier > NGC > IC > VdB/Barnard/Sh2 > other catalog identifiers`, and slightly relaxes named-star label thresholds so common-name stars around Orion-belt brightness can label at the current mid-wide Sky View zoom.
- Tightened the Sky View upper-left object panel UX. The panel now stays hidden until something is actually selected, clicking empty sky clears the current selection and dismisses the panel, the old `Selected Object` / pinned-help / local-dataset copy is gone, magnitude moved into the top-right header, the object name is larger, the `Catalog` row is removed, and the field order now leads with RA and Dec before Alt/Az with uniform value styling.
- Refined the Sky View selected-object panel UX. The upper-left panel now behaves as a pinned selection summary rather than a hover-driven info card, keeps the main object name prominent, shows secondary names in a quieter line underneath, adds compact high-value fields including altitude and azimuth, and uses a flatter overlay treatment that blends into the immersive canvas more naturally.
- Added a final Sky View performance stabilization/documentation pass for the current recommended runtime state. Keep `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_ATMOSPHERE=1`, keep `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_STARS` unset/off, keep `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_FAINT_STARS=0`, and keep `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_GROUND=0` when GL atmosphere is enabled. With that setup, sampled real-use paint windows stayed below `16.7 ms`, GL atmosphere stayed active with no fallback, grid remained cheap, non-star spikes stayed gone, overlay stayed secondary, fast-star drawing was not dominant, and `collect` remained the dominant bucket.
- Recorded the final Collect-2 conclusion. The worst sampled case was the focused Orion/Rigel selected view at about `15.69 ms` paint / `12.32 ms` collect on the normal `spatial_bins` path with catalog size `61719`, about `90` selected bins, about `5398` spatial candidates, about `1558` scanned objects before the magnitude break, about `386` appended visible objects, zero selected-object fallback, about `0.0 ms` fallback time, about `4.9-5.8 ms` scan/projection, negligible merge time, and variable candidate enumeration/sort jitter as the changing component. This is not a GL-star problem, not selected-object fallback, not label-related, and not a grid/ground issue.
- Decision for the current Sky View performance state: stop optimization for now. Do not lower spatial fallback thresholds, do not optimize GL stars further, do not vectorize collect yet, do not change selected-object fallback, and do not change magnitude formulas. Reopen performance work only if real-use paint windows start exceeding `16.7 ms` persistently, steady-state collect starts repeatedly exceeding about `10-12 ms`, another bucket becomes dominant again, a deeper catalog materially increases fast-star counts, or there is still visible stutter. If Collect-2 is reopened later, the next target should be spatial-bin candidate enumeration/sort jitter in identical Orion-like mid-FOV camera states.
- Added a Stage GL-Star-2 stabilization/documentation pass. The Sky View comments and repo notes now document the current renderer contract: `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_STARS=1` stays opt-in, QPainter fast stars remain the default/reference path, `GL_STARS` takes precedence over legacy `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_FAINT_STARS`, the old immediate-mode `GL_POINTS` experiment should not be extended, Stage GL-Star-2B uses a shader-backed expanded-quad VBO path because `QOpenGLFunctions_2_0` does not expose the instancing support this renderer would need, and Stage GL-Star-2C keeps the adaptive production gate at `1000` after sampled real fields peaked around `362` fast-star candidates and forced GL at that density was not clearly faster than QPainter.
- Added Stage GL-Star-2B Sky View fast-star rendering behind `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_STARS=1`. The experimental path now replaces only the grouped QPainter fast/background-star `drawPoints` pass with a shader-backed OpenGL renderer that flattens CPU-projected fast-star groups into a reusable expanded-quad vertex buffer, reports submitted/upload/draw/capacity diagnostics, preserves render order with the existing atmosphere layer, and disables itself for the session on any GL initialization, upload, or draw failure.
- Added Stage GL-Star-2A Sky View scaffolding behind `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_STARS=1`. The new retained-mode star-renderer path is stubbed only for now, keeps QPainter fast stars as the default/reference output, disables for the session on failure, reports placeholder GL-star diagnostics, and takes precedence over the legacy `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_FAINT_STARS` experiment when both flags are set.
- Added a Stage GL-Atmo-1 stabilization/documentation pass for Sky View. The repo docs and local renderer comments now spell out that QPainter remains the default/reference path, the fullscreen GL atmosphere layer stays opt-in and owns both atmosphere and flat ground only when it succeeds, the existing fallback/diagnostic contract must be preserved, and no visual behavior changed.
- Added Stage GL-Atmo-1 Sky View fullscreen OpenGL atmosphere/ground masking behind `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_ATMOSPHERE=1`. The experimental path now uses a full-screen shader with the existing screen-ray horizon math to draw atmosphere above the horizon and flat ground below it, skips the CPU ground mask/geometry and cached atmosphere image work on success, and disables itself for the session on any GL initialization, compile, or draw failure.
- Added Stage GL-Atmo-0 Sky View scaffolding: a reusable GL projection uniform package, an opt-in atmosphere layer session/fallback stub behind `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_ATMOSPHERE`, and timing/detail diagnostics while keeping the existing QPainter atmosphere and ground rendering active.
- Added an opt-in experimental standalone `Sky View` OpenGL solid-color ground mesh behind `CITIZEN_PHOTOMETRY_SKY_VIEW_GL_GROUND=1`. The default QPainter ground path remains unchanged, while the experimental layer reuses the existing CPU screen-ray ground/horizon geometry, falls back to QPainter on any GL failure, and reports GL ground triangle, draw-time, and fallback counters through the existing timing diagnostics.

## 2026-05-21
- Started the standalone `Sky View` renderer-extraction stage by adding a private render-scene dataclass and QPainter renderer helper. The widget still owns camera/input/selection/catalog/timing state, while QPainter remains the only active renderer and delegates cache-sensitive layer drawing through the existing code paths.
- Simplified the standalone `Sky View` green ground rendering to a single accurately clipped base color in both static and active camera states. The atmosphere layer and horizon/ground mask remain unchanged, while altitude-band path generation and drawing are now disabled.
- Added an interaction-only standalone `Sky View` ground preview path for active camera motion. Rendered ground-layer cache misses while dragging, wheel-settling, or animating now draw only the accurately clipped base ground fill and skip altitude-band path generation/drawing until motion settles, when the existing full-quality rendered layer cache is built again.
- Made the standalone `Sky View` rendered ground-layer cache interaction-safe. Dragging, wheel zoom settling, and animated camera motion now avoid building a new transparent ground-layer `QImage` on cache miss; exact cache hits still draw, and static frames can still populate the Phase 6 cache once motion settles. Opt-in ground diagnostics now also report rendered-layer stores and interaction bypasses.
- Added a conservative standalone `Sky View` rendered ground-layer cache for identical static frame states. Mixed-horizon ground rendering now keeps a one-entry transparent DPR-aware `QImage` cache of the final ground fill plus altitude bands, while atmosphere rendering remains separate; opt-in ground diagnostics report layer cache hits/misses, generation, uncached draw, and cached draw-image time.
- Added a conservative standalone `Sky View` projected-grid point-sequence cache for identical frame states. Equatorial and alt-az grids now keep separate exact-key caches of projected sample sequences only, so repeated static repaints can rebuild the same `QPainterPath` geometry without rerunning grid sample generation, culling, or projection; cache diagnostics remain gated by `CITIZEN_PHOTOMETRY_SKY_VIEW_TIMING` plus `CITIZEN_PHOTOMETRY_SKY_VIEW_GRID_DETAIL`.
- Restored the standalone `Sky View` render-stage split after the phase-3 revert cleanup: `_draw_non_star_objects` again handles only non-star glyph drawing, while `_draw_object_overlays` draws rich star/object overlays, labels, selection rings, and rebuilt hit targets for hover/click/object-inspector behavior. The spatial-bin candidate path remains on the reverted concatenate-and-sort implementation with phase-2 hoisting and phase-2.5 diagnostics intact.
- Reverted the experimental standalone `Sky View` phase-3 exact-order spatial-bin k-way merge after timing showed it was slower than the previous path. The binned candidate pipeline is back to concatenating selected bin contents and sorting the merged list per frame, while keeping the existing phase-1 timing instrumentation, phase-2 collect-state hoisting, and phase-2.5 detailed collect diagnostics.
- Standalone `Sky View` timing now has an additional opt-in `CITIZEN_PHOTOMETRY_SKY_VIEW_COLLECT_DETAIL` diagnostic path that records collect-stage candidate-path counts and timing splits for spatial-bin selection, candidate merging, per-frame sorting, scan/projection work, rejection counts, and selected-object fallback without changing rendering behavior.
- Standalone `Sky View` now hoists frame-invariant collection state out of `_collect_visible_objects` so rough-cull geometry, projection basis, field geometry, and per-frame magnitude limits are computed once per collect pass instead of once per candidate. The optimized internal helpers preserve the existing cull thresholds, projection formulas, draw order, ground cutoff, and selected-object fallback behavior while reducing Python work in the collection hot path.
- Fixed a Sky View regression from the recent phase-1 timing/visible-object cleanup where enabling `Ground/Horizon` could blank the star pass and prevent the ground from appearing. The ground-enabled star appearance branch is restored, with a narrow regression test to keep that path covered.
- Standalone `Sky View` now has a left-side object dataset inspector overlay. Selecting, hovering, or searching a sky object updates a polished compact panel with object type, magnitude, RA, Dec, catalog/constellation context, and local dataset status while keeping catalog loading cached and frame rendering on the existing indexed draw path.

## 2026-05-20
- Restored the standalone `Sky View` mode in the main `Mode` menu. It is back as its own all-sky panel with a dedicated atlas canvas, drag pan, wheel zoom, direct object selection, search-driven centering, and no left-side results list.
- Standalone `Sky View` now loads real scientific sky data instead of synthetic filler stars. On first open it downloads and caches the brighter Hipparcos all-sky star catalog, merges that scientific star field with the packaged named bright objects/DSOs, and reuses the cached catalog on later opens.
- Standalone `Sky View` now layers the bright-sky catalog by zoom level so zoomed-out views only render the brightest stars and objects, while progressively fainter Hipparcos stars appear as the user zooms in. Its sky projection is now continuously spherical at every FOV instead of switching between flat and fisheye modes, so grid lines and sky geometry warp smoothly as you zoom out, and wheel zooming also uses smaller continuous scale steps for a less abrupt feel.
- Standalone `Sky View` now fades stars and brighter deep-sky objects in and out continuously as you zoom instead of hard popping them at fixed thresholds, pre-sorts the atlas draw order to avoid extra per-frame sorting work, slows wheel zoom a bit more for finer control, and adds a `Full Screen` toggle directly in the Sky View toolbar.
- Standalone `Sky View` now renders on a real OpenGL-backed `QOpenGLWidget` surface with double buffering, vsync, and multisampling, so pan and zoom go through a GPU canvas instead of the old plain QWidget paint path.
- Standalone `Sky View` now keeps the stable star sprite renderer visible on the OpenGL-backed canvas instead of suppressing stars behind an unreliable experimental point-sprite path, and wheel zoom applies immediately again so navigation does not lag behind the input. Search/reset recentering can still ease when the GL widget is valid, and entering Sky View expands the main window to a full-window presentation by default, with the existing toolbar button still available for true full-screen mode.
- Standalone `Sky View` now batches faint non-selectable Hipparcos background stars into grouped point draws, while keeping richer glow rendering for bright/selectable stars and named objects. This removes a large amount of per-star ellipse overdraw in dense zoomed-in fields without making the scientific background stars disappear.
- Standalone `Sky View` wheel zoom now moves farther per scroll notch, and the render loop skips catalog rows that are too faint or too far outside the current view before doing expensive spherical projection math. This keeps the stable visible-star renderer while reducing avoidable frame-time work during pan and zoom.
- Standalone `Sky View` now uses a coarse RA/Dec tile index at narrow fields of view, so zoomed-in views only scan nearby sky bins before projection instead of touching the full Hipparcos catalog on every repaint.
- Standalone `Sky View` now presents as an immersive full-canvas planetarium view: the sky renders edge-to-edge, the normal status bar is hidden, and search/reset/full-screen plus object details sit in translucent overlays above the sky instead of occupying separate mode-panel rows.
- Standalone `Sky View` now removes the always-visible top and bottom cards entirely. It uses a centered icon-only bottom dock that auto-hides below the sky unless the mouse reaches the reveal area, and its `Search`, `Date/Time`, and `Options` controls open popup panels above that dock while `Full Screen` remains a direct icon button.
- Standalone `Sky View` bright and medium stars no longer use the old flat two-circle marker look. They now draw from cached multi-layer sprite halos with a white-hot core, tinted inner glow, broader outer bloom, and subtle diffraction-style spikes only on the brightest stars, while the faintest background stars stay on the grouped fast-point path for performance.
- Standalone `Sky View` restored the stable default planetarium feel after the experimental horizon/star pass proved too aggressive: ground/horizon rendering is now off by default and available as an explicit bottom-dock toggle, `N/E/S/W` markers and low-altitude atmosphere effects only appear with ground enabled, bright/medium stars use the stable painter sprite renderer by default instead of the risky OpenGL textured-quad path, and small mouse drags pan incrementally again instead of compounding into a large rotation.
- Standalone `Sky View` drag control now uses incremental screen-space camera rotation instead of clamped RA/Dec panning, so the sky follows the cursor direction, wheel zoom preserves the current free-look orientation, and the view can continue moving through the celestial poles without getting stuck.
- Standalone `Sky View` now keeps both equatorial and alt-az grid overlays available across the full visible sky instead of lazily clipping them to the old RA/Dec center window, and the bottom dock has a dedicated grid icon with independent toggles for the two grid systems.
- Standalone `Sky View` grid overlays no longer reproject the full dense sphere at narrow zoom levels. They now use a cheap visibility pass plus adaptive curve sampling so the zoomed-in view stays responsive while still drawing complete equatorial and alt-az lines that intersect the current camera view.
- Standalone `Sky View` grid overlays now keep edge-crossing line fragments visible on the initial frame instead of waiting for a drag to make them appear. The equatorial grid cull now uses the same sampled curves as the draw pass with an expanded projection margin, and the alt-az grid no longer relies on the fragile coarse cull that was dropping small visible fragments near the viewport edge.
- Standalone `Sky View` equatorial grid overlays now keep their line fragments visible even at very narrow zoom levels. Zoomed-in meridians and parallels now use slightly denser equatorial curve sampling in the affected FOV ranges, which fixes the last missing-fragment cases without restoring the old full-grid lag.
- Standalone `Sky View` grid overlays are smoother and cheaper to draw now. The grid renderer precomputes the active camera projection once per frame, reuses projected curve samples between visibility checks and path drawing instead of projecting the same points twice, keeps a small explicit off-screen grid margin, and spends that recovered budget on denser meridian/parallel sampling at zoomed-in fields where coarse segments were still visible.
- Standalone `Sky View` grid overlays now remain populated at extreme zoom instead of thinning to one line or disappearing between the old 5-degree major grid intervals. Narrow fields now use finer local RA/Dec and az/alt grid spacing around the current view, while keeping the generated grid window bounded so deeply zoomed views do not return to full-sphere grid lag.
- Standalone `Sky View` grid overlays now keep zoomed-in `~7 degree` fields continuous instead of drawing short coarse fragments. The local grid path now uses denser equatorial and alt-az curve sampling across the full narrow-zoom band, with regression coverage for the screenshot-like `7.3 degree` field where visible curves previously had only one or two projected samples.
- Standalone `Sky View` drag control now keeps the camera roll level with the local horizon. Mouse drag still changes the camera pointing direction, but the screen-up vector is recomputed from local vertical after each pointing change so ground/horizon orientation no longer accumulates arbitrary roll.
- Standalone `Sky View` now defaults both grid systems off, and its `Ground/Horizon` rendering now uses a lightweight horizon-coordinate dome-strip ground renderer clipped by the same screen-ray horizon classifier used for hit tests. The atmosphere now renders as a cached continuous screen-ray gradient instead of coarse stacked color bands, the glow and ground stay continuous even when the horizon leaves and re-enters the field of view, looking above the horizon no longer lets back-side terrain wrap over the sky, split-edge views keep ground visible on both sides even when the horizon middle is outside the field of view, the old thin horizon line is gone, and the terrain draws after stars/labels so it cleanly occludes anything below the horizon.
- Standalone `Sky View` polar and strongly curved zoom views no longer lose stars or equatorial grid meridians near corners. Narrow-view star pre-culling and local grid selection now use a spherical view cap instead of a flat RA/Dec rectangle, and polar views expand longitude coverage without returning to an unbounded dense grid.
- Asteroid/Comet Detection now has an initial `Sky View` tab with a synthetic sky canvas for the current solved frame, showing bright Gaia stars, predicted known-object markers, click-to-inspect context text, drag pan, wheel zoom, and a past/future UTC offset slider that moves the selected object's sky position and trail.
- Differential Photometry Source/File Results now has a top-right `Filter` button that can limit rows by minimum source SNR, minimum Var Score, predicted ML label, and catalog-magnitude range. Source Results and File Results stay in sync with the filtered source subset.
- Differential Photometry Source Results now keeps linked comparison-star rows pinned as a sticky bottom overlay inside the same Source Results table, including the matching row-number gutter and a faint accent highlight so the relevant comparison rows stay visible and readable while scrolling that table.

## 2026-05-16
- Differential Photometry `Discover` now opens a larger preflight dialog before any heavy work starts, follows the active app theme instead of hard-coded light colors, and shows a live image preview with `Shift`+drag ROI drawing, normal drag panning, and mouse-wheel zoom so the run can be scoped visually without accidental full-field starts.
- Differential Photometry `Discover` now runs in two stages: the existing potential-candidate pass first, then a broader random-search pass over remaining Gaia stars that fit the ideal ADU range on the representative frame. Retained sources are labeled sequentially as `Discover 1`, `Discover 2`, and so on, and the Source Results type column now distinguishes `potential candidate` versus `random search` discoveries.
- Differential Photometry `Discover` now merges retained results into Source Results incrementally while the worker continues in the background, and each retained batch is immediately written into the normal cached processing report so a crash or relaunch can still reopen the discoveries found so far.
- Differential Photometry `Discover` is faster now because it reuses one candidate photometry thread pool across the run and no longer spends extra time calculating periods for each retained discovery candidate.
- Differential Photometry `Discover` now batches candidate target photometry per solved frame before the per-candidate comparison-group search, instead of remeasuring each target frame-by-frame inside the candidate loop. Large runs should start scoring candidates much sooner, and the work log now shows per-frame candidate measurement progress during that batched pass.
- Differential Photometry `Discover` now explains failed candidate skips more clearly: when no differential light curve can be built, the work log reports the raw target-row count plus the best attempted comparison group's post-photometry row, usable-value, differential-value, and excluded-point counts so the user can see why the candidate never formed a series.
- Differential Photometry `Discover` now marks batched candidate photometry targets explicitly as target-variable rows before measurement, so ROI-limited or batched runs no longer misclassify those candidate measurements as reference rows and report impossible `0 target rows after differential photometry` diagnostics.
- Differential Photometry `Discover` now exposes a `Try multiple comparison-star pools` checkbox in the preflight dialog. It defaults off for faster runs that use only the top-ranked comparison group, while enabling it restores the broader multi-pool comparison search used to optimize each candidate's differential light curve.
- Differential Photometry `Discover` no longer runs the old staged candidate-search filter before adding results. It now builds raw light curves for all discoverable non-variable field stars outside the current Generate target list, keeps the ROI filter only as a target limiter, and ranks reusable comparison stars by magnitude similarity plus sky proximity before building each star's differential light curve.
- Differential Photometry `Discover` no longer rebuilds the entire processing report and cached light-curve file on every retained incremental result. Discover batch updates now merge results incrementally and defer the heavy source-table refresh and cache-write work, which prevents long runs from appearing to hang after the first added light curve.
- Differential Photometry `Discover` no longer rebuilds the full Source Results table and related processing views during timed mid-run flushes. While Discover is still running, those flushes now persist cache only; the expensive view refresh happens once when the run completes or fails, which avoids another long UI freeze on very large reports.
- Differential Photometry `Discover` is faster during the post-measurement target pass: raw-light-curve evaluation now skips discarded period inference, evaluates each target with only the selected comparison-star measurement rows instead of the full reference pool, indexes reusable reference rows once, runs target evaluations in parallel, and writes lightweight append-only Discover checkpoints during the run before merging into the normal cached report at completion.
- Differential Photometry `Increase SNR` now preserves each affected series' original light-curve and cached period state, forces `Fit Period` to recompute on the derived binned data instead of reusing the pre-binning fit, and lets `Reset SNR` restore the original measurements and period cache immediately without rebuilding the full report.

## 2026-05-15
- Differential Photometry animated light-curve GIF export now renders each frame through the same themed pyqtgraph widget used on screen instead of a separate Matplotlib path, so the exported animation matches the live plot styling, current zoom/view window, fit-period badge, and phase-plot visibility settings.
- Differential Photometry guided Generate now treats saved manual apertures as additions to automatic catalog results instead of a replacement mode: stale manual-only cached reports are ignored during automatic processing, workflow settings normalize saved manual aperture configs back to Auto while preserving their sources, and automatic processing appends saved manual Target/Check/Comparison measurements into the normal Source Results/cache so rerunning a dataset such as Davida/aligned brings back the field stars plus the manual target.
- Differential Photometry image overlays now switch with the selected Source Results row after manual aperture editing: catalog targets show their own target and comparison star overlays, while manual rows still show the saved manual aperture set. Aperture Editor mode continues to show editable manual apertures while it is active.
- Differential Photometry manual aperture Apply now merges the newly measured manual Target/Check/Comparison rows into the existing report instead of replacing Source Results with a manual-only list. Newly added manual targets are reselected after processing, and the light-curve axis switches to Differential Magnitude when the manual series has no calibrated magnitudes so its curve is visible immediately.
- Differential Photometry's Aperture Editor now opts into a Differential-only live image drag path, restoring direct target/check/aperture-handle editing while leaving the shared image-view pan and other mode interaction paths unchanged.
- Differential Photometry period inference now rejects harmonic-fit and BLS candidates that land on the search boundaries, so one-night light curves with only a broad trend no longer all report the same maximum-span period such as `0.1584 d` as if it were a measured cycle. Older cached calculated periods at those boundaries now restore as inconclusive instead of re-showing stale aliases. `Fit Period` also redraws the selected light curve immediately after applying the inferred period, so the fitted curve appears without needing a follow-up Ctrl-drag segment refresh.

## 2026-05-14
- Sky Explorer now caches the automatic Explore annotation overlay layer separately from manual annotations, so dragging manual circles, ellipses, and text over a populated Explore result no longer rebuilds and repaints every catalog annotation on each mouse move.
- Sky Explorer image views now keep the annotation tools grouped on the left and move `Display` into the right-side control cluster. The contextual properties strip keeps its fixed footprint while shape properties align left instead of floating awkwardly, `Stroke`/`Fill` now use labeled square color swatches, and `Opacity` now has a clearer Photoshop-style control: drag the `Opacity` label to scrub the value or open the `%` dropdown for a slider and manual numeric entry. Text insertion now opens the add-text dialog on mouse release instead of during the image press gesture, preventing the image view from getting stuck after the first label and blocking later move/delete/circle/ellipse actions. Text hit-testing also uses the rendered widget-space label bounds so existing text can be selected, dragged, and removed reliably at any zoom level. Manual annotations remain visual-only and are not saved into catalog results or app settings.
- The Sky Explorer opacity popup is now a tighter slider-only panel instead of repeating the percentage field a second time, the opacity dropdown arrow is kept inside the button chrome, and the shape-properties strip now includes a compact `Weight` control for stroke thickness so the fixed-width properties area stays fuller when switching between text and circle/ellipse tools. The neighboring Sky Explorer header buttons such as `Explore`, `Center Object`, `Display`, and `Export` are also taller now so they match the annotation panel height more closely.
- Manual Sky Explorer text annotations now stay on the stable widget-space label pass, but their rendered font size scales with the current zoom level so they remain visually proportional to circles and ellipses on the image. The matching text hit-testing path still uses the same underlying annotation bounds, so selection and dragging remain aligned after zoom changes without reintroducing the stuck tool behavior.
- Sky Explorer text insertion now queues the add-text dialog until after the image-view mouse release handler has fully unwound, preventing the modal dialog from interfering with the direct-edit gesture state. Text hit-testing was also realigned to the zoom-scaled widget-space text rendering so clicking visible text selects and drags the existing annotation instead of starting a broken pending text operation.
- Manual text zoom scaling no longer clamps rendered text to a 6 pt minimum while the image is fit-to-window, so text visibly shrinks and grows with zoom instead of appearing fixed until very high zoom levels. The text hit-test footprint now uses the same unclamped zoom scale.

## 2026-05-13
- Moved `Astrostack` to the bottom of the `Mode` menu and relabeled it as `(alpha) Astrostack` to better reflect its current status.
- Windows app startup now sets an explicit AppUserModelID in addition to the existing `.ico` window icon so the taskbar can use the Citizen Astronomy logo instead of falling back to the Python interpreter icon.
- Restyled the shared `Display` popup layout used by Differential, HR, Asteroid/Comet, and Sky Explorer so stretch controls and action buttons have more breathing room and a less cramped two-row presentation.
- Fixed stationary Asteroid/Comet `Trail` GIF exports caused by generated cumulative stack frames reusing the first rendered image when every frame borrowed the same source FITS path. Annotated image views now include the generated display array identity in their render cache key, so one-pass Trail exports keep the speedup while each cumulative frame is actually rerendered.

## 2026-05-12
- Asteroid/Comet `Trail` export now uses a one-pass cumulative synthetic-tracking stack path. The previous implementation rebuilt prefix stacks from scratch for every GIF frame and wrote temporary FITS stacks, so a 100-frame trail repeated earlier frame reads/alignment thousands of times; the default no-rejection average path now prepares each target-aligned crop once, advances a running stack, and renders each cumulative frame from memory.
- Fixed a calibration artifact source introduced by dark exposure scaling: raw dark masters are no longer exposure-scaled unless they are known to be bias-corrected. When no bias master is available, the dark is applied unscaled and calibration warns if the science/flat exposure differs, avoiding scaled camera-offset and banding patterns that can show up as horizontal/structured artifacts in stretched stacks.
- Legacy saved image-display settings that still had `Asinh` from the old startup default now migrate to `Auto Stretch`, and all remaining image-view fallback paths now use `Auto Stretch` instead of falling back to `Asinh` when no saved setting is available. Astrostack `Stack` now opens a compact GIF settings dialog before export with frame rate, frame time, scale, loop behavior, and estimated per-pass duration controls.
- WCS alignment now performs a star-based residual refinement after reprojecting each non-reference frame onto the reference grid. This makes the Astrostack and calibration alignment path behave closer to PixInsight's star registration when a frame has a valid but slightly imperfect WCS solution: the WCS gets the frame close, then bright star matches correct remaining whole-frame translation before the aligned FITS is written.
- Calibration now rebuilds a bias/dark/flat master whenever that source is explicitly selected instead of silently reusing an older cached master from the output folder, and dark subtraction now scales the bias-corrected master dark by the science/flat exposure time when exposure metadata is available. If frames inside a selected calibration-master set have mismatched exposure times, calibration now shows a warning and continues with the current mismatched set using the median exposure for scaling instead of stopping the run. This makes calibrated aligned stack/GIF output respond to the selected dark frames and removes under-subtracted fixed-pattern noise when dark and light exposures differ.
- Astrostack export now asks whether the loaded dataset is already aligned before building the cumulative GIF. Choosing the aligned path skips the WCS alignment pass entirely and stacks the loaded frames directly, while choosing the nonaligned path keeps the existing alignment workflow.
- Added a new `Astrostack` mode for regular-object cumulative stack animations. It opens a folder of supported plate-solved frames, shows the shared annotated-image preview controls, lets you Shift-drag an optional rectangular ROI on the reference frame, and exports a cumulative aligned GIF that averages more frames over time so the SNR improvement is visible frame by frame. Astrostack ROI drafting now enables the image-view gesture path correctly, so Shift-drag selection works instead of falling through to normal pan behavior. Settings now also include a dedicated `Astrostack` tab with an `Alignment Workers` control, and explicit Astrostack worker settings are honored by the Astrostack alignment path instead of being clamped to the generic low default caps. Astrostack export is also faster now because rectangular ROI exports align directly onto the cropped reference grid instead of the full frame, the cumulative export loop no longer writes a temporary FITS file per animation frame, rendered GIF frames are produced directly from arrays instead of through a widget capture path, and aligned frame reads now use bounded read-ahead parallelism before the sequential prefix-stack step.
- Fixed a crop-aware Astrostack alignment regression on real solved fields by deriving the cropped reference WCS through Astropy WCS slicing instead of manually shifting only `CRPIX`, so distortion-aware solutions keep the stacked stars aligned inside rectangular ROI exports.
- Asteroid/Comet export now uses a single `Export` drop-down with `Image`, `Blink`, and `Trail`; the blink frame time now lives in `Settings -> Asteroid/Comet -> Export` with a default of `0.05 s`, `Image` saves the current asteroid/comet image viewport with overlays, and `Trail` exports a target-aligned cumulative GIF.
- Annotated image displays now support an `Auto Stretch` mode across Differential, HR Diagram, Asteroid/Comet, Transient, and Sky Explorer views. Raw science images can recommend `Auto Stretch` automatically where recommended-stretch logic already applies, and the shared image-display setting now persists it alongside the existing Linear, Asinh, Sqrt, and Log modes. The stretch now uses a PixInsight/XISF-style adaptive display function with a true midtones transfer function and display-source statistics, avoiding the harsher full-native-range normalization that made low-signal backgrounds look noisier and highlights blow out more easily.
- `Auto Stretch` is now the default annotated-image stretch mode for the shared image display settings and the main image viewers, replacing `Asinh` as the neutral startup default.
- The stretch drop-downs now also list `Auto Stretch` first, so the visible menu order matches the shared default selection instead of still presenting `Asinh` above it.

## 2026-05-11
- Asteroid/comet timestamp handling now treats timezone-naive image header times as UTC and no longer queues alternate timestamp interpretations, eliminating the daylight-saving/local-time ambiguity that could place known-object markers at future positions.
- Asteroid/comet and scan-layer observation timestamps now come only from image header metadata such as `DATE-OBS` or XISF observation-time properties; filename-derived timestamps are no longer used as fallbacks or alternate-time candidates, so open-source datasets with different naming schemes do not silently change observation times.
- Full-frame Synthetic Track now skips per-frame weighting-metric estimation for `average` GPU streams and uses a finite-fast-path for the zero-filled full-frame GPU accumulator/extrema updates, reducing unnecessary host/device synchronization and overhead in the common no-rejection GPU path.
- The Synthetic Track CPU/GPU comparison dashboard and standalone table now use a cleaner academic presentation with safer spacing, right-aligned metric columns, and non-overlapping panel annotations so the exported visuals read cleanly in documentation and review material.
- The dashboard now renders a compact comparison-table variant with abbreviated notes and tighter typography so the Notes column and footer stay inside the smaller dashboard card while the standalone table keeps the roomier full-width layout.
- Full-frame Synthetic Track now has an internal NumPy/CuPy backend abstraction, a persisted `Auto`/`CPU`/`GPU` backend preference in Settings, dedicated backend and GPU warm-up status lines in the preview and completion status, a one-time GPU warm-up pass before the first full-frame GPU stack, optional CuPy acceleration for full-frame `No rejection` Average/Mean/Min/Max stacks when available, safe CPU fallback when the selected rejection mode or local CUDA runtime cannot support the GPU path, and a dedicated full-frame benchmark script with untimed warm-up support. The optional GPU extra now targets the CUDA 11.x CuPy build that matches the validated local toolkit.
- The full-frame Synthetic Track benchmark tooling now reads the saved shared worker setting instead of silently defaulting to serial `0` workers, and there is a new real-dataset comparison script that uses the same timestamp-sanity and known-object motion inference path as the UI before timing CPU versus GPU full-frame stacks.
- The real-dataset CPU/GPU comparison script now relies on the stacker's tiny internal GPU warm-up instead of running an entire extra full-resolution GPU stack before timing, and it prints flushed per-backend progress during long full-frame comparisons so the run no longer looks stuck.
- The real-dataset CPU/GPU comparison script now also supports `--backend cpu`, `--backend gpu`, or `--backend both`, so quick GPU diagnostics no longer have to pay for a CPU stack, and the CPU/GPU image-difference block is computed only when both backends are requested.

## 2026-05-10
- File menu now includes a mode-aware `Clear Cache` action that clears only the currently selected dataset's cached app artifacts and warns instead of doing nothing when no dataset is selected.
- Asteroid/comet aligned-frame overlays now keep per-object current-frame predictions even if one local-match measurement fails, instead of discarding the whole frame refresh and falling back to stale reference-frame pixels for every object.
- Asteroid/comet Generate now sanity-checks ambiguous observation timestamps by testing alternate `DATE-OBS`/filename interpretations against local image matches, then applies the selected time correction across the loaded sequence so Davida-style raw and aligned stacks no longer keep a consistent asteroid marker offset.
- Asteroid/comet target markers on the main image no longer draw a filled center dot, so small objects stay visible inside the target box/crosshair overlay.
- Synthetic Track now splits the old two-choice combine mode into separate Integration, Weights, and Rejection controls in both Settings and the advanced one-stack dialog, and the stacker now supports weighted mean plus min/max and sigma-based rejection variants while keeping legacy saved combine-mode settings compatible.
- Asteroid/comet Settings now include an off-by-default manual detection magnitude-limit override so advanced users can force Generate to use a fixed limit instead of the saved or estimated Mag Limit value.
- Synthetic Track export now also offers a linear raw FITS save that writes the unstretched stacked float data directly, using a separate pre-preview stack in crop mode so display stretch, preview background normalization, and curve adjustments are not baked into the saved stack.

- Fixed an asteroid/comet detection regression where the live SkyBoT query service still exposed the pre-`magnitude_limit` method signature, causing Generate to fail immediately when the UI passed the configured magnitude limit.
- Asteroid/comet overlay and tracking coordinates now reuse the representative solved field while viewing aligned stacks, so current-frame marker positions no longer fall back to stale reference-frame pixels when the displayed aligned frame itself lacks celestial WCS.
- Asteroid/Comet `Track Object` now follows the known/predicted position by default instead of the noisy detected local match, with an advanced visual setting to switch back to detected-match tracking when desired. The current-frame overlay path also retries prediction-only asteroid positions before falling back, so labels no longer silently reuse stale reference-frame coordinates when live local matching fails.
- Differential Photometry now invalidates the older frame-fallback aperture measurement cache and reruns local-FWHM photometry, and Discover candidate measurements now carry the resolved frame FWHM fallback so targets and comparison stars use the same adaptive aperture basis.

## 2026-05-08
- Asteroid/Comet Detection now removes the lower duplicate panel progress bar, uses the shared top progress bar during busy work, drops the extra `Predicted Objects` and `Asteroid / Comet Image View` card titles, and moves stretch/curves/invert/reset controls into a `Display` menu.
- Asteroid/Comet Detection now shows `Open` when no sequence is loaded, and that primary button opens a subframe folder and immediately starts Generate for the loaded asteroid/comet sequence. SkyBoT cone searches now retry transient upstream 5xx failures and report a clearer IMCCE service error when the server stays unavailable.
- Asteroid/Comet Detection now keeps the idle `Generate` button visually muted after loading instead of highlighting it like a required next click, groups `Target Marker` / `Center Object` / `Track Object` separately from blink playback controls, keeps `Hide Info` on its own, and shows the shared top progress bar while manual frame changes are refreshing.
- Sky Explorer now uses the shared app progress bar during Explore and Mag Limit runs, and the orphaned tiny top-left progress widget has been removed so progress feedback appears in one place only.
- Sky Explorer no longer shows the extra recently added panel progress bar row; the mode now keeps the existing status/work-log progress feedback without duplicating a second visible progress bar.
- Sky Explorer now mirrors Differential Photometry’s image-toolbar pattern with a `Display` menu for stretch/curves/invert/reset, keeps `Center Object` and `Mag Limit` adjacent, and moves `Export` to the far right of the toolbar.
- Sky Explorer now keeps star-like SIMBAD rows on their own stellar identifiers instead of promoting parent deep-sky aliases from `IDS`, so M46-field stars no longer appear as separate `NGC 2437` star overlays/results.
- Sky Explorer no longer mistakes hyphenated Minkowski planetary-nebula identifiers such as `M 1-18` for Messier objects, so M46-field nebulae are no longer mislabeled as distant `M 1`/Crab Nebula overlays.
- Sky Explorer now deduplicates deep-sky objects across all SIMBAD catalog aliases, so `M 46` and `NGC 2437` collapse to one overlay/result, and SIMBAD cluster-member stars keep their own member names instead of inheriting the parent cluster alias.
- HR Diagram now gives `Show Motion` the same accent-outline tool styling as `Find Cluster`, and HR preparation progress now appears as a compact animated footer bar so background generation work is visibly active instead of looking stalled.
- HR Diagram now places `Show Motion` directly before `Find Cluster` in the plot toolbar, and the `Image`/`Results` view toggle pair now shares the same accent-vs-dim button styling so the selected view is visually obvious.

## 2026-05-07
- Asteroid/Comet Synthetic Track advanced mode now lets you choose object-centered crop stacking or manual full-image stacking, the crop radius is honored again when using the cropped path, and full-image mean stacking no longer allocates a giant frame cube before combining.
- HR Diagram now reuses the existing footer draft-status slot for rotating guidance tips when no ROI draft is pending, and the top workflow row now frames the `View` and `Catalog Links` controls in accent-colored groups so `Image`/`Results` and `CDS`/`Simbad`/`Gaia`/`VSX` read as clear button clusters.
- HR Diagram ROI now uses a two-state action button: after applying a draft ROI, `Update` becomes `Reset` so the currently applied ROI can be removed directly; drawing a new draft switches it back to `Update`.
- HR Diagram bright-target background catalog/designation lookup now defaults to rows at or brighter than 9.0 mag instead of 12.0 mag.
- HR Diagram ROI drag start and mouse-up commit are faster now because beginning and ending a draft stay on the lightweight overlay-preview path instead of forcing control refreshes on press or a full HR image redraw on release.
- HR Diagram ROI drawing is faster during Shift-drag because mouse-move previews now repaint only the previous/new ROI bounds and skip footer/button refresh work until the drag starts or finishes.
- HR Diagram ROI drafting is responsive again because live Shift-drag previews now update only the lightweight selection overlay instead of rebuilding the full image panel every mouse-move, and the HR top row now adds an `Update` action beside `Open` that enables only when a newly drafted ROI differs from the ROI already applied to the current diagram.
- HR Diagram restores the visible `Image` and `Results` switch pair in the top row, while the HR plot-toolbar checkbox chrome now adds enough left inset that `Class Guides` and `Age Guide` indicators render cleanly instead of looking clipped.
- HR Diagram now matches Differential Photometry more closely in the top workflow row: the redundant `Image` toggle is hidden, `Stretch`, `Curves`, and `Reset` now live under a `Display` menu, and the rounded primary action stays as `Open` while automatically starting HR preparation as soon as a source image is chosen.
- HR Diagram no longer shows the extra recently added inline progress bar inside the mode workspace, and the plot-toolbar spacing now gives `Only Group`, `Class Guides`, and `Age Guide` enough separation that their checkbox indicators are fully visible.
- HR Diagram mode now uses the same premium card-style chrome as Differential Photometry and Asteroid/Comet Detection, with a polished workflow strip plus styled results, source-image, plot, and work-log panels for a more consistent cross-mode workspace.
- Asteroid/Comet Detection now uses the same premium card-style chrome as Differential Photometry, with a polished top workflow strip plus styled `Predicted Objects` and `Asteroid / Comet Image View` panels so the modes feel visually consistent.
- Differential image `Center Object` now sits beside `Display` instead of inside the Display popup, and it behaves as an enabled/disabled toggle that can automatically recenter the annotated image when the selected Source Results target changes.
- Differential image equatorial grid overlays now keep their full sampled path once they cross the image, so lines reach the frame edges instead of stopping short near the borders.
- Light Curve `Export` is now wider and simplified to `Theme-based`, `Scientific`, and `Animation`. Theme-based and Scientific exports now share one save dialog that supports PNG, PDF, SVG, and JPG output, while Animation continues to export the animated GIF workflow.
- The Light Curve view now adds an `R` button beside the existing bottom-left auto-range control, and clicking it clears any active Ctrl-drag segment isolation so the full dataset is shown again without needing to redraw the selection window manually.
- The Light Curve bottom-left `R` control now uses the same hover-only plot-button treatment as `A`, with matching styling and side-by-side placement instead of a separate floating widget look.

## 2026-05-06
- Animated Light Curve GIF export now renders both major and minor grid lines to better match the live Light Curve view, and the export runs in a background worker with a visible progress window so the app stays responsive while frames are rendered and encoded.
- Light Curve `Export` now includes `Animated GIF`, which renders a themed premium animation that starts from an empty plot, reveals data points progressively, then draws the fitted curve at the end when fit display is enabled. The GIF loops, targets a 5-second baseline, and automatically extends longer for dense light curves so point-by-point reveals remain readable.
- Animated Light Curve GIF opening frames now keep visible grid lines and no longer show the empty-state `No light curve to show` message before the first points appear.
- Differential image previews now draw neutral gray solid equatorial grid lines with shorter RA/Dec labels, and the image controls add `Center Object` so the selected target can be recentered immediately.
- Renamed the guided Differential workflow section from `2. Generate` to `2. Pipeline`, added always-on `Plate-solving` and `Filter Targets` indicators before `Light Curves`, and added a grouped Filter Targets panel beside the Pipeline checkboxes.
- Added workflow-only target-filter overrides for Differential Pipeline runs: Designation filters can be toggled against Settings > General defaults, Brightest Variables recalls the Settings-General percentage while allowing a per-run override, and Analyze Best uses the Sky Explorer visible-limit estimator plus AAVSO-friendly ADU/saturation screening to keep targets that are neither too dim nor too close to saturation.
- Fixed `Find Better Fit` spawning visible child command windows on Windows by configuring process-pool workers to launch through `pythonw.exe` when available. This keeps the parallel comparison search but stops the worker-console spam.
- Restored fast interactive `Fit Period` behavior for selected light-curve segments. The button still runs in the background, but now skips the batch-only BLS duration pass that was making segment fits take tens of seconds.
- Made Ctrl-drag light-curve segment selection feel immediate again by repainting the filtered curve without forcing an expensive annotated-image panel rerender during the same UI event.
- Restored status-bar feedback when `Fit Period` reuses cached Find Better Fit, manual override, or legacy saved period values.
- Fixed a cached Differential Photometry relaunch regression where restoring cached comparison-fit series could crash with `name 'normalized_filter_name' is not defined` while reopening cached results after Generate.
- Differential Photometry cached Generate runs now persist and restore both successful period results and explicit no-result period lookups/fits, so previously cached Pull Period and Calculate Period outcomes are reused instead of requerying the same targets again. Cached calculated-period filter keys are also normalized during restore so workflow skip checks match the restored series keys reliably.
- Polished the guided Differential Photometry workflow completion state so the success message now shows a green `✓ Results are ready...` cue and the `Close` button switches to the accented primary style once Generate finishes.
- Fixed a second `Find Better Fit` regression by restoring the missing `dataclasses.replace` import in the worker path.
- Added the first local Machine Learning review flow for Differential Photometry Source Results. You can now review rows in a dedicated `Label` window with a `Review -> Label -> Next` workflow, save `Detectable Variable`, `Not Detectable`, or `Ambiguous` labels, train a local detectability model from those reviewed rows, and see `Label` plus `ML Score` columns directly in Source Results. The Differential feature extractor uses current light-curve variability metrics, photometry QA values, aperture/annulus settings, exposure and frame metadata when available, plus observing-setup metadata such as telescope, camera, site, and Bortle settings.
- Differential quick-label saves are now immediate instead of forcing a full Source Results refresh after every click. Each label action updates only the touched row and selected details pane, so `Detectable Variable`, `Not Detectable`, and `Ambiguous` clicks advance much faster.
- Differential training now uses a single `Train Model` entry point instead of separate `Save Label`, `Label`, and `Train Model` controls. The button opens the review window, the dialog uses the active app plot theme, `<` and `>` navigation flank the labeling buttons, and clicking `Train` closes the dialog and immediately trains the local model from the reviewed labels.
- Removed the visible Differential training summary text beside `Train Model` so only the button remains in the toolbar. Training summaries continue to go to the Work Log and status messaging.
- Fixed `Find Better Fit` failing with `name 'combinations' is not defined` by restoring the missing `itertools.combinations` import in the comparison-fit worker path.
- `Fit Period` button clicks now run the selected-series solve in a background worker with a reentry guard instead of blocking the UI thread. This keeps the app responsive and prevents accidental duplicate queued fits if the button is clicked again while the solve is still running.
- Differential `ML Score` sorting now groups rows by predicted label first and then sorts by confidence within each label group, making it much easier to find all `Detectable Variable`, `Not Detectable`, or `Ambiguous` rows together.
- Refined the Differential Photometry center splitter so the divider between the left results stack and right image/info stack now matches the thinner Source Results and Light Curve handle, visually spans the full panel height, and remembers restored widths and heights for the left/right stack splitters together with the existing right-side image/info splitter.
- Fixed the remaining Differential Photometry cached Generate freeze around the `Workflow Reused Cached Results` dialog. Large cached processing reports are now loaded in a background worker before the dialog appears, and restoring an already cached report no longer rewrites the same large cache file before Pull Periods begins.
- Improved Differential Photometry Generate responsiveness after cached or completed light-curve results are accepted. The workflow now paints a `restoring cached results` stage and yields UI repaint/timer events between the heavier Source Results, file table, notes, plot, and period-cache restoration steps before Pull Periods starts, avoiding the apparent freeze after the found-files/cached-results prompt closes.
- Refined the Differential Photometry `Light Curve` toolbar again so `Fold` now sits before `Filter`, both buttons keep the same width for a cleaner premium row, and the old visible `Period` label/spinbox summary has been removed from the toolbar. When a fit is active, the current fitted period now appears as a themed in-plot badge instead of separate toolbar text.
- Simplified the Differential Photometry `Light Curve` controls so the redundant target selector, arrow buttons, reset button, standalone filter checkbox, standalone fit checkbox, and inline export-format picker are no longer shown. `Fit Period` now toggles the active fit on and off directly, `Filter` stays as the single entry point for filtering, and export format is now chosen from the `Export` menu actions.
- Added a reset path inside the Light Curve filter settings dialog so reopening `Filter` can also clear or turn off active filtering without needing a separate toolbar checkbox.
- Changed Light Curve keyboard navigation to follow the selected `Source Results` row with Up/Down movement, keeping the active light curve aligned with whichever source row is currently selected.

## 2026-05-05
- Rebalanced the Differential Photometry workspace hierarchy so the right pane now keeps `Work Log`, `Summary`, and `QA Details` directly under `Annotated Image`, while the left pane stacks `Light Curve` above `Source Results` / `File Results` for a more natural review flow from image inspection to curve analysis to tabular results.
- Restyled the Differential Photometry main window to match the newer workflow chrome: the workspace strip, analysis strip, Source Results area, annotated-image panel, and light-curve panel now use the same rounded premium card treatment, softer splitter handles, and workflow-style rounded controls and hover states so the main workspace and the guided workflow feel like one consistent interface.
- The guided Differential Photometry workflow now also skips `Pull Periods` and `Calculate Periods` when the currently displayed source rows or light-curve series already have compatible cached period results. The main window no longer shows separate `Pull Period` and `Calculate Period` buttons; those actions now live on the Source Results right-click menu, and the analysis strip swaps `Increase SNR` ahead of `Discover`.
- The guided Differential Photometry workflow now checks for reusable `calibration_output` products before reopening Calibration. If cached `aligned` or `calibrated` frames already exist, Generate can continue from those folders instead of repeating preparation, and if a compatible cached processing report already exists for the current files/settings, the workflow restores the Source Results and light curves immediately and shows a summary explaining which cached stages were reused.
- Optimized the guided Differential Photometry workflow for large calibrated stacks such as AE UMa: when `Calibration` and `Star Alignment` are both selected inside the workflow, Generate now skips writing a second full aligned FITS folder and continues directly from the calibrated frames, because Differential photometry already measures each frame from its own WCS. The standalone menu-bar `Calibrate` flow still supports materialized aligned output when the user actually wants exported aligned files.
- Added a faster WCS alignment path for typical solved image stacks: frames whose WCS differs by an affine shift/rotation/scale now use SciPy's optimized affine transform with a higher safe worker cap, while complex WCS cases still fall back to generic `reproject`. Calibration also releases full-frame master arrays before alignment starts, and fallback `reproject` no longer allocates an unused footprint array.
- Fixed a Differential workflow regression where calibration could finish but alignment would bloat RAM and fail on larger folders. WCS alignment no longer spins up one worker per shared calibration slot; it now uses a conservative worker cap because each reprojection worker holds much larger full-frame arrays in memory than the calibration stage.
- Cleaned up the workflow calibration integration so the hidden workflow-only light-frame/output controls no longer leak a stray `C:\Users\...` path line into the dialog header, and workflow-driven calibration now reuses the existing in-dialog progress/status area without opening the separate menu-bar `Calibrate Images` progress popup.
- Refined the workflow-only calibration dialog so the stray alignment checkbox no longer appears over the header, the single section is now titled `Calibration Files`, the workflow step only accepts Bias/Dark/Flat folders instead of file-or-folder inputs, and the confirmation button now reads `Continue` to reflect that the Generate workflow remains the action that actually proceeds with processing.
- Refined the guided Differential Photometry workflow so the inactive `Generate` button now looks visibly disabled, and Generate's `Calibration` path now opens a slimmer premium-styled calibration dialog that only asks for Bias, Dark, and Flat sources while reusing the cached calibration-frame folders saved from the main menu Calibration tool.
- Refined the guided Differential Photometry workflow dialog so `Folder` and `Generate` now have a more obvious hover state, the section titles sit farther above their rounded cards, and `Star Alignment` stays independently selectable and maps to the existing aligned-stack Differential processing path even when Calibration is left off.
- Enlarged the guided Differential Photometry workflow window and removed the temporary scrolling body so the full Folder, Generate, Progress, and footer controls stay visible at once inside one fixed-size dialog.
- Reworked the guided Differential Photometry workflow dialog body into a scrollable fixed-size layout so moving the window or crossing display/layout changes no longer squeezes the Folder, Generate, and Progress sections into overlapping controls.
- Fixed the guided Differential Photometry workflow dialog collapsing while being dragged or moved by removing the move-time section-height recalculation that could re-freeze group boxes at bad sizes; the dialog now relies on stable fixed-row sizing and retains progress-bar space without reflowing the Generate section.
- Stabilized the guided Differential Photometry workflow dialog so dragging or moving the window no longer collapses the Generate controls, and added new default-off `Calibration` and `Star Alignment` workflow options above Light Curves. Calibration now reuses the existing calibration pipeline dialog, and the workflow can continue from the calibrated or aligned output folder automatically before generating the Differential results.
- Polished the guided Differential Photometry workflow window so it now stays fixed-size, long folder paths elide with `...`, the optional Measurements-table warning moves to a footer tip instead of expanding the layout, Reset and Default buttons were added for the workflow controls, and the dialog uses accent-colored progress and refined card/button styling.
- Added a guided Differential Photometry Open workflow window: the main button now always opens a Folder -> Generate -> Progress flow, folder scans assume one object and skip the old loaded-results popup, Generate always creates light curves, optional Pull Periods and Calculate Periods default on, optional Measurements Table defaults off with an extra-time warning, and the workflow dialog stays open with simplified progress while the Work Log keeps detailed output.
- Added a dedicated root-level differential_photometry.md reference that documents the Differential Photometry workflow end to end, including UI structure, catalog flow, measurement math, QA logic, refinement tools, caching, and export behavior for AI-readable repo context.
- Reorganized Differential Photometry controls so `Open` or `Generate` and `Filter` stay visibly open on the left, the analysis actions are grouped as visible side-by-side buttons on the upper right, source-table double-click opens SIMBAD, source-table right-click opens `Simbad`, `VSX`, `CDS`, and `Gaia` review actions, `Display` stays as a popup button in the image toolbar, and the top processing bar stays visible as a thin dark line that switches to an indeterminate busy animation during background work.
- Refined the Differential Photometry analysis strip by removing the visible `Analysis` text label, styling the remaining action row as a grouped control, moving the old `Measure` action behind the `Measurements` tab with an explicit tab tooltip, and normalizing the `Display` menu button height so it aligns with neighboring actions such as `Increase SNR`.
- Fixed a remaining Differential Photometry header alignment offset by removing the default right-panel layout margins, so the right-side `Display` toolbar row now sits on the same horizontal line as the left-side analysis actions such as `Increase SNR`.
- Refined Differential Photometry mode UI: the light-curve plot no longer reserves space for the fixed interaction hint, the Workspace and Results side-panel group frames were flattened, and the primary action now starts as `Open` before scan results exist and switches to `Generate` afterward with an `Open` menu for changing or rescanning folders.
- Added default-on rotating status-bar tips for Differential Photometry, exposed a Settings > General toggle to disable them, and moved cached period recall notices into the Work Log so the bottom status area can teach workflow tips without being occupied by cache messages.

## 2026-05-03
- Hardened the annotated image view against RGB preview variants from raster sources. Sky Explorer previews now coerce RGBA, single-channel, or channel-first rendered arrays into the grayscale/RGB shapes Qt expects instead of raising `Rendered annotated image must be grayscale or RGB` while opening a source image.
- Fixed Sky Explorer wide JPEG fields that could spend many minutes in the SIMBAD named-object step after astrometry.net solved them. SIMBAD region searches now use server-side object-type criteria, keep fine 20-arcminute tiling only while the tile count stays bounded, switch very wide fields to a small overlapped coarse query set, and stop spending unbounded time on one catalog layer.
- Differential Photometry annotated images now offer the same right-click `Search` action as Sky Explorer, opening a SIMBAD coordinate search for the clicked WCS position.
- Added the Differential Photometry processing progress bar to the top of the app under the menu bar and made it visible while WCS/photometry progress updates are active.
- Simplified the Differential Photometry annotated-image toolbar to a single `Apply` action for aperture edits, added `Hide Info` / `Show Info`, changed manual target labels to `Target`, and made aperture drags preview in the image/info panel until mouse release.
- Removed the framed `Annotated Image` and `Light Curve` group boxes from the Differential Photometry analysis splitter so the image and plot use more of the available workspace.
- Refined the Differential Photometry aperture editor so plain left-drag pans the image, no-drag clicks place apertures on release, info-panel scroll position survives aperture updates, and hiding the info overlay no longer resizes the image viewport.

## 2026-05-02
- Added an Asteroid / Comet Image View `Target Marker` toggle beside `Center Object`, and wired the selected-object overlay to reuse the square-and-guides target marker directly on the main review screen.
- Added persistent Asteroid/Comet target-marker styling controls for line color, accent color, label color, and thickness in `Settings -> Asteroid/Comet`, and applied the same style settings to discovery target overlays.
- Added a persistent Asteroid/Comet target-marker outline color, stopped asteroid/comet blink playback automatically before applying image invert, and split asteroid GIF versus MP4 export resolution defaults in settings.
- Differential Photometry light-curve export now uses an explicit `Export` menu with `Theme-based` and `Scientific` choices instead of the old advanced toggle, equatorial RA/Dec labels stay attached to visible grid lines while zooming and use blue tones tuned for normal versus inverted images, and the right-side `Annotated Image` / `Light Curve` pane is now a cached vertical splitter.
- Differential Photometry automatic measurements now use each source's local post-recenter FWHM for aperture and annulus radii with frame-FWHM fallback, the settings dialog exposes only adaptive FWHM scale controls, and the annotated image now shows RA/Dec hover coordinates while hiding labels for grid lines outside the visible frame.
- Differential Photometry RA/Dec grid labels now prefer bottom anchors for Dec labels and left anchors for RA labels when zoomed out, avoiding central or alternating-edge label clusters while preserving smooth movement during pan and zoom.
## 2026-05-01
- Fixed MP4 blink export for odd-sized captured viewports by padding frames to codec-safe even dimensions before ffmpeg/libx264 encoding.
- Asteroid/Comet and Transient Finder `Export Blink` now offer MP4 video output alongside GIF, using the same captured viewport/overlay state and the existing export scale setting where available.
- Fixed the Asteroid/Comet Target Marker toggle so the selected object's square-and-guide marker still appears when the broader known-object marker layer is hidden.
- Extended the local Candidate Trainer to Asteroid/Comet Discover results inside the Discover dialog. Potential discovery and borderline-review rows can now be labeled where their candidate overlays are visible, saved as moving-object training examples, trained with the shared lightweight Random Forest path, and re-scored with dialog-local Label and ML Score columns while the main Predicted Objects table stays focused on known-object predictions.
- Fixed a Discover streaming regression where the visible-limit benchmark summary could fail with `name '_detection_within_estimated_limit' is not defined` before any residual results appeared.
- Added a transient-style quick-label popup for Discover candidates and a new target marker style with a red square plus guide lines so potential discoveries stand out more clearly in the Discover preview and main asteroid/comet image.
- Refined the fast transient labeling dialog so label buttons no longer retain a focus outline after advancing to the next candidate, and added a blink-speed selector with Slow, Normal, Fast, and Very Fast playback options.
- Added a fast transient labeling dialog opened from the Transient Finder `Label` button. It shows a zoomed 50x50 candidate cutout, auto-blinks across the candidate's available frames, presents label choices as direct buttons, saves the chosen training label, and immediately advances to the next candidate so transient training review can be done rapidly without manually switching images, zooming, and blinking from the main table.
- Added the first local Candidate Trainer foundation for Transient Finder. Reviewed transient candidates can now be labeled as Real, Artifact, Known Object, Moving Object, Noise, or Unsure, saved to a shared SQLite training database, trained with a lightweight scikit-learn Random Forest model, and re-scored in the candidate table with label and ML-score columns while preserving model provenance for future extension to moving objects, variables, and exoplanets.
- Replaced the shared `Adjust Levels` image-display tool with a Photoshop-style `Curves` editor across annotated images, HR Source Image, asteroid/comet review, transient review, and Sky Explorer. The new editor provides histogram-backed draggable curve points, click-to-add and right-click-to-remove control points, fast downsampled live previews, persisted annotated-image curve settings, and full-resolution application when accepted.
- Sky Explorer `Mag Limit` representative stars now also appear in the results table under their own `Magnitude Limit Measurement` group instead of existing only as image overlays. These rows reuse the star styling, support row selection and `Center Object` focusing, and double-clicking a measurement row opens the matching catalog details for that star.
- Fixed Sky Explorer `Mag Limit` rendering after dense uncapped Gaia queries. Mag Limit no longer rejects its own Gaia result just because the circular catalog search returned stars outside the rectangular image footprint, so the successful "annotated N representative Gaia stars" completion now actually produces visible image overlays.
- Fixed Sky Explorer `Mag Limit` representative-star selection for dense fields. The dedicated Mag Limit Gaia query now keeps the full Gaia star set for magnitude-bin sampling instead of collapsing to the normal 250-brightest Sky Explorer Gaia overlays first, so wide fields such as the Soul Nebula can annotate representative stars all the way down to the configured Gaia limit instead of stopping near the brightest few bins.
- Refined Sky Explorer `Mag Limit` behavior so toggling it back on after changing the Gaia magnitude-limit settings re-runs the Gaia star query instead of reusing stale same-image stars from the previous settings. Mag Limit labels also now prefer a better star identifier, such as HD/HIP-style names when Gaia metadata provides one, instead of always showing the Gaia source number.
- Added a Sky Explorer `Mag Limit` annotation button. When enabled, it can run its own Gaia-only field query if Explore has not already loaded usable Gaia stars, then labels representative stars from bright to faint in 0.5 magnitude intervals with the Gaia G magnitude under the star name. Settings > Sky Explorer controls how many example stars are shown per interval.

## 2026-04-30
- Restored Sky Explorer catalog-shape rendering for galaxy-system annotations. Ellipse geometry now uses available major-axis, minor-axis, and position-angle metadata directly instead of requiring the object to also match the broad Galaxy filter, so Galaxy Group and Galaxy Cluster rows keep their size/shape overlays after the filter-semantics split.
- Fixed Sky Explorer galaxy-system filter semantics. Structural SIMBAD rows such as `GrG`, `CGG`, `ClG`, `SCG`, `PaG`, and `IG` now stay in Galaxy Group, Galaxy Cluster, or Galaxy Pair filters instead of also matching the broad Galaxy filter, while member-galaxy rows such as `GiG` and `GiP` remain individual galaxies. Group rows also keep their SIMBAD group name instead of being renamed to embedded NGC/IC member aliases such as NGC 7331, and the results table now shows the resolved user-facing type such as `Galaxy Group` while preserving the raw SIMBAD code in the tooltip.
- Fixed Sky Explorer type selection precedence for grouped galaxy objects in the UI. Objects such as `[CHM2007] HDC 1198` that resolve to `GrG` / galaxy-group codes now keep the more specific Galaxy Group or Galaxy Cluster style and classification when multiple related galaxy filters are selected, instead of being downgraded to the broader Galaxy definition because of Advanced-mode list order.
- Fixed Sky Explorer wide-field SIMBAD deep-sky discovery for crowded galaxy fields. Large deep-sky searches now fall back to deduplicated smaller SIMBAD cone tiles, request decimal-coordinate fields, and avoid the SIMBAD `V`/`B` region-query fields that were suppressing some galaxy rows, which recovers galaxies such as `LEDA 2051985` and `AGC 322715` in the SN2025rbs field.
- Fixed Sky Explorer classification for SIMBAD `AG?` / possible active-galaxy objects. Rows such as NGC 7320C now participate in the expected active-galactic-nucleus and galaxy filter buckets instead of falling through as unselected `other_deep_sky` entries.
- Fixed image overlay interaction so left-drag now works from anywhere on the image, including on top of annotated objects. Overlay selection is now deferred to mouse release when no real drag occurred, and ellipse overlays use the drawn rotated ellipse for hit-testing instead of an invisible bounding circle.
- Fixed Sky Explorer ellipse label placement for galaxies. Labels are now anchored from the ellipse edge in the chosen label direction instead of treating every galaxy overlay like a circle, so flattened galaxies such as NGC 7331 keep their labels noticeably closer to the visible ellipse.
- Fixed a Sky Explorer regression where adding SIMBAD/HyperLEDA galaxy magnitudes could make Stephan's Quintet and Deer Lick companions disappear under the saved Gaia/stellar magnitude limit. Deep-sky rows now remain available for galaxy-specific annotation controls, duplicate NGC rows can promote the surviving object's galaxy/Seyfert type over X-ray aliases, and interacting/group galaxy codes now match the Galaxy Pair/Group filters.
- Fixed Sky Explorer galaxy alias priority for NGC/IC components such as Stephan's Quintet. SIMBAD rows now de-duplicate against other SIMBAD rows, NGC/IC suffixes like `NGC 7318A` are canonicalized without merging distinct A/B components, NGC names outrank IC and other survey aliases, `B2` catalog identifiers no longer become Barnard designations, and galaxy rows can fall back to SIMBAD/HyperLEDA B-band style magnitudes when V magnitude is unavailable.
- Fixed Sky Explorer galaxy magnitude enrichment for SIMBAD/2MASX rows. HyperLEDA rows are now used to enrich matching SIMBAD galaxies with magnitude and shape metadata even when the dense anonymous-galaxy catalog is not being added as separate table rows, and NGC/IC names from matching galaxy supplements can replace less useful survey names.
- Sky Explorer now pulls galaxy brightness from HyperLEDA when available, so the galaxy annotation magnitude limit no longer drops shaped galaxies just because their row lacked SIMBAD photometry. Galaxy-class SIMBAD names also now prefer NGC/IC designations over Messier aliases when those identifiers are present.
- Fixed zoomed Sky Explorer panning so labels for overlays that are completely outside the visible viewport no longer get clamped into the image corner and dragged around while their object stays off-screen.
- Added Sky Explorer galaxy-annotation filters in Settings. Galaxy overlays can now be limited to objects at or brighter than a chosen magnitude threshold, and a separate toggle can suppress galaxy annotations unless enough catalog geometry is available to draw an ellipse-shaped marker.
- Added Sky Explorer color-relation controls in Settings. Generated object colors can now keep the existing bright fill/dark stroke relation or invert it to bright stroke/dark fill, while label text can independently choose the darker or brighter related color unless an explicit text override is set.
- Refined Sky Explorer interaction and rendering responsiveness. Planetary nebulae now inherit the simple Nebulae / ISM category coloring/filtering, object-type toggles no longer recenter the image unless Center Object is enabled, hidden-by-filter objects are left out of overlay construction, and normal label placement now uses a fast path instead of running the clamped edge-search on every repaint.
- Sky Explorer large-object annotations now stay bounded by the image field. Oversized circular overlays are clipped at the frame edge, labels remain clamped inside the visible image while staying near the circle edge, and a new default-on Settings > Sky Explorer option scales overlay stroke thickness with object size so broad nebula markers remain readable.
- Sky Explorer extended-nebula overlays now use an image-size-proportional radius cap instead of the old generic 220 px cap. High-resolution frames such as the Soul Nebula no longer shrink large Sharpless extents back down to a small marker after catalog scaling is applied.
- Fixed Sky Explorer deep-sky de-duplication so SIMBAD rows that duplicate Sharpless, Barnard, VdB, or NGC2000 supplement rows inherit the supplement's geometry metadata instead of discarding it. This lets extended-nebula overlay scaling actually use Sharpless diameters for objects such as Sh2-199 when SIMBAD returned the visible row first.
- Added a default-on Settings > Sky Explorer control for extended nebula overlay scaling. When enabled, emission, reflection, dark, HII, and general nebula annotations use a larger visual footprint derived from catalog diameter so broad structures like the Soul Nebula are covered more realistically while table/catalog data remain unchanged.

## 2026-04-29
- Sky Explorer overlay labels now scale up with large annotated objects instead of staying at a fixed small font. Large nebulae, galaxies, and other extended objects now receive larger on-image text while still preserving per-type font family and style overrides.
- Added a `Filter` button beside Sky Explorer's `Select All` control with a toggle to hide objects whose magnitude is unavailable. The toggle updates the current results view immediately, recomputes displayed layer counts in the summary panel, and is saved so future Sky Explorer runs honor the same missing-magnitude rule.
- Generalized the Sky Explorer magnitude limit into a final object-level filter. Any catalog object with a known numeric magnitude fainter than the configured limit is now removed before sorting, summaries, table rows, and overlays are built, regardless of which catalog or object type produced it; unknown-magnitude nebulae, clusters, and similar objects remain visible when the catalog cannot prove they are fainter than the limit.

- Fixed a Sky Explorer SIMBAD classification leak where literal `*` star rows with NGC aliases, such as faint `2MASS` members around the Rosette field, could be promoted into Deep Sky before the magnitude filter ran. These rows now stay in the star-like/general path and respect the configured Sky Explorer magnitude limit.

- Sky Explorer's magnitude setting now applies consistently to star-like catalog layers instead of Gaia alone. VSX variable-star queries now use the same cutoff, exoplanet hosts are filtered by host magnitude when available, and SIMBAD general/star-like rows respect the limit while deep-sky structure catalogs remain available for nebulae, clusters, and galaxies with incomplete magnitude metadata.

## 2026-04-28
- Sky Explorer wide-field overlays now suppress bulk text labels on dense layers instead of labeling every non-Gaia object at once. Variable-star labels are now limited to the selected target, and deep-sky or general-object labels are capped so large fields remain readable while the full marker and results-table data stay available.

- Sky Explorer now has dedicated Gaia query controls in Settings: a default `17.0 mag` Gaia limit, an optional hard row cap toggle, and a stored row-cap value for dense fields. Sky Explorer Gaia lookups now run separately from the VSX and exoplanet catalog path so variable-star or exoplanet exploration no longer waits on Gaia when that layer is disabled.

- Sky Explorer now supplements deep-sky results with dedicated Sharpless, Barnard, and Van den Bergh catalogs, and SIMBAD alias resolution now prioritizes `Sh2`, `VdB`, and `B` designations over more generic alternate names when those identifiers are available.

- Sky Explorer result groups are now collapsible from the grouped results table, and group hue changes applied from Settings now refresh member text-label colors alongside stroke and fill colors when no explicit text override is set.

- Fixed Sky Explorer SIMBAD coordinate parsing for newer lowercase decimal-degree `ra` and `dec` result columns. Objects such as NGC 1799 now keep their catalog RA/Dec instead of being reinterpreted as hourangle text, and saving Settings > Sky Explorer now immediately refreshes the object-type colors and current image overlays.

- Refined Sky Explorer grouping, size display, and visual settings. Result sorting now keeps semantic group sections fixed while sorting objects inside each group, the old distance column has been replaced with catalog angular size when available, and Settings > Sky Explorer now includes fill opacity, stroke opacity, and per-object-group hue controls that steer related object-type colors.

- Refined Sky Explorer result selection and grouped rows. Result group headers now span the table with centered labels such as `Galaxies (57 objects)`, group headers no longer consume row numbers, object rows are numbered sequentially, selected annotations use the current accent color, and selecting a result row now focuses the image on that object.

- Refined the Sky Explorer results table. Results are now grouped by the same semantic object families used in the Object Type styling table, the old `Layer` column has been removed, clicking an image annotation selects and highlights the matching result row, and double-clicking a result opens an appropriate external details page such as SIMBAD, VizieR, VSX, HyperLEDA, JPL SBDB, Gaia, or the NASA Exoplanet Archive.

- Added checkable group header rows to Sky Explorer's Object Type table. Each semantic group can now be toggled on or off at once, group rows visually separate the taxonomy families, and column sorting keeps the main group order stable while sorting object types inside each group.

- Grouped Sky Explorer's Object Type panel by semantic families such as nebulae, galaxies, AGN, clusters, stars, and solar-system objects. The table now includes a sortable `Group` column, and default annotation colors are generated within each group's palette so related object types look visually related while user color overrides remain respected.

- Added a dedicated `Sky Explorer` tab to `Settings`. The manual SIMBAD search radius now lives there instead of the generic Advanced tab, Sky Explorer search sources can be toggled on or off per catalog path, those source toggles now persist in settings, and Explore only runs the enabled query layers that still match the currently selected object types.

- Sky Explorer's object-type table can now be sorted by `Object Type`, `Description`, and the style columns as well, making it easier to navigate larger Advanced or Scientific type lists without losing the existing click-to-edit behavior.

- Refined Sky Explorer's object-type styling controls and manual SIMBAD search settings. The right-click image `Search` radius is now configurable from Advanced settings, object-type stroke and text colors no longer implicitly overwrite each other, the `Text` table cell now stays readable while summarizing the chosen font instead of resizing the row text itself, and per-type stroke/fill/text/font overrides now persist across reloads and restarts.

- Added a Sky Explorer right-click image search. Right-clicking a solved image now opens a small `Search` action that sends the clicked WCS coordinates to SIMBAD's coordinate search, making it easier to manually inspect unmarked sources.

- Added a Sky Explorer image hover coordinate overlay. When the displayed image has a solved WCS, moving the cursor over the image now shows the corresponding RA/Dec in a compact upper-right overlay; the overlay stays hidden for images without a usable WCS.

- Broadened Sky Explorer's normal HyperLEDA/PGC galaxy supplement from NGC/IC-only aliases to named galaxy aliases such as MCG. Visible galaxies like `MCG-01-13-049` can now be included with centered coordinates and size/position-angle metadata even when they are not NGC or IC objects, while anonymous PGC-only rows remain reserved for the dense `Scientific all` path.

- Corrected Sky Explorer's source priority for NGC/IC galaxies so sparse NGC2000 fallback rows no longer win over richer SIMBAD or HyperLEDA/PGC data. Named HyperLEDA galaxies are now used as a regular deep-sky quality supplement, compact aliases such as `IC402` are normalized to `IC 402`, and NGC2000 is kept as the fallback source when better typed, better centered, size-aware galaxy metadata is unavailable.

- Fixed Sky Explorer IC-object normalization from the NGC2000 supplement. Catalog rows such as `I402` and `I2118` now display as `IC 402` and `IC 2118`, IC-designation duplicates from NGC2000 are suppressed when SIMBAD already returned the same object with better metadata, and NGC2000 `NB` rows now classify as `Nebula` instead of falling through to `Other Deep-Sky` colors.

- Sky Explorer galaxy overlays can now use true ellipse markers when the catalog provides major axis, minor axis, and position angle metadata. SIMBAD galaxy dimensions now flow through as structured axis metadata, HyperLEDA/PGC supplement rows carry the same geometry, and the image overlay path projects those axes through the solved WCS so galaxy markers better match real apparent shape and orientation.

- Added a denser HyperLEDA/PGC galaxy supplement for the `Scientific` Sky Explorer mode when every scientific object type is selected. This supplements the existing SIMBAD plus NGC2000 deep-sky path with many more resolved galaxies while deduplicating against existing deep-sky hits by name and sky position so the `Scientific all` view annotates more visible galaxies without stacking duplicate markers on the same target.

- Fixed Sky Explorer relaunch layout persistence so the left panel heights and cached table widths are now restored on initial startup instead of only after a later settings/root reload. This also stops the results-table construction path from overwriting saved Sky Explorer column widths with default startup values before the real settings snapshot is applied.

- Astrometry.net solve caching no longer replays stale `UNSOLVED` results. Sky Explorer now ignores old cached failure records, re-runs the solve, and only reuses cached solved entries whose solved FITS still validates. This prevents old pre-fix WCS parse failures from resurfacing forever after the 2D celestial-slice WCS fix.

- Single-image raster opens now infer a recommended initial stretch mode from the file type and sampled pixel distribution. Obvious already-stretched JPG/PNG-style images now default to `None/Linear` when opened in Sky Explorer, Asteroid/Comet, or HR instead of inheriting the prior astronomy-style stretch, while linear science-style images still default to `Asinh`.

- Fixed Sky Explorer astrometry fallback for solved FITS headers that carry a 3D core WCS together with SIP/distortion metadata. The shared WCS path now forces a 2D celestial slice for validation and footprint building, which avoids the Astropy/WCSLIB `use the naxis kwarg` failure on otherwise usable solved fields.

- Astrometry fallback now keeps JPEG and PNG source images in their original compressed form for upload instead of first inflating them into temporary FITS files, which reduces upload size substantially for large Sky Explorer raster images and avoids one common upload-failure path.

- Reworked the new single-image loading dialog so it no longer blocks on preview generation in the GUI thread. The dialog now waits on the existing image-preload worker, which keeps the window responsive while Sky Explorer, Asteroid/Comet, and HR warm a first preview cache entry.

- Hardened the shared astrometry.net HTTP client against transient remote disconnects by retrying login, upload, submission polling, job polling, and solved-FITS download requests with short backoff delays instead of failing immediately on the first dropped connection; retries now also reset the underlying HTTP session between attempts, and uploads force a fresh connection instead of reusing a potentially broken TLS state.

- Removed the last Sky Explorer FITS/XISF-only gate so Explore now accepts the same TIFF, PNG, and JPEG source images that the shared Open flow already supports, allowing raster fields to continue into the normal WCS check and astrometry.net fallback path instead of failing immediately on file suffix alone.

- Added a modal indeterminate loading dialog when opening a single source image into Sky Explorer, Asteroid/Comet, or HR Diagram so the UI shows explicit progress while the first preview is being prepared instead of looking frozen during a cache miss.

- Refined single-image raster preview rendering so 16-bit TIFF, PNG, and JPEG inputs no longer force the astronomy-style sigma-clipped auto window when the image display is set to `Linear` or `None`; integer RGB rasters now keep a native linear preview path that matches external editors more closely while the astronomy auto-stretch behavior remains available for the non-linear preview modes.

- Expanded `Open` image support beyond FITS/XISF so the shared image I/O layer now accepts TIFF, PNG, and JPEG files, the main Sky Explorer or Asteroid/Comet or HR open dialogs advertise those formats directly, and HR field resolution no longer rejects non-FITS inputs before attempting its normal WCS or astrometry fallback path.

- Refined the Sky Explorer label-style controls again: the temporary separate `Font Color` column has been folded back into a single `Text` column, that cell now shows the chosen label color as its background while listing the current font-family and style details in the cell text, and clicking it opens a combined text-style editor that uses Qt's local font database instead of the basic native font picker so the app can offer the full set of fonts visible to the current PC installation.

- Extended the Sky Explorer object-type table again with a dedicated `Font Color` column plus a `Text` column between the color controls and `Object Type`. Each object type can now keep its own label color and full text style settings, and clicking the `Text` cell opens a font picker so family, size, bold, italic, underline, and similar per-type label settings flow through to the on-image Sky Explorer annotations.

- Refined Sky Explorer interaction and overlay semantics again: `Center Object` is now a real on/off toggle that keeps the image centered on the currently selected sky object, the header adds a `Select All` or `Unselect All` object-type toggle beside the `Simple/Advanced/Scientific` selector, stroke and fill colors now map to the actual drawn marker stroke and interior fill instead of treating `Stroke Color` as a label-only color, and deep-sky overlays now grow from catalog angular-size metadata when that information is available so large galaxies and nebulae are marked with circles that better match their real apparent extent.

- Fixed the follow-up Sky Explorer annotation failure for galaxy-rich fields such as the SN2025rbs or NGC 7331 frame. Those galaxies were being returned from the NGC2000 supplement with the `GX` type code, but that code was not mapped back into the simple `Galaxy` object-type filter, so the default Simple-mode overlay pass could show `Deep Sky: 21` in the log while drawing nothing on the image. Sky Explorer now classifies `GX` entries as galaxies, so the default `Galaxy` selection again produces visible markers and labels for returned NGC/IC galaxies.

- Fixed a crowded-field Sky Explorer deep-sky lookup failure that could leave galaxy-rich images such as the SN2025rbs or NGC 7331 field apparently empty. The SIMBAD path no longer relies on one small mixed-object row budget for both deep-sky and general-object layers, so deep-sky lookups now get their own larger pass and are much less likely to be starved by unrelated stellar rows before galaxies and other non-stellar objects can be marked.

- Added Sky Explorer layout-state persistence so the main splitter, left-side splitter, results/work-log splitter, and both Sky Explorer table column-width sets are now cached in settings and restored on reload. This pass also removes the remaining `Sky Objects` header text and fixes the initial `Stretch` control alignment by forcing a post-layout sync between the Sky Explorer header row and the main image splitter.

- Tightened the latest Sky Explorer control pass so the `Open/Explore` and `Simple/Advanced/Scientific` header buttons now keep fixed widths instead of shifting as their labels change, the `Stroke Color` and `Fill Color` columns moved to the left side of the Object Type table, those color cells are now clickable and open a color picker, and the Object Type table columns now use adjustable interactive widths.

- Reworked the Sky Explorer top chrome again so the old `Sky Explorer Image View` title text is gone, the image display controls now sit in the top header row aligned with the image pane, the row adds an `Export` button to save the annotated Sky Explorer image view, and the Object Type table now includes `Stroke Color` and `Fill Color` columns that match the on-image annotation colors for each object type.

- Moved the Sky Explorer mode-cycle button into the main header row beside `Open/Explore`, dropped the redundant `Mode` label, restyled the primary action as a filled accent button and the mode selector as an accent-outlined button, and brought the Sky Explorer progress bar onto the same shared themed appearance used by the other major modes such as HR Diagram.

- Refined the Sky Explorer left column again so the Object Type area and Sky Objects area now share a draggable vertical splitter, the Sky Objects section no longer uses the old group-box frame, and the visible status/tooltip strip under the Explore button has been removed while status text still updates internally for workflow state and tests.

- Polished the Sky Explorer workspace chrome so the redundant top image strip is gone, the left object-selection area and right image area no longer use the heavy outer group-box frames, the mode selector is now a single accent-colored cycling button (`Simple -> Advanced -> Scientific`), and the image stretch control now exposes a `None` option for an un-stretched view.

- Split Sky Explorer object selection into three real list modes instead of one shared table with row hiding. The selector is now `Simple`, `Advanced`, and `Scientific`: `Advanced` restores the earlier broader human-readable category list, `Scientific` exposes the larger exact SIMBAD-style condensed-code list, and switching between them now rebuilds the table so stale rows from the previous mode do not remain at the top.

- Reworked Sky Explorer `Advanced` object selection again so it no longer stops at a small hand-curated subset of broad labels. The advanced table now exposes a much larger SIMBAD-style condensed-code taxonomy with descriptions, including many of the detailed entries used by WhatsInMyImage such as `Y*O`, `Y*?`, `TT*`, `TT?`, `WR*`, `WR?`, `WD*`, `WD?`, `V*`, `V*?`, `WV*`, `WV?`, `SX*`, `XB*`, `XB?`, `Sy*`, `Sy?`, `SyG`, `Sy1`, `Sy2`, `SNR`, `SR?`, `ULX`, `UX?`, `UV`, `St*`, and many more. Sky Explorer now matches returned objects against both the broad simple buckets and these exact SIMBAD-style codes, so detailed advanced selections can drive the overlay and table directly.

- Expanded Sky Explorer `Advanced` object selection toward the broader SIMBAD taxonomy used by tools like WhatsInMyImage. The advanced list now includes additional SIMBAD-style categories such as HII Region, Supernova Remnant, Molecular Cloud, Star-Forming Region, Galaxy Pair, Galaxy Group, Galaxy Cluster, Quasar, AGN, Seyfert Galaxy, Blazar, Radio/Infrared/Ultraviolet/X-Ray/Gamma-Ray sources, Gravitational Lens, Binary or Multiple Star, Young Stellar Object, Emission-Line Star, White Dwarf, and Brown Dwarf. Those selections now map back into the correct Sky Explorer query layers and object overlays.

- Reworked the Sky Explorer query UI around object-type selection instead of the earlier coarse layer row. The left-side panel now has a `Simple` default that shows only Emission Nebula, Reflection Nebula, Dark Nebula, Galaxy, Open Cluster, and Globular Cluster, plus an `Advanced` mode that exposes the full supported object-type list with descriptions. Those object-type selections now drive both the Explore query scope and the objects marked in the Sky Explorer image and results table.

- Fixed a fourth Sky Explorer deep-sky regression in crowded cluster fields such as M46. The original SIMBAD region pass could be star-dominated and starved by row limits, while cluster-member aliases like `NGC 2437 118` were not the actual cluster object the user expects to see. Sky Explorer now supplements `Deep Sky` results with the NGC2000 catalog for NGC/IC objects in the solved footprint, which restores labels such as `NGC 2437` and `NGC 2438` in fields where SIMBAD alone does not surface the aggregate deep-sky entry cleanly.

- Fixed a third Sky Explorer regression in the SIMBAD deep-sky path: region queries commonly return object coordinates in the standard sexagesimal `RA` and `DEC` columns, but the first implementation only accepted `RA_d` and `DEC_d` degree fields. Sky Explorer now parses both formats, which restores overlay and table entries for deep-sky targets such as M46 and nearby planetary nebulae when SIMBAD returns the usual row shape.

- Fixed a second Sky Explorer solar-system overlay regression where the adapter that converts `SolarSystemSearchResult` entries into Sky Explorer markers still expected old `ra_deg` or `dec_deg` style fields instead of `SolarSystemDetection`'s `predicted_ra_deg` and `predicted_dec_deg`. The mode now maps predicted coordinates and predicted motion metadata correctly for asteroid/comet overlays.

- Fixed a Sky Explorer solar-system lookup regression where the new mode tried to read a non-existent `observatory_code` field from `AppSettings`. Sky Explorer now avoids reusing the science-export `Observer Code` setting for SkyBoT/MPC-style queries, so the mode no longer crashes or sends the wrong code when only the AAVSO observer metadata is configured.

- Added a fifth top-level `Sky Explorer` mode for single-image field exploration. The new workflow opens one FITS/XISF image, validates embedded WCS when present, falls back to astrometry.net solving when needed, computes the image footprint from the solved center and corners, and then queries multiple object layers for the exact field coverage.

- Sky Explorer now combines named deep-sky lookups from SIMBAD, field-star and variable-star layers from the existing catalog service, and known solar-system objects from the existing sky-search pipeline into one overlay-ready result set with a filterable layer panel, table, work log, inspector summary, and annotated image controls aimed at educational image review.

- Wired `Sky Explorer` through the main mode menu, mode-aware `File > Open File` routing, window-title updates, and the shared stacked-mode UI so it behaves like the other dedicated workflows, and added focused main-window tests that cover mode reachability, open-file dispatch, and result-table population.

## 2026-04-27
- Followed up the latest Transient Finder and worker-pool pass so the visible picker row now reports the current `Frame` instead of the root folder, candidate review starts background full-image preloading to reduce first-switch latency between detections, and WCS alignment now honors the shared analysis-worker setting in both the asteroid alignment flow and optional calibration alignment while still capping active workers to the amount of real frame work available.

- Refined the new calibration workflow so the dialog now remembers the Bias/Dark/Flat source paths, reuses cached `master_bias.fits`, `master_dark.fits`, and `master_flat_normalized.fits` from the chosen output folder when they already exist, writes newly built masters directly into that output folder, removes the extra empty dialog footer space, and styles `Run Calibration` with the active accent color. Calibration now also parallelizes independent frame reads and per-frame science calibration across the shared worker pool instead of running each science frame strictly one-by-one inside a single worker thread.

- Added Transient Finder review refinements: Blink now shows a preparing dialog before playback, has the same cadence selector used by Asteroid/Comet mode, records timestamped Work Log entries, and the search workflow exposes an ROI border margin that excludes edge/corner artifacts while drawing the retained region on the image panel. Adjust Levels preview updates now preserve the current pan/zoom after the first draw instead of recentering while sliders move.

- Reworked Transient Finder candidate screening so Gaia matches are no longer discarded before review; detections are grouped by sky position, measured across every solved frame with local aperture/peak evidence, and retained only when they show significant frame-to-frame variability. This suppresses static faint stars and seeing-sharpening artifacts in real two-frame fields while recovering the SN2025rbs location in the aligned NGC 7331 example.

- Replaced the top-level `Analysis` menu with a direct `Calibrate` action that opens a staged image-calibration pipeline dialog. The new workflow separates science inputs, bias/dark/flat masters, and optional post-calibration WCS alignment, writes calibrated FITS outputs plus master calibration products, and keeps the old catalog-cache actions under `Settings -> Catalog Cache`.

- Reworked the Transient Finder UI so its primary action now matches the HR-style `Open` then `Search` flow, candidate results sit above the usual bottom-left Work Log and Inspector tabs without the extra review frame or folder footnote, and a new right-side image panel provides stretch, levels, invert, reset, center-candidate, blink, and blink-GIF export controls for selected transient candidates.

- Fixed the first Transient Finder real-field performance pass so wide images no longer query the full deep Gaia catalog once per frame: the search now builds one shared magnitude-limited Gaia veto catalog for the solved sequence, vectorizes source-to-Gaia matching, groups detections with an indexed sky-neighbor search, and caps crowded-field detections/results to keep the UI responsive on two-image transient checks.

- Added a fourth `Transient Finder` mode with a folder-based workflow that checks WCS, solves missing astrometry through astrometry.net when configured, saves solved outputs in the transient WCS cache, queries Gaia as a known-source veto, and lists repeated uncataloged point-source candidates with coordinates, SNR, nearest-Gaia separation, and per-frame detection details.

- Tightened HR catalog/designation name caching so a later `Generate` can reuse cached SIMBAD results when regenerated HR rows land within the same sky-position bucket, avoiding another background query for stars whose friendly names or no-match result were already cached.

- Updated HR Diagram controls so the startup `Open` action and cached `Update Diagram` state keep the accent color while active generation still clears it, and split `Settings > HR Diagram > Appearance` ROI colors into separate drag-preview and defined-selection pickers.

- Kept the HR Diagram progress bar reserved in the layout like Asteroid/Comet mode: it now resets to an idle empty bar when work finishes instead of hiding and shifting the HR workspace vertically, and the HR Generate button again clears its attention accent while generation is busy.

- Fixed the paired HR `Image + Plot...` export so high-DPI plot captures are normalized to their visible screenshot dimensions before compositing, keeping the saved PNG sized like the on-screen Source Image and HR Plot areas instead of mixing logical and backing-pixel dimensions.

- Replaced the HR datapoint popup's top-level tooltip window with an in-plot pyqtgraph scene overlay anchored to the selected star, so it pans smoothly with the HR plot and stays visible until the user explicitly clears the selection or clicks elsewhere.

- Anchored the HR datapoint popup to the selected plotted star instead of the click cursor, and wired plot range changes to reposition the popup so it slides with the datapoint while the user pans or drags the HR plot.

- Reworked the HR datapoint popup so returning to a previously clicked star opens a fresh owned tooltip-style label instead of relying on Qt's `QToolTip` reuse behavior, fixing the case where a datapoint only showed details on its first click.

- Fixed the current HR datapoint tooltip regression for newer click handling: selecting a plotted point now explicitly accepts the pyqtgraph click event so the same scene click cannot immediately fall through to the background-clear path and hide the popup before it becomes visible.

- Fixed another HR background name-search reuse hole: regenerated HR rows can now reuse a previously resolved catalog/designation lookup by sky position even if the row key changes, which prevents duplicate SIMBAD queries for the same star after a later regenerate or refresh rebuilds the table with a different source identifier.

- Fixed the remaining HR datapoint popup regression for repeated clicks on the same already-selected star: the selection-ring and common-motion highlight overlays were separate pyqtgraph scatter layers sitting above the real plotted points, so after the first click they could intercept later clicks without forwarding them. Those overlay layers are now mouse-transparent, which lets repeat clicks on the same highlighted datapoint reopen the popup reliably.

- Fixed the latest HR background name-search failure for real fields such as M46: SIMBAD `query_region` rows can return lowercase column names like `main_id`, `otype`, and `sp_type`, while the earlier parser only checked uppercase variants, so stars were being found but then misclassified as unresolved. The lookup now reads catalog rows case-insensitively, which restores friendly names such as `HD 61866` and related identifiers into the HR results, popup, and overlay refresh path.

- Followed up the new HR background name-search workflow so cached `Update Diagram` refreshes no longer reset an in-flight bright-target lookup, the lookup now honors the saved shared worker-count setting instead of staying effectively fixed at the earlier cap, and the SIMBAD detail query now retries at wider radii and prefers friendlier non-Gaia identifiers from `IDS` when available so known stars are more likely to pick up recognisable catalog/designation names.

- Replaced the purely lazy HR star-name lookup path with a settings-driven bright-target background search: `Settings > HR Diagram` and `Open Settings > HR Diagram` now both expose `Search for catalog/designation names` plus a Gaia G magnitude threshold that defaults to enabled at `12.0` mag, and after the HR image or plot finishes loading the app continues resolving those bright stars in the background while the top HR progress bar and left footer note report that follow-up work until the search completes.

- Fixed three follow-up HR Diagram regressions: cached SIMBAD `main_id` names now also refresh into the visible `Source Results` name column instead of only the popup or overlay paths, repeated plot clicks now force the tooltip popup to reopen cleanly for previously visited stars, and the `Image + Plot...` export now normalizes HiDPI plot captures so the saved composite no longer contains the large blank right or bottom margins seen on some displays.

- Updated HR Diagram generation feedback so a thin progress bar now appears across the top of the HR workspace, the left footer note temporarily shows live preparation messages while generation is running and then returns to its draft-ROI note, and the `Export` menu now includes `Image + Plot...` to save the current annotated source image and visible HR plot side by side as one PNG snapshot.

- Added an `HR Diagram -> Settings -> HR Diagram -> Color vectors by angle` option that colors Source Image motion vectors from a circular hue palette based on their on-image direction, making different proper-motion groups easier to distinguish without changing the existing highlight treatment for explicitly selected common-motion members.

- Fixed the latest HR Diagram footer pass so `No draft ROI pending.` and the current-diagram summary now render below the split image or results and plot area instead of above the `Generate` row or inside the plot pane, keeping the action row compact and preventing the plot area from being pushed upward by the footer text.

- Tightened that HR Diagram footer row again so both notes are forced into a fixed single-line bottom strip instead of expanding vertically as the text changes, which removes the extra blank footer area that had started growing under the image and plot.

- Removed routine HR preparation progress from the global bottom status bar so the HR footer now stays limited to the two pane notes, and added `Scale vector saturation by magnitude` under `Settings > HR Diagram` so motion-vector saturation tracks the smallest-to-largest proper-motion range in the current field without hiding the direction color cue.

- Removed the shared application status bar from HR mode entirely so the bottom of the HR view no longer shows a third full-width footer strip, and queued an extra footer-splitter sync after HR layout updates and generation completion so the plot-side footnote starts aligned with the plot pane instead of shifting into place only after a manual resize.

- Fixed `Ctrl` + left-click selection in the HR `Source Image` so it now picks the nearest measured star without recentering or resetting the current image zoom, keeping the user's current inspection view intact while still syncing the plot selection and popup details.

- Updated the selected-source HR naming surfaces to prefer cached SIMBAD `main_id` values when available, so the plot popup title, Source Image selection label, copied selected-source names, and selection status text can show a friendlier designation such as `HD 123` instead of only the Gaia-style source identifier while still keeping Gaia IDs as the canonical internal key.

## 2026-04-26
- Reworked the HR Diagram workflow and presentation for the latest UI pass: the live HR controls now favor the simplified `Generate`, `Export`, and `Find Cluster` flow, the plot title is edited directly from the plot, redundant helper text and footer noise were removed, and the HR settings split now matches the intended menu-versus-full-settings layout.

- Reworked the HR Diagram mode layout so `HR Plot` now stands alone on the right while the left side uses tabs for `Source Results` and `Source Image`, with the `Work Log` staying attached to the `Source Results` tab instead of appearing beside the image tab.

- Followed up the HR Diagram layout so the shared `Generate` or `Update Diagram` action row now stays above both left-side tabs with always-available `CDS`, `Simbad`, `Gaia`, and `VSX` links, `Source Image` opens as the first default tab, and the old live `Rows` control now persists as `Settings > Open Settings > HR Diagram > Table Row Limit`.

- Smoothed the HR Diagram split view by removing the right-side `HR Plot` group-box frame and title while keeping the splitter boundary and a small right-panel inset, so the plot controls read as a separate pane without the heavier outer stroke.

- Updated the HR `Source Image` controls so the main button now reads `Open` until an image is loaded, then returns to `Generate`; the toolbar now follows the asteroid/comet order with `Adjust Levels`, `Invert`, and `Reset Display` after `Stretch`, and `Source Image...` export moved into the top-right `Export` menu with its menu indicator visible again.

- Followed up the HR Diagram left pane again by replacing the `Source Image` and `Source Results` tabs with direct `Image` and `Results` switch buttons, removing the `HR Source Image` group-box stroke, moving the live ROI `Tool` and `Invert ROI` controls into `Settings > HR Diagram`, dropping the RGB display `Invert` option, and collapsing the image-side clear or display or view actions into one shared `Reset` button beside `Generate`, `Stretch`, `Adjust Levels`, and `Show Motion`.

- Refined that same HR action row so `Image` and `Results` now sit directly beside the fixed-width `Generate` button, the draft-ROI and current-diagram status notes moved to the bottoms of the left and right panes respectively, and `Ctrl` + left-click on the HR source image now selects the nearest measured star and opens its linked HR or catalog context even when `Show Motion` is turned off.

## 2026-04-25

- Moved the asteroid/comet Synthetic Track defaults out of the separate settings page and into `Asteroid/Comet -> Search -> Discovery Advanced -> Preparation`, so crop radius, combine mode, mixed-`All` behavior, and the advanced manual single-stack toggle now live beside the rest of the discovery workflow controls.

- Hardened the advanced asteroid/comet `Synthetic Track` path so enabling the manual single-stack option raises an explicit modal dialog before launch instead of appearing to no-op when the toolbar button is pressed.

- Persisted asteroid/comet `Discover Pipeline` run options across restarts, including alignment assumption, batch mode, minimum seed displacement, motion prior, retry mode, and the rest of the per-run pipeline choices that were previously forgotten after the app closed.

- Refined the synthetic-tracking preview `Overlays` menu so each marker and label entry is explicitly named for its target (`Predicted center`, `Auto peak`, `Measured peak`) and remains directly clickable instead of leaving the middle pair effectively inert.

- Extended synthetic-tracking motion summaries to show both `px/h` and `arcsec/h`, and added an `Advanced Synthetic Track` settings toggle that opens a manual one-stack parameter dialog for custom center and motion checks before showing the normal preview/export popup.

- Fixed asteroid/comet blink slowdown and apparent frame skipping after marking potential discoveries on the image by caching per-source WCS for discovery-overlay projection, rendering marked overlays from the marked-candidate subset only, precomputing marked discovery positions for the loaded blink sequence before playback, reusing already-rendered frame previews when marks change, and pausing the repeating blink timer during each frame swap so slow repaints cannot advance from frame 14 to 16 before frame 15 is actually painted.

- Replaced the synthetic-tracking preview's coarse `Show Markers` and `Show Labels` checkboxes with a compact `Overlays` menu that independently controls predicted-center, auto-peak, and measured-peak markers and labels.

- Unified photometry processing, `Synthetic Track`, `Calculate Period`, and `Pull Period` onto one shared advanced worker setting, with backward-compatible migration from the older per-task worker values.

- Added multi-threaded per-frame preparation to `Synthetic Track` so the tracked stack can use the shared worker setting instead of always processing frames serially.

- Added direct asteroid/comet overlay visibility toggles under `Settings -> Asteroid/Comet` so main-image marker and name-label visibility can be switched independently, and updated the asteroid visual settings wording to match that split marker-vs-label behavior.

- Extended the synthetic-tracking preview window with `Show Markers`, `Show Labels`, and `Export Image...` controls so stacked-result inspection can be simplified or saved directly without leaving the preview.

- Removed the old 128 px synthetic-tracking crop-radius ceiling from settings storage and the settings dialog so larger object-centered stacks can be requested up to the practical image size instead of being blocked by the previous fixed cap.

- Finished the follow-up Synthetic Track improvements from the same workflow pass: preview exports now save the full-resolution rendered stack instead of the widget-sized viewport, the `Predicted Objects` table includes a `Direction` motion-angle hint, the full-frame threaded stacker uses the shared worker setting, optional `Known objects` markers can be shown for initial reference positions, progress now streams as frames finish, and the heavy full-frame path is capped to two workers to avoid exhausting memory.

- Updated asteroid/comet blink preparation to follow the shared logical-processor setting and made `Export Blink` warm the same rendered-frame cache before capture, so both playback and GIF export stay fast without depending on a prior manual blink run.

- Refined the asteroid/comet `3D View` session workflow from the same date range by caching reusable planet/context loads for wider spans, adding a direct reset back to the original observation time, and tightening the top-right control alignment so the playback/date controls stay visually anchored.

## 2026-04-24
- Fixed asteroid/comet `Discover` batched-result streaming so `Visible Limit`, `Known In Limit`, and `Recovered In Limit` no longer fall back to `Pending` on later residual windows or synthetic-sweep batches; intermediate snapshots now preserve the benchmark counts while continuing to append and refresh the current candidate set after each batch finishes.

- Extended asteroid/comet `Discover` candidate provenance through the review flow: recovered known objects, `Potential Discoveries`, `Borderline Review`, exports, and the main `Predicted Objects` table now label whether a candidate came from the residual search or the synthetic sweep.

- Cleaned up the asteroid/comet `Discover Results` actions by accenting `Trajectory...`, shortening `Mark Selected on Main Image` down to `Mark Selected`, removing the redundant separate mark-all button, switching the potential-discovery table to multi-select so selecting every row covers the old mark-all behavior, and collapsing the CSV actions into a single `Export...` menu while keeping the distinct candidate-review export.

- Strengthened asteroid/comet discovery overlays and review markers so potential-discovery positions are easier to spot on both normal and inverted imagery: the review preview uses larger outlined markers with center dots, and the main-image selected or marked discovery crosses now draw thicker and brighter than before.

## 2026-04-23
- Refined asteroid/comet `Discover` to estimate first and stream meaningful review state immediately: the visible-limit estimate now shows up before residual searching begins, the results window opens with that estimate-stage context, and later residual or sweep batches keep updating the same review surface instead of waiting for only the final pass.

- Added a dedicated asteroid/comet `Discover Test` action beside `Discover` for residual-search debugging: it exports the prepared background model, per-frame residual images, and residual stack for the current solved group so residual misses can be inspected directly outside the normal review dialog.

- Made the asteroid/comet `Discover` residual seed threshold user-configurable per run by exposing `Min Seed Displacement` in the run dialog, allowing slower movers to be recovered without forcing the global default for every later search.

- Improved asteroid/comet `Discover` run and review wording so motion guidance is presented in a more directly comparable per-exposure form instead of only the earlier more abstract adaptive-motion phrasing, making the setup text easier to compare against the review-table motion values.

- Added a deferred asteroid/comet `Discover` `Continue` action to the results dialog for synthetic-sweep follow-up: when the user skips the sweep prompt, the same group can now be continued later from the review window instead of forcing an immediate all-or-nothing choice during the original run.

## 2026-04-22
- Trimmed the asteroid/comet `Plan Object` dialog down to the requested minimal inputs: it now keeps only object name plus start and end dates with a single search action, removes the planner-specific asteroid/comet type and predicted-magnitude controls entirely, and normalizes both chosen dates to `00:00:00 UTC` internally.

- Rolled the asteroid/comet `Plan Object` dialog back to the original name-plus-date workflow: the blank `Object` full-sky bright-search mode has been removed again, the planner now requires an explicit asteroid/comet name or designation before searching, and the planner-specific type and predicted-magnitude controls are no longer part of this dialog.

- Simplified the asteroid/comet `3D View` again by hiding the sampled observation markers by default across the main 3D scene, the heliocentric top-down panel, and the right-side distance plots, and adding a new `Show Sample Points` toggle under the gear-menu display settings so users can bring those markers back only when they actually want that extra timeline detail.

- Hardened the asteroid/comet `Plan Object` search against the follow-up `C/2025` planner misses: SBDB candidate collection now merges both the exact and wildcard family queries instead of stopping on the first partial match set, and Horizons-based planner predictions now retry transient lookup failures once without caching those misses as permanent `None` results for the rest of the app session.

- Added direct date seeking from the asteroid/comet `3D View` right-side plots: left-clicking the distance or literature-magnitude time-series now jumps the shared playback state to the clicked UTC position, so the slider, table, 3D markers, and plot playback markers all sync to that selected date without manual timeline scrubbing.

- Fixed a comet-magnitude inconsistency between the `Plan Object` search rows and the right-side literature-magnitude plot in the asteroid/comet `3D View`: the plot-series builder now prefers Horizons `Tmag` before `Nmag`, matching the planner search path, so bright comets such as `PANSTARRS (C/2025 R3)` no longer appear near nuclear magnitude on the plot while showing total magnitude in the search results.

- Expanded the asteroid/comet `Plan Object` workflow with a bright-object mode and streamed results: leaving the Object field blank now scans the sky at the selected Start UTC for the brightest asteroid/comet planning candidates up to a user-defined predicted-magnitude threshold, the planner dialog now includes asteroid/comet type filters plus that threshold control, matching rows begin appearing in the table while the search is still running instead of only after the full search finishes, and those streamed table refreshes are now batched so large bright-object scans do not rebuild the full table on every single incoming row.

- Reduced the follow-up `Plan Object` search overhead by deferring synthetic planning-timeline construction until `Open Selected`: the search-results table now keeps lightweight detection-only entries while scanning, and the synthetic frame measurements for the chosen UTC window are generated only for the rows the user actually opens in the heliocentric `3D View`.

- Unified the asteroid/comet planner search threading with the shared settings knob: the global-name and bright-object `Plan Object` backends now use the existing asteroid-search parallel-worker setting instead of separate hardcoded worker caps, so the thread count for SkyBoT/Horizons planner lookups follows the same main-settings control already used by the rest of the asteroid search workflow.

- Reworked the blank-object `Plan Object` backend toward a local-first architecture: bright-object planning searches now consult a local SQLite snapshot database under the app cache before falling back to the expensive live all-sky SkyBoT scan, and live fallback results are persisted back into that database so repeated planning at nearby UTCs can reuse precomputed snapshots instead of rescanning all 188 sky tiles every time.

## 2026-04-21
- Extended asteroid/comet `3D View` with an `Objects > Add...` flow so the current heliocentric scene can bring in additional nearby known asteroids or comets that are not inside the image itself: the dialog now opens a searchable picker with name/designation search plus type and predicted-magnitude filters, the main window queries a wider nearby SkyBoT field around the current solved image when needed, and selected off-image targets are converted into frame-timed prediction tracks and merged back into the same shared Horizons view without reopening the window.

- Reworked that new asteroid/comet `3D View` add path after the first pass proved too blocking and unclear in practice: `Objects` now opens an explicit async `Nearby Search...` window where the user chooses the nearby-field radius and limiting magnitude before seeing a countable results table of addable objects, the 3D header now also includes a separate `Lookup` button for broader exact-name/designation lookups through JPL Horizons, and the main asteroid/comet detection header has been trimmed by removing the visible `Estimate` and `Recover Known` buttons from that primary workflow row.

- Broadened the asteroid/comet `3D View` `Lookup` flow so it no longer depends on one exact Horizons identifier match: the dialog now searches the JPL Small-Body Database by keyword, name, or designation, resolves the matching candidates into predicted objects for the current observation time, and presents them in the same selectable results table before anything is added to the shared 3D scene.

- Fixed the next lookup edge case after that broader search landed: global asteroid/comet matches are no longer discarded just because they cannot be projected into the current image footprint, and the 3D-view add path now synthesizes per-frame timeline measurements from the current group timestamps when image-based prediction rows are unavailable, so outside-field lookup matches can still be selected and loaded instead of vanishing or failing during add.

- Added a separate asteroid/comet `Plan Object` workflow for future-observation planning: the main asteroid/comet results row now includes a global planner search that is independent of the current frame contents, the planner dialog searches SBDB/Horizons by name/keyword/designation over a user-chosen UTC time window, and the selected object(s) open in the heliocentric 3D viewer using synthetic timeline samples for that planning window instead of requiring existing image-frame measurements.

- Fixed an immediate planner-search regression caused by non-Horizons observer codes in Settings: small-body lookups now retry JPL Horizons with geocentric `500` when the configured observatory code returns no ephemeris, so planner searches and object lookups still return matches for sites whose local observer code is not accepted as a Horizons location.

- Tightened the new asteroid/comet `Plan Object` search flow after follow-up debugging: SBDB candidate lookup now retries prefix-style designation searches with a trailing wildcard so partial comet searches such as `C/2025` return the expected family of matches, and the planner dialog no longer leaves its action buttons as implicit default buttons, so pressing `Enter` in the object, UTC, or sample-count inputs stays bound to search instead of accidentally acting like a dialog accept path.

- Refined asteroid/comet `Plan Object` search again after broader comet-family testing: planner searches now expand the SBDB candidate pool beyond the final displayed row cap, prefer one identifier per matched object instead of burning duplicate Horizons lookups on both the display name and designation, classify small bodies from designation text embedded in Horizons target names so comet families such as `C/2025` no longer appear as asteroids, and resolve candidate ephemerides concurrently so large family searches return faster while still surfacing later matches such as `C/2025 R3 (PANSTARRS)`.

- Simplified the asteroid/comet `Plan Object` dialog by removing the user-facing `Samples` control entirely: the planner now derives its synthetic observation timestamps automatically from the selected UTC window using a capped day-scale cadence, so the 3D planning view still opens with a sensible timeline table and observed markers without exposing that internal sampling detail in the search UI.

- Fixed the follow-up `Plan Object` comet-family lookup miss where some SBDB search rows were being reduced to bare designations such as `2025 R3` before the Horizons lookup step: the candidate normalizer now preserves a comet-safe designation such as `C/2025 R3` from the matched SBDB name text when the raw designation omits the comet prefix, so searches like `C/2025` keep showing `C/2025 R3 (PANSTARRS)` instead of dropping it again.


## 2026-04-17
- Corrected the remaining stale asteroid/comet `Discovery Advanced` synthetic-sweep wording after batch support landed: the settings checkbox, explanatory note, and hover-help now describe the sweep as running on each discovery search window rather than only on the detailed whole-group stage, and the discovery methods summary now reports batched sweeps as having actually run instead of only being configured.

- Tightened the new batched asteroid/comet `Discover` synthetic-sweep path after its first regression pass: discovery now raises a clear error if residual-detection output ever stops matching the current search-window frame count instead of crashing later during candidate linking, the batched synthetic-sweep regression now mocks residual buckets per batch correctly, and progress text now reports the sweep against the current discovery search window rather than incorrectly calling every run a whole-group stage.

- Enabled the optional asteroid/comet `Discover` synthetic sweep inside overlapping batched search windows as well as whole-group runs: each batch can now run its own configured velocity-grid pass after normal residual linking, batched sweep FITS output is isolated under per-batch `synthetic_track\batch_##` folders to avoid collisions, and the candidate aggregation path still remaps and deduplicates batch results before known-object benchmarking and Borderline Review splitting.

- Fixed asteroid/comet `Discover` reopen state for the same frame group after closing the results window: the stored restore context now keys off the stable ordered frame-path list instead of volatile file mtime or size metadata, and the restore prompt path now imports the missing `cast` helper so clicking `Discover` again can actually show the restore-versus-new-scan popup reliably.

- Added a reusable asteroid/comet `Discover` benchmark harness for real datasets: `scripts/profile_discover_run.py` scans a solved frame folder, reuses the persisted app settings, runs the same Generate, visible-limit estimate, and Discover core functions as the desktop workflow, and prints both a timestamped progress timeline and top cumulative `cProfile` hot spots for the Discover stage.

- Accelerated the corrected asteroid/comet `Discover` synthetic sweep without reverting to the older rounded stack approximation: the final velocity-grid pass now reuses cached interpolation grids for its subpixel bilinear shifts and can evaluate sweep vectors across multiple CPU worker threads using the existing `Settings > Open Settings > Asteroid/Comet > Discovery Advanced > Search Workers` limit.

## 2026-04-16
- Improved asteroid/comet `Discover` synthetic sweeps at the algorithmic path the misses were coming from: the blind velocity-grid stacker now uses subpixel bilinear shifts instead of whole-pixel rounding, `Settings > Open Settings > Asteroid/Comet > Discovery Advanced` now persists an explicit main-belt focus-width control in addition to the existing direction mode, and saved `synthetic_track` FITS stack filenames and headers now record that focus width alongside the tested speed, angle, focus mode, and combine mode.

- Extended asteroid/comet synthetic tracking and Discover synthetic sweeps for motion diagnostics: manual `Synthetic Track` previews now show the stack motion rate and stacking angle, `Settings > Open Settings > Asteroid/Comet > Discovery Advanced` now persists a `Main-belt direction focus` sweep mode plus optional `synthetic_track` FITS stack saving, and the final Discover sweep narrows itself to the image-plane direction of known main-belt asteroids when that can be inferred while still falling back to a full 360-degree pass when it cannot.

- Fixed asteroid/comet `Discover` reopening after a completed run while preserving the intended rerun choice: clicking `Discover` again for the same current group now reliably asks whether to `Restore Last Result`, `Run New Scan`, or cancel, and the stored review window itself uses the safer rebuilt dialog lifecycle with delete-on-close cleanup instead of depending on older hidden modal state.

- Compacted `Settings > Open Settings > Asteroid/Comet` so the asteroid/comet workflow no longer lives in one overly tall page: the tab now opens with inner `Search`, `Visuals`, and `Export & Tracking` sections, the `Discovery Advanced` controls are grouped into smaller preparation, residual-detection, and tracklet or synthetic-sweep blocks, and focused settings-dialog regressions cover the new structure while preserving the existing hover-help rows.

- Fixed another asteroid/comet `Discover` reopen edge case after closing the review window: the main window now drops invalid cached Qt dialog wrappers before trying to restore the stored Discover result, and clearing stored Discover state also disposes the cached dialog reference so clicking `Discover` again recreates the review window instead of landing on a stale hidden dialog that shows nothing.

- Added an optional final all-direction synthetic-track sweep to asteroid/comet `Discover` for the detailed whole-group stage: `Settings > Open Settings > Asteroid/Comet > Discovery Advanced` now persists enable/max-motion/motion-step/angle-step/stacked-SNR controls for that last-stage velocity-grid pass, the detailed search path can use the existing synthetic-tracking combine mode to recover movers that did not survive ordinary residual linking, and the discovery summaries/tests/README now describe that heavier fallback stage explicitly.

- Fixed asteroid/comet `Discover` restore reliability after closing the review window: the main window now keeps the current Discover dialog instance alive with its stored result, so clicking `Discover` again for the same subgroup can reopen that same review window reliably instead of silently failing to show anything.

- Renamed the weaker asteroid/comet `Discover` review bucket from `Better Detection Possibility` to `Borderline Review` across the review dialog, summaries, export bucket naming, advanced-setting help text, tests, and README so it no longer reads as stronger than `Potential Discoveries`.

- Added hover help to the settings dialog's advanced controls so both the global `Advanced` tab and the asteroid/comet `Discovery Advanced` rows now show brief mouse-hover explanations on the label and input widget, including how higher versus lower numeric values change the behavior of settings such as `Review Deflection RMS`.

- Added a second-pass linear-motion screen to asteroid/comet `Discover` so residual tracklets are now split into stronger `Potential Discoveries`, a new review-only `Borderline Review` bucket, and fully suppressed higher-deflection junk detections. The new workflow persists separate deflection-RMS thresholds in `Settings > Open Settings > Asteroid/Comet > Discovery Advanced`, shows deflection RMS plus max deflection in the discovery and trajectory dialogs, overlays the fitted linear path in trajectory review, and keeps only the stronger potential-discovery bucket mirrored into the main `Predicted Objects` table.

- Fixed asteroid/comet `Discover` batch-window duplication where a known mover such as `3I/ATLAS` could be shown both under recovered known objects and again as an unmatched `Candidate ...` row: once a candidate tracklet matches a recovered known detection, any other batched duplicate tracklets that also match that same known object are now suppressed from the discovery-candidate list.

- Fixed the remaining asteroid/comet `Discover` candidate coordinate mismatch by projecting candidate sky coordinates back into each source frame before main-image marking, selection focus, and candidate synthetic tracking, reduced leaks of known movers such as `3I/ATLAS` into the unmatched candidate list by hardening known-object association with sky-coordinate and uncertainty-aware matching, and relabeled candidate-strength UI values from misleading `SNR` wording to residual-score wording.

- Fixed a serious asteroid/comet `Synthetic Track` bug for temporary `Candidate ...` discovery rows: candidate stacks now use the discovery-linked per-frame candidate positions directly across the current frame group, instead of falling back to the generic prediction/timestamp path that could lock onto the wrong nearby object or a stationary star and make different selections such as a discovery candidate and `3I/ATLAS` produce the same stack.

- Refined asteroid/comet blink playback again so each timer tick advances exactly one frame instead of catch-up skipping across multiple subframes, and restyled temporary `Candidate ...` trajectory windows onto the same darker scientific plot family used by the main known-object `Trajectory` view.

- Enabled asteroid/comet main-window `Trajectory` for temporary `Candidate ...` discovery rows in the `Predicted Objects` table, reusing the existing moving-tracklet dialog so candidates no longer need to be reopened through the `Discover Results` window just to inspect their motion plots.

- Reduced blink slowdowns that appeared after `Discover` added many candidate rows by skipping per-frame `Exposure Motion` table rewrites while blink playback is active, then refreshing that table normally again once playback stops.

- Fixed asteroid/comet `Synthetic Track` failures triggered from both the main window and the `Discover Results` dialog when the current frame was using prediction-only measurements: frame-measurement objects now always initialize `local_fwhm_px`, so synthetic tracking no longer crashes while the UI refreshes selection details.

- Tightened asteroid/comet blink playback timing by switching the blink timer to Qt's precise timer mode, keeping blink progression responsive after `Discover` adds candidate rows, and preserving one-frame-at-a-time advancement so the effective blink speed stays closer to the selected `0.05 s`, `0.10 s`, and slower review presets without visual frame skipping.

- Promoted asteroid/comet `Discover` candidates into the main `Predicted Objects` table as temporary `Candidate ...` rows, so unmatched movers remain selectable from the main window after the review dialog closes instead of disappearing from the primary results surface.

- Added temporary restore behavior for asteroid/comet `Discover`: the latest matching subgroup result is now kept in session state, and clicking `Discover` again offers `Restore Last Result` versus `Run New Scan` so accidental dialog closes do not force an immediate rescan.

- Extended asteroid/comet synthetic tracking to work from temporary discovery candidates as well as generated known objects, including a new `Synthetic Track...` action in the `Discover Results` dialog and main-window candidate selection support that still keeps `3D View` disabled for those temporary rows while `Trajectory` is now available.

- Strengthened asteroid/comet and Discover-result overlay visibility by adding contrast outlines to the drawn markers, added `Mark Selected on Main Image` and `Mark All on Main Image` actions to the `Discover Results` dialog, and introduced separate `Settings > Open Settings > Asteroid/Comet > Visuals` toggles for showing generated known-object overlays versus marked potential-discovery overlays on the main image.

- Split custom asteroid/comet overlay styling so `Theme > Customize...` now exposes one style block for the selected object and another for all other generated objects, letting the main image and preview dialog use different circle, line, and label styling for the active target versus the rest of the field.

- Hardened asteroid/comet `3D View` target availability so non-selected generated objects stay present in the `Objects` menu even when per-group measurement collection misses them, by falling back to the current frame's cached prediction measurement instead of dropping the target entirely.

## 2026-04-15

- Refined the asteroid/comet `Discover Results` dialog so it now opens wider by default, gives the table and image panels more room immediately, replaces the old multi-line prose block with a compact summary strip for visible limit and recovery/discovery counts, and trims the generated report text to remove the long predicted-magnitude list.

- Moved the remaining benchmark CSV actions into the asteroid/comet `Discover` review dialog so the unified recovered-known, missed-known, and potential-discovery workflow now offers `Export Benchmark...`, `Export Unmatched...`, and `Export Summary...` directly alongside the existing trajectory inspection tools.

- Persisted the asteroid/comet splitter widths for both the image-versus-results layout and the `Predicted Objects` versus lower `Work Log` / `Inspector` layout, so custom panel widths now restore across app restarts instead of snapping back to the hard-coded defaults every time.

- Unified asteroid/comet `Discover` with the known-object recovery benchmark so Discover now runs the Gaia-based visible-limit estimate first, keeps catalog-known movers in the residual search instead of discarding them, separates recovered known objects and missed known objects from unmatched tracklets in the review dialog, and reports how many generated known objects fell within the estimated magnitude limit versus how many of those were actually recovered before listing potential discoveries.

- Reworked asteroid/comet `Discover` residual detection so it no longer depends only on the fixed `DAOStarFinder` point-source pass: `Discovery Advanced` now persists configurable minimum linked frames, point-detection sigma/FWHM, per-frame residual caps, edge margin, and detector mode, while the residual scanner can run point-only, streak-only, or hybrid point-plus-streak detection with connected-component streak filtering by minimum area and elongation.

- Added asteroid/comet selected-object local-match FWHM to the right-side inspector so `Current Frame Match` now shows a measured FWHM value alongside offset, SNR, flux, and trail length for the active prediction when a local match is available.

- Fixed the asteroid/comet `3D View` right-side top-down, distance, and literature-magnitude plot color mapping so comparison targets now use their own asteroid-versus-comet style consistently instead of occasionally inheriting the next target's color from an off-by-one comparison-track lookup.

- Added a per-run asteroid/comet `Discover` pipeline popup so each search can choose alignment mode, fast versus detailed scan behavior, optional automatic detailed retry after an empty fast pass, working-image binning, temporary prepared-frame cache usage, residual local-SNR thresholds, and overlapping frames-per-batch windows without forcing users back through the full settings dialog first.

- Added crash-safe asteroid/comet `Discover` temporary working-cache handling: prepared binned/aligned run files now live only in an auto-cleaned temp area during the search, are removed after the run finishes, and stale leftover Discover temp folders are purged again on the next app startup after an unexpected shutdown.

- Added asteroid/comet `Discover` advanced controls under `Settings > Open Settings > Asteroid/Comet > Discovery Advanced`, including residual local-SNR minimum and maximum thresholds plus an optional frames-per-batch window size, so long subgroups can be searched in overlapping batches and weak or overly strong residuals can be filtered before tracklet linking.

- Threaded those asteroid/comet `Discover` controls through the main window, worker, persisted settings, and residual-search core, and expanded the focused discovery/settings tests to cover residual SNR filtering, batched search windows, and settings round-trips.

- Clarified asteroid/comet `Discover` progress and Work Log messaging so the UI reports the active alignment/search phase and any enabled batch-window or residual-SNR options while the search is running.

## 2026-04-14

- Refined asteroid/comet `3D View` playback chrome again by removing the visible dynamic hover text strip above the right-side plots, sorting the orbital-period overlay from shortest period to longest with open trajectories last, changing the top-right status readout to a fixed-width `Sample` counter instead of repeating UTC time, and giving the icon-only `Play` button stronger sizing plus accent styling so it stands out as the primary animation control.

- Refined asteroid/comet `3D View` again so the orbital-period legend no longer consumes header height; it now floats as an overlay inside the 3D scene panel, stays anchored to the top-left of that view during resizes and GL refreshes, and keeps the main header bar compact.

- Refined asteroid/comet `3D View` controls again by replacing the long inline `Periods` text with a compact legend-style orbital-period panel under the left controls, separating the animation controls with a shorter scrub bar, moving playback beside the speed selector, and switching `Play` plus `Settings` to icon-based buttons for a cleaner header layout.

- Restored the shared annotated-image display contract by adding black, midtone, and white level support back to `AnnotatedImageRenderSettings`, so Differential Photometry, HR Diagram, and Asteroid/Comet image previews can build render settings and display frames again.
- Updated Differential Photometry to keep the first `Source Results` row current after processing, so the annotated frame view initializes immediately instead of staying on the placeholder prompt until a manual selection is made.
- Fixed a restored-state regression in `CatalogService` where catalog query/cache helpers were accidentally nested outside the class body, which caused Differential Photometry preview/generate catalog lookups to fail with `'CatalogService' object has no attribute '_cache_key'`.
- Added focused catalog-service regression coverage so cache-key availability and basic field-catalog cache round-trips are exercised directly.
- Restored the Differential Photometry VizieR fallback path by retrying alternate solved-field centers for Gaia and VSX lookups before surfacing the concise catalog-service warning again, and added focused regressions for the retry and warning behavior.
- Restored the asteroid/comet `3D View` pyqtgraph time-axis contract by switching the plotted data, hover lookup, and playback markers back to Unix-timestamp x-values so they once again line up with the custom UTC date axis.
- Restored the asteroid/comet `3D View` hover overlays so the small per-series popup includes the hovered `YYYY-MM-DD` value again, the date guide is anchored in the visible bottom region, and the guide state stays aligned with the rebuilt plots.
- Restored asteroid/comet `3D View` primary-target state syncing after dialog-side reloads so the window title, summary, labels, and per-type styling refresh from the active loaded target set instead of keeping stale comet-versus-asteroid state.
- Added a date-by-date recovery checklist at `docs/recovery_audit_2026-04-11_onward.md` that marks every `version.md` change from `2026-04-11` forward as implemented in the current workspace.
- Hardened the direct SkyBoT VOTable parser so asteroid/comet detection now accepts service responses that include leading whitespace before the XML declaration instead of failing with `SkyBoT query returned an unreadable response`, and added a focused regression for that response shape.
- Hardened the direct SkyBoT VOTable parser again so asteroid/comet detection also accepts responses with stray proxy/preamble bytes before the XML/VOTable document instead of surfacing the same unreadable-response error, and added a focused regression for that case.
- Fixed the asteroid/comet visible-limit workflow so `Estimate` no longer silently overwrites the saved detection magnitude limit before the user applies it; the estimated limit now stays pending until `Apply` or `Generate` is clicked, preventing one low estimate from unexpectedly shrinking future detection lists across sessions.
- Optimized asteroid/comet `3D View` playback by keeping the Matplotlib heliocentric top-down plot static during play and updating only lightweight playback markers and labels on each timer tick, which removes the old full top-down redraw from every frame and makes the simulation noticeably smoother.
- Replaced the asteroid/comet `3D View` heliocentric top-down panel with a native `pyqtgraph` plot, removing the dialog's remaining Matplotlib playback surface and toolbar from that view while keeping the same orbit context, hover readout, labels, and playback markers on a lighter-weight Qt plotting stack.
- Smoothed asteroid/comet `3D View` motion again by switching playback to a higher-frequency precise Qt timer and advancing simulation time from real elapsed wall time instead of coarse fixed 120 ms steps, so objects traverse the same selected speed more continuously instead of jumping between larger time chunks.
- Restored the asteroid/comet `Trajectory` window diagnostics by carrying measured RA/Dec through local frame measurements again, bringing back the darker 3D-style plot theme, endpoint-only labels, sky residual overlays, match-offset error bars, and balanced image-plane limits.
- Changed the Windows/Qt app bootstrap to register `Citizen Astronomy` as the application name instead of the separate `Citizen Astronomy (CAst)` display caption, so the shell no longer appends a second program name after the mode title.
- Reworked asteroid/comet `3D View` playback from observed-frame stepping into a time-window simulation driven by Horizons path samples, with manual UTC date entry, time-based speed presets, and playback that moves planets plus selected asteroids/comets through past and future positions instead of only cycling observed frames.

## 2026-04-13



- Updated Differential Photometry `Discover` so its reusable reference-measurement pass and per-candidate per-frame photometry now honor the shared `Settings > General > Photometry Workers` limit instead of always running serially.

- Added a Differential Photometry `Discover` workflow that reuses the solved field catalog, tests filtered non-catalog Gaia stars one by one, optimizes comparison-star groups for each candidate, retains only stronger ranked light-curve candidates, and surfaces those retained Gaia candidates back into `Source Results` with saved advanced Discover settings for candidate count, magnitude range, and minimum score.

- Refined the Differential Photometry action and image toolbars again by adding `Discover` beside `Find Better Fit` and `Increase SNR`, shrinking the `Aperture presets` control, and right-anchoring the aperture-editor controls so they are visually separated from the stretch and display controls.

- Refined Differential Photometry again by moving `Equatorial Grid`, `Mark Saturated`, the new saved `Frame Margin` visibility toggle, `Keep Comparison Stars`, and the manual recenter controls into `Settings > Light Curve`, reducing the visible `Annotated Image` toolbar to two rows, and replacing the old `Edit Apertures` checkbox with a dedicated checkable `Aperture Editor` mode button.

- Refined Differential Photometry again by removing the visible Annotated Image frame chooser, moving the aperture preset/editor actions inline after `Reset`, and collapsing that area down to one control row above the image while leaving frame access available from the results tables.



## 2026-04-12



- Refined Differential Photometry again by removing the visible `Sources` search/type strip from the main results chrome, replacing the `Source Results` `Catalog` column with `Type` while keeping catalog provenance in-table, splitting `Find Better Fit` and `Increase SNR` into accent-outlined right-side results actions, simplifying `Annotated Image` down to one `Reset` button that restores both view and display settings, and accenting the `Loaded Results` dialog's `Use Selected Object` button.

- Refined Differential Photometry again by moving the filter selector into the Workspace row ahead of `Generate`, removing the inline active-object readout, reordering the main results tabs to start on `Source Results`, collapsing the visible results chrome down to the ordered `Measure`, `Pull Period`, `Calculate Period`, `Find Better Fit`, and `Increase SNR` action row plus the remaining source filters, and switching the annotated-image frame-edge margin overlay to white.

- Fixed a preview-regression in the VizieR fallback work where the new catalog-error formatter accidentally split `CatalogService`, restoring its cache/query methods so alternate solved-field lookup and the friendly preview warning both work again.

- Softened Differential Photometry preview failures caused by unreachable VizieR catalog requests by retrying alternate solved-field centers when one field lookup fails, replacing the raw requests exception with a concise actionable warning, and covering both the fallback and warning paths with focused regressions.

- Tightened the Differential Photometry layout again by restoring the Workspace actions to one line, adding clearer hover and disabled states for the catalog buttons, grouping the entire left-side measurement/source/results area under one `Results` section, removing the redundant right-side `Analysis` wrapper to give `Annotated Image` and `Light Curve` more room, replacing the differential annotated-image brightness/contrast controls with the same apply-only `Adjust Levels` workflow used for asteroid/comet image review, and aligning the focused main-window regression coverage with the rebuilt one-line Workspace layout.

- Refined the Differential Photometry mode chrome by turning the workspace strip into a cleaner two-row header, adding a draggable split between results and the lower detail tabs, flattening the differential results/detail tabs into the same lighter treatment used elsewhere, and giving primary versus secondary differential actions clearer visual emphasis plus dim disabled states.

- Fixed launch-state polish by creating the top-level `assets` icon folder with icon-placement notes, removing the startup application-display-name duplication that made the title bar repeat the app name, and loading saved settings before the first mode sync so the last selected mode opens correctly and stays checked in the `Mode` menu.

- Renamed the application branding to `Citizen Astronomy (CAst)`, updated the main window to show the active mode in its title bar, reordered the `Mode` menu to put `Asteroid/Comet Detection` before `HR Diagram`, and added optional runtime plus PyInstaller icon lookup through a top-level `assets` folder.

- Optimized the asteroid/comet `Adjust Levels` dialog by rendering its interactive preview from a downsampled image payload and preserving the dialog viewport during handle drags, and removed the inline asteroid/comet status text from above `Predicted Objects` so transient messages stay out of the main layout.

- Reworked asteroid/comet level adjustment again by replacing the live inline levels control with an `Adjust Levels` button that opens a smaller preview dialog, lets black/midtone/white points be tuned there responsively, and only applies the chosen levels back to the main image after confirmation.

- Refined the asteroid/comet image controls again by giving `Blink` and `Track Object` a visible checked state, moving `Center Object` and `Track Object` into the image toolbar, replacing the freeform blink-duration spinbox with preset cadence choices, and swapping the asteroid/comet brightness/contrast widgets for a compact draggable histogram levels strip with black, midtone, and white handles.

- Refined the asteroid/comet UI again by making the secondary workflow buttons match `Generate` height, adding explicit dim disabled styling for unavailable actions, flattening the lower `Work Log` / `Inspector` tabs into a more 2D treatment, merging the old view and playback strips into a single image-control row, and changing the default blink cadence to `0.05 s`.

- Simplified the asteroid/comet main surface again by collapsing the top controls into a single compact action row, moving the saved detection magnitude limit into `Settings > Asteroid/Comet`, removing the redundant metadata and summary text lines, relocating `Hide Info` onto the image controls, and making the `Predicted Objects` table plus lower `Work Log`/`Inspector` area vertically resizable with `Work Log` shown first.

- Refined the asteroid/comet mode hierarchy further by replacing the heavier boxed `Prepare` and `Search` sections with lighter labeled clusters, giving the main `Generate` action stronger persistent emphasis, reordering the selected-object toolbar so `Trajectory` and `3D View` lead the advanced actions, and collapsing the left-side details plus work log into tabbed `Inspector` and `Work Log` panes under `Predicted Objects`.

- Reorganized the main asteroid/comet mode so the top area now separates workflow actions from contextual frame and magnitude controls, groups `Recover Known` and `Discover` under a dedicated search section, moves object-specific actions like `Trajectory` and `3D View` into a selection-aware bar above `Predicted Objects`, and splits the old overlay control strip into distinct image-view and playback rows.

- Extended the known-object `Trajectory` window so per-frame local centroid measurements now carry measured RA/Dec through the WCS, the sky-plane panel overlays measured centroids and predicted-to-measured residuals against the predicted path, and the image-plane panel uses balanced equal-span limits instead of collapsing into the narrow top-left strip seen on long motion tracks.

- Refreshed the known-object `Trajectory` window opened from asteroid/comet `Generate` with a darker scientific presentation, endpoint-only frame labels on the image-plane and sky-plane plots, predicted-to-measured residual connectors, measured-point match-offset error bars, and median guide lines for the offset and SNR time-series so the view is less cluttered and more diagnostically useful.

- Fixed stale asteroid/comet `3D View` primary styling after switching targets by making the dialog derive its primary summary/color state from the currently loaded target set, syncing that state after dialog-side reloads, deleting 3D dialogs on close, and only reusing an existing hidden dialog when it actually matches the same target request.

- Updated the asteroid/comet `3D View` pyqtgraph hover callouts so the small per-series popup beside a hovered point now includes the corresponding `YYYY-MM-DD` x-value along with the magnitude or distance y-value, instead of showing only the y-value.

- Moved the asteroid/comet `3D View` pyqtgraph hover date label into the visible bottom region of the time-series plots so the vertical guide now shows its corresponding `YYYY-MM-DD` value directly on the plot instead of losing that date near the top edge.

- Added an immediate asteroid/comet `3D View` loading message and forced the modal progress dialog to paint before the Horizons worker starts, so clicking `3D View` now gives instant feedback that the window is being prepared.

- Fixed the asteroid/comet `3D View` pyqtgraph time-series panels after the date-axis regression by restoring Unix-timestamp x-values for the plotted data, hover lookup, and playback markers so they once again match the custom UTC axis formatter and show the correct dates and hover values.



## 2026-04-11



- Changed asteroid/comet `3D View` startup back to loading only the currently selected target set from JPL Horizons instead of every generated target in the `Objects` menu, and restored the dialog's internal split between loaded targets and merely available targets so later add/remove actions only trigger a reload when a newly requested object was not already loaded.

- Fixed ambiguous periodic-comet `3D View` Horizons lookups such as `154P` by trying an exact comet designation form like `DES=154P;CAP` before the generic small-body identifiers, which avoids the multi-record ambiguity failure for targets like `154P/Brewington`.

- Refined the asteroid/comet multi-target `3D View` so the pyqtgraph distance and literature-magnitude panels now use calendar-formatted date axes without the old scaled `UTC (x1e+09)` text, keep the hovered x-value visible as an in-plot `YYYY-MM-DD` label, and keep playback markers aligned to that same date scale.

- Added separate asteroid and comet color pickers to the asteroid/comet multi-target `3D View` `Settings` popup and applied those per-type colors through the OpenGL orbit scene, labels, and primary time-series styling so comets remain visually distinct from asteroids.

- Changed asteroid/comet multi-target `3D View` object toggles to reuse the already loaded Horizons context for generated targets whenever possible, so adding or removing objects no longer resets the current 3D camera/view and instead just updates the visible overlays in place.

- Hardened the asteroid/comet multi-target `3D View` pyqtgraph time-series panels during object add/remove reloads by suppressing hover callbacks while the plots are being cleared and rebuilt, which avoids stale graphics-item access crashes when loading a new object after several visibility changes.

- Migrated the asteroid/comet multi-target `3D View` distance and literature-magnitude side plots from Matplotlib to pyqtgraph so the time-series interaction is markedly smoother while preserving the UTC hover guide, inline value callouts, linked date axis, and current-playback markers.

- Added an explicit hovered UTC x-value label to the asteroid/comet multi-target `3D View` time-series guide overlay so the literature-magnitude plot now shows the current x-position directly on the plot while the vertical cursor is active.

- Fixed the asteroid/comet multi-target `3D View` time-series hover guide so it no longer expands the date axis back toward the Matplotlib epoch, restoring the correct Horizons window instead of showing a misleading range that appeared to start near 1970.

- Refined the asteroid/comet multi-target `3D View` plot hover behavior so the distance and literature-magnitude plots now draw a live vertical cursor guide with intersection markers and inline value callouts at the hovered time, and widened the heliocentric top-down subplot so it fills the same right-column width as the other plots.

- Rebalanced the asteroid/comet multi-target `3D View` right column so the plots sit in a resizable splitter above a smaller default table area, and added interactive hover readouts on the Matplotlib plots so moving the cursor across the distance and literature-magnitude series reveals the corresponding UTC and value at that position.

- Added a third asteroid/comet multi-target `3D View` side-plot for literature magnitude over time using JPL Horizons ephemerides for the visible selected objects, including the current-playback marker on that magnitude series while keeping the view fail-soft if Horizons does not return usable magnitudes.

- Tightened asteroid/comet multi-target `3D View` text placement so labels stay closer to their bodies when objects are added or removed, focused the heliocentric top-down plot limits on the visible object trajectories instead of distant planet positions, hid out-of-window planet markers from that top-down view, and added a new `Side View` camera mode.

- Moved the asteroid/comet multi-target `3D View` label controls into a new `Settings` popup beside `Play`, replaced the inline object checkbox panel with an `Objects` dropdown that also lists the other generated targets from the current detection run so they can be added later inside the same window, and stopped overview/top-down playback from snapping the camera back to the default view on every frame advance.

- Finished the asteroid/comet multi-target `3D View` visibility controls by adding in-dialog label font/size/bold/italic customization plus per-target hide/show toggles, and fixed playback updates so hiding the primary selected object no longer stops Earth or the remaining visible selected targets from updating.

- Fixed the asteroid/comet multi-target `3D View` span reload path so changing presets such as `1y` or `5y` keeps every selected target in the shared Horizons scene instead of rebuilding the dialog with only the primary object, added direct Sun/planet/object labels inside the view, and standardized all selected-object orbit colors so labels rather than color changes carry the distinction.

- Added multi-target asteroid/comet `3D View` support so the `Predicted Objects` table can now use multi-row selection to load several known objects into the same heliocentric JPL Horizons scene at once, while the first selected target still drives the detailed table and playback readout and the additional selected targets are overlaid in the OpenGL and top-down orbit views.

- Fixed the asteroid/comet Generate-side `3D View` OpenGL blank-pane failure on Windows by clearing `pyqtgraph.opengl`'s cached shader-program handles whenever a fresh `GLViewWidget` is created, so a new dialog no longer tries to reuse invalid shader IDs from an older destroyed OpenGL context.

- Changed the asteroid/comet Generate-side `3D View` to reuse one existing dialog for different targets as well as same-target reopens, loading the new Horizons context into the current window instead of creating another fresh OpenGL dialog that could come back blank on Windows.

- Changed the asteroid/comet Generate-side `3D View` reopen path to reuse a matching hidden dialog instead of destroying and rebuilding a fresh OpenGL window every time, which avoids the repeated Windows second-open failure where the left 3D pane came back blank after a close-and-reopen cycle.

- Changed the asteroid/comet Generate-side `3D View` launch from true fullscreen to a maximized windowed view and deferred `GLViewWidget` creation until after the dialog is shown, which fixes the Windows case where closing and reopening the view could leave the left OpenGL pane blank.

- Changed the asteroid/comet Generate-side `3D View` to open maximized by default, moved the frame table into the right-hand scientific column so the OpenGL orbital pane stays wider, added optional orbital-period display for the currently shown bodies, and added an optional major-planet overlay mode that reuses the same span-aware JPL Horizons context loader.

- Fixed the asteroid/comet Generate-side `3D View` OpenGL pane so closing and reopening the window rebuilds the GPU scene reliably, added in-dialog span presets from the default local window out to `+/-90d`, `+/-180d`, `1y`, and `5y`, and faded the distant parts of the heliocentric trajectories so the observed segment stands out more clearly against the full context arc.

- Replaced the large left pane of the asteroid/comet Generate-side `3D View` with a GPU-backed `pyqtgraph.opengl` scene, added packaged OpenGL dependencies, and kept the right-side scientific plots intact while adding frame playback, speed presets, and camera modes for overview, top-down, and follow-style viewing.

- Fixed the asteroid/comet Generate-side `3D View` dialog crash so the new space-sim styling now imports its NumPy-based starfield helpers correctly, and hardened dialog creation so any future render-time exception falls back to the short logged failure message instead of a window that disappears immediately.

- Refreshed the asteroid/comet Generate-side `3D View` window with a darker space-sim presentation, including a starfield-style background, stronger object/Earth/Sun visual emphasis, darker scientific side plots, and themed tables so the view feels more like a lightweight simulation while keeping the quantitative plots intact.

- Shortened asteroid/comet `3D View` failure UX so detailed Horizons errors stay in the Work Log instead of being copied verbatim into the top status/header area, which prevents long unbroken HTTP error text from stretching the main window geometry.

- Fixed the asteroid/comet Generate-side `3D View` for long frame groups and interstellar objects such as `3I/ATLAS` by splitting JPL Horizons heliocentric vector requests into smaller chunks and querying the orbit arc separately from exact frame times, which avoids oversized `TLIST` URLs and keeps Horizons failures from dumping full request URLs into the UI.

- Added a Generate-side asteroid/comet `3D View` action that queries JPL Horizons heliocentric state vectors for the selected known object and Earth in a background worker, then opens a separate 3D orbital-context window with heliocentric path plots and per-frame distance samples around the current group.

- Extended asteroid/comet trajectory inspection so after `Generate` the main `Predicted Objects` table can now open a known object's subgroup trajectory directly through the new `Trajectory` button or a table-row double-click, using per-frame predicted plus measured positions across the current group instead of limiting trajectory plots to `Recover Known` or `Discover` dialogs.

- Added a separate `Trajectory...` window to the asteroid/comet `Discover` and `Recover Known` review dialogs so linked mover detections can now be inspected as image-plane paths, sky-coordinate paths, and time-series plots, while also capturing per-detection RA/Dec directly during residual search for those views.

- Added a separate asteroid/comet manuscript draft at `docs/paper/citizen_asteroid_comet_paper.tex`, covering the local-first solar-system workflow, known-object recovery benchmark design, residual moving-object discovery path, visible-limit interpretation, exports, and planned quantitative evaluation framing.

- Updated `docs/paper/build_pdf.py` and the paper README so alternate manuscript sources under `docs/paper/` can now be compiled directly with `--source`, including the new asteroid/comet paper draft.

- Added an `Advanced` `Recover/Discover Workers` setting for asteroid/comet mode and threaded the moving-object search so frame preparation and residual scanning can now use a user-selected worker count instead of a fixed single-threaded path.

- Refreshed ambiguous targeted interstellar catalog rows such as SkyBoT's generic `3I` result with the authoritative `3I/ATLAS` Horizons identity and magnitude when that targeted fallback is available, which removes the inconsistent name and unrealistically bright magnitude jump seen on the same dataset.

- Renamed the asteroid/comet object-brightness UI from `V_mag` to `Pred Mag` and clarified the `Estimate` / `Visible Limit` messaging so users can see that the limit is a Gaia G star heuristic while object rows may use different catalog passbands, especially for comets.

- Expanded the asteroid/comet `Recover Known` review dialog with `Export Unmatched...` so unmatched recovery candidates can now be saved directly as a CSV table with motion, fit, coordinate, and per-frame summary fields for appendix material or downstream vetting.

- Added `Export Summary...` to the asteroid/comet `Recover Known` dialog so recovered and missed likely visible catalog objects can also be written as a concise manuscript-style CSV summary with recovery fractions, match quality, predicted brightness, and catalog-status fields.



## 2026-04-10



- Added `Export Benchmark...` to the asteroid/comet `Recover Known` dialog so recovered and missed likely visible known objects can now be written directly to a CSV benchmark table for paper figures, appendix tables, or downstream analysis.

- Fixed the asteroid/comet post-`Generate` UI state so `Recover Known` and `Discover` now refresh their enabled state immediately after the detection result is stored instead of staying stuck in the pre-generate disabled state until some later frame or group change.

- Added an asteroid/comet `Recover Known` workflow beside `Discover` that searches the current solved subgroup for moving targets first, matches recovered tracklets back to the likely visible catalog predictions from `Generate`, and opens a review dialog with recovered known objects, missed likely visible known objects, and still-unmatched moving candidates for recovery benchmarking.

- Replaced the irrelevant differential-photometry File-menu exports in HR mode with a single `Export` action that writes a named science bundle containing the annotated Source Image, scientific HR diagram, and matching science-table CSV into a user-chosen folder.

- Updated the HR `Source Image` selection overlay so `Find Motion Group` members now appear there automatically, simplified those image annotations to a single circle instead of photometry-style annuli, and made all selected or highlighted stars use the same configured HR selection-circle color.

- Removed the experimental HR `X-Axis Log Scale` setting after review, expanded `Source Image` so current HR plot or table selections are annotated there together and the view recenters to keep those stars visible, and added `Save Image...` so the current Source Image export includes overlays such as selected-source apertures, motion vectors, and ROI drawings.

- Corrected the live HR `X-Axis Log Scale` toggle so switching it now resets the plot viewport onto the transformed axis immediately instead of reusing the previous linear BP-RP range, which removes the temporary UI hang and ensures the default Gaia BP-RP view visibly repositions stars onto the logarithmic temperature axis.

- Fixed HR scientific-export temperature-axis labeling by using explicit visible anchor ticks so the bottom labels stay readable and no longer repeat the `30,000` K or `2,600` K endpoints, added a saved `X-Axis Log Scale` option in both `Settings > HR Diagram` and the full `Open Settings` dialog, and corrected the live HR plot so enabling that option now visibly switches default Gaia BP-RP views onto a logarithmic bottom temperature scale instead of leaving the plot unchanged.

- Updated HR `Find Motion Group` plot highlighting so the same saved selected-circle color, opacity, and size-factor settings now drive common-motion highlight rings in both the live plot and scientific export instead of leaving those rings hard-coded.

- Standardized the app's user-facing HR terminology, restored the visible `HR Diagram` tab inside `Open Settings`, and updated the HR workflow/status/export text plus the focused tests and docs to match the preferred notation consistently.

- Added configurable selected-point circle styling for the HR plot in both `Settings > HR Diagram` and the full `Open Settings` dialog, including saved color, opacity, and size-factor controls while changing the selection ring to use a multiplicative size factor so small stars no longer get an oversized gap.

- Reworked HR `Find Motion Group` into a preset-based workflow: the main toolbar now stays simple with `Find Motion Group`, `Advanced...`, `Only Group`, and `Clear Motion Group`, while the new Motion Group Detection dialog saves `Default`, `Tight`, `Loose`, `Parallax Priority`, or `Custom` settings for backend choice, strictness, parallax handling, HR cleanup, and optional automatic group-only filtering.

- Added `scikit-learn` as an optional HR common-motion clustering backend, exposed a `Method` selector beside `Find Motion Group`, and wired the workflow so users can switch between the existing lightweight in-app grouping pass and `Sklearn DBSCAN` while keeping the same strictness, HR refinement, highlight/filter, and test coverage paths.

- Changed HR plot selection so clicking empty plot space now clears the active star selection and dismisses the popup without changing the plotted data, and split common-motion group state into a persistent `Only Group` filter membership plus a separate visual highlight layer so empty clicks can remove the selected-looking amber highlight while keeping the filtered group visible.

- Fixed the HR layout after the expanded motion-group controls by splitting the crowded plot toolbar into multiple rows, giving the HR tab area an ignored horizontal size policy so the main divider can be dragged again, and normalizing cached splitter widths so the Source Results panel reopens at a usable size instead of getting pinned too narrow.

- Added physical secondary HR scales so Gaia BP-RP views can show color temperature in Kelvin on the bottom axis and absolute-magnitude views can show luminosity in `L_sun` on the left axis, while the raw plot axes move to the top and right when those conversions are meaningful; updated the scientific export and focused HR widget tests to match.

- Changed the HR luminosity scale to use decade-style `L_sun` ticks, added derived temperature and luminosity values to the HR point popup, and replaced the broken Gaia CSV browser action with exact-object SIMBAD or CDS/VizieR lookups that prefer named-object pages when available.

- Added a first-pass `Find Motion Group` workflow in HR mode that detects the dominant common-proper-motion grouping directly in-app without sklearn, using Gaia pmRA / pmDec and parallax when enough rows support it, then filters the HR plot, `Source Results`, and motion overlays to that candidate cluster until `Clear Motion Filter` is used.



## 2026-04-09



- Refined the HR `Find Motion Group` workflow so it now highlights candidate common-motion members before filtering, adds `Strictness` and `Only Group` controls plus a `Clear Motion Group` reset, applies a second-pass HR consistency refinement to reject obvious sequence outliers, and carries that highlighted membership through the plot, scientific export, Source Results table, and source-image motion vectors.



- Changed the HR plot so `Gaia Absolute G Magnitude` is now the default Y axis, added editable plot-title and `Snapshot` / `Scientific` export controls, added `Copy Name` to the HR Source Results right-click menu, and changed the Gaia button to open a source-specific Gaia query result instead of the archive landing page.

- Moved the HR `Class Guides` and `Age Guide` educational overlays to the Gaia-only educational view, so they now appear for `Gaia BP-RP` versus `Gaia Absolute G Magnitude` instead of the measured proxy axis.

- Renamed the HR UI label for `absolute_magnitude_proxy` to `Measured Absolute Magnitude Proxy` so it is easier to distinguish from the new Gaia-only `Gaia Absolute G Magnitude` axis, and updated the related guide/age messaging and docs to match.

- Fixed HR working-table CSV export after adding `Gaia Absolute G Magnitude` by extending the explicit CSV field list to include `gaia_absolute_magnitude`, and tightened the plot filter so the new Gaia-only absolute-magnitude axis follows the existing parallax requirement correctly.

- Added a clean `Gaia Absolute G Magnitude` HR Y-axis option that plots Gaia G plus Gaia parallax directly, alongside the existing image-measured `Measured Absolute Magnitude Proxy`, and covered the new axis with focused HR core/widget tests and docs updates.

- Added an optional line-only `Class Guides` overlay for the canonical HR plot, drawing approximate educational guide curves for the Main Sequence, Giants, Supergiants, Subgiants, and White Dwarfs without the filled-region background that was previously tried and reverted.

- Reverted the HR `Class Regions` feature entirely after review; the HR plot is back to its original implementation without the experimental educational region overlay, related toolbar toggle, or supporting tests/docs.

- Changed asteroid/comet `Discover` so it now opens a Yes/No/Cancel popup asking whether the current subgroup is already aligned, skips reprojection when the user answers Yes, keeps the old align-first path for No, and treats closing or canceling the Discover progress window as a real cancellation request that stops the background task instead of merely hiding the dialog.

- Added an asteroid/comet `Discover` workflow for the current solved subgroup that aligns frames in memory, suppresses catalog-matched predictions from the current `Generate` result, links residual detections into motion-consistent candidate tracklets, and opens a ranked review dialog for unmatched moving objects; also wired the new header action into asteroid busy-state handling and added focused discovery regressions.

- Fixed asteroid/comet `Exposure Motion` refreshes so changing frame, group, or exposure after sorting the `Predicted Objects` table no longer writes trail-length values onto the wrong visible rows; the column now refreshes by each row's stored detection index and keeps its numeric sort value in sync.

- Added `Ctrl` + click manual peak correction to the synthetic-tracking preview so a faint target can be reselected when the auto-picked measured peak locks onto a nearby star, recomputing stacked SNR, center offset, peak value, and local flux live with a `Reset to Auto` fallback and separate auto/manual overlays in the dialog.

- Changed the synthetic-tracking preview so the `Measured peak` marker now renders as a hollow circle instead of drawing a filled center dot over the target, and added a focused image-view regression for optional overlay center dots.

- Added `Settings > Open Settings > Asteroid/Comet > Synthetic Tracking` with persisted crop-radius, combine-mode, and mixed-`All`-group controls, wired those settings into the `Synthetic Track` worker and stack builder, and added focused regressions for settings persistence, sigma-clipped stacking, and the mixed-group availability guard.



## 2026-04-08



- Added an asteroid/comet `Synthetic Track` action that shifts and stacks the current frame group on the selected predicted object, runs the work in a background worker, and opens a preview dialog with stacked SNR, center offset, and per-frame inclusion details.

- Added asteroid/comet `Visuals` support for invert-aware annotation colors, added `Export` settings for GIF resolution scaling and looping, removed the `Status` column from `Predicted Objects` so that status stays only in the right-side inspector, and merged the frame inspector's filter plus exposure readout into a single `Filter and Exposure` row.



## 2026-04-07



- Added an `Asteroid/Comet > Visuals` settings group for labeling every detected object, showing prediction crosshairs for the full field, and optionally disabling selected-object highlight colors, and revised the asteroid/comet info panel to merge date/time, rename `Confidence Status`, use `V_mag`, move `Field` below `Setup`, and reorder Moon illumination ahead of exposure.

- Changed asteroid/comet blink preload to precompute per-frame local-match measurements in the background so the right-side inspector can keep SNR, flux, and frame-specific trail values updating dynamically during blink playback without falling back to live per-frame measurement work on each timer tick.

- Fixed asteroid/comet `Export Blink` cancellation so the progress dialog now exposes an explicit `Cancel` action, closing or canceling the capture stops the frame-export loop cleanly, and the workflow exits as a normal canceled operation instead of reopening the progress window until completion.

- Added a dedicated `Setup` tab before `Advanced` in `Open Settings` for telescope, camera, focal length, aperture, focal ratio, pixel size, derived pixel scale, location, and Bortle storage, and expanded the asteroid/comet image inspector with hide/show controls plus richer frame and setup metadata including Moon illumination.

- Added an `Export Blink` action to Asteroid/Comet Detection that captures the current blink viewport frame-by-frame and writes an animated GIF for educational playback, added a small shared GIF export helper, and fixed the asteroid rendered-frame cache key so `Invert` uses distinct cached previews.

- Added an `Invert` display toggle beside `Contrast` for the annotated-image viewers, persisted the shared default invert state in settings, and expanded `Customize Theme` with an `Asteroid/Comet Detection` group for selected-object overlay circle color, line color, line width, label size, and label color.

- Traced the remaining missing `3I/ATLAS` case in aligned asteroid/comet runs to live IMCCE Miriade ephemeris HTTP timeouts, added a targeted JPL Horizons fallback behind the existing canonical `3I` lookup so the visitor is still recovered when IMCCE stalls, and covered the timeout-to-Horizons path with a focused solar-system regression.

- Fixed the targeted interstellar `3I/ATLAS` fallback so IMCCE ephemeris requests now use an Astropy-compatible UTC epoch string and query Miriade with the canonical `3I` identifier while still displaying `3I/ATLAS` in the asteroid/comet results, and added a focused solar-system regression around the corrected fallback lookup.

- Added an asteroid/comet `Align` action beside `Estimate` for already plate-solved multi-frame sequences, reprojected each frame onto the current reference frame's WCS and saved derived FITS outputs into an `aligned` subfolder without touching the originals, wired the workflow through a background worker that reloads the aligned sequence on success, and fixed aligned-header construction to preserve each source frame's own timestamp and metadata instead of leaking the reference header cards across outputs.

- Fixed aligned XISF timestamp parsing by promoting explicit `Observation:Time:Start` / `Observation:Time:End` values into the synthesized header when PixInsight stored a naive FITS-style `DATE-OBS` alongside timezone-aware UTC XISF properties, which prevents those frames from being shifted a second time through the configured local observation timezone, and added focused image-I/O plus scanner regressions for the real aligned-file pattern.

- Restored header-first asteroid/comet timestamp selection so `DATE-OBS` stays authoritative when present while filename timestamps remain only a fallback for missing or unreadable header times, changed `Track Object` and blink updates to keep following each frame's WCS-predicted position even for unaligned sequences by using fast prediction-only frame measurements when live peak matching is suppressed, and added focused scanner, solar-system, and asteroid UI regressions for the new behavior.

- Fixed a serious timestamp-source issue for processed frames by broadening filename timestamp parsing to handle PixInsight-style prefixes/suffixes such as `10. ... _c.xisf`, and now prefer the filename capture timestamp over a materially conflicting naive `DATE-OBS` header while still honoring explicit header timezone offsets.

- Clarified the intended timestamp rule by adding DST-aware `America/New_York` regression coverage so naive filename and `DATE-OBS` times are interpreted in the user-selected `Image Timestamp Timezone`, and trimmed the interstellar fallback to a single cached `3I/ATLAS` lookup so asteroid/comet `Generate` no longer stalls on multiple sequential IMCCE requests.

- Fixed image-metadata timestamp handling so scanned `DATE-OBS` and filename-derived observation times stay timezone-aware in UTC instead of being normalized to naive datetimes and then accidentally shifted again by later UI formatting, updated asteroid/comet timestamp consumers to treat older naive metadata as UTC for compatibility, and added scanner plus asteroid metadata regression coverage for the corrected UTC display.



## 2026-04-06



- Added a targeted interstellar-object ephemeris fallback so asteroid/comet detection can supplement missing SkyBoT cone-search results with direct IMCCE Miriade lookups for objects such as `3I/ATLAS`, and covered the merge/deduplication path with new solar-system regression tests.

- Traced the remaining missing `3I/ATLAS` case to astroquery silently clamping SkyBoT `position_error` to 120 arcsec, replaced the asteroid/comet cone-search call with a direct SkyBoT VOTable request so the full relaxed 3600-arcsec uncertainty filter is actually sent, and added a regression test around the outgoing SkyBoT payload.

- Sped up asteroid/comet `Group` switching by reusing cached frame measurements and remembered group frame selection instead of forcing immediate live remeasurement, moved the `Generate`/`Apply` action to the far left of the asteroid header, and moved `Observation Time` beside the `Frame` selector.

- Replaced the hard-coded bright green asteroid/comet row highlight with a softer theme-blended highlight for readability, made every `Predicted Objects` column user-resizable, and relaxed the SkyBoT query/filter path so high-uncertainty and interstellar comets such as `3I/ATLAS` are less likely to be dropped before image confirmation.

- Fixed SkyBoT parsing for split interstellar designations such as `3I` + `ATLAS`, enabled numeric sorting for asteroid/comet `V_mag`, `Confidence`, `Motion`, and `Exposure Motion`, moved `Exposure` and `Mag Limit` into the top header row beside `Observation Time`, put `Estimate` immediately to the right of `Generate`, changed `Exposure` into a read-only display sourced from the selected frame, and removed the in-panel asteroid/comet observatory-code field.



## 2026-04-05



- Reduced the thickness of the inline asteroid/comet progress bar, switched the asteroid/comet `Generate` button to a temporary `Apply` label after `Estimate` updates `Mag Limit`, added an exposure-motion column that reports expected pixel motion plus a point-source versus tail note, and replaced the one-shot mixed-folder prompt with a persistent `Group` selector that always includes `All` plus per-filter/per-exposure subsets for the `Frame` blink list.

- Added `Settings > Open Settings > Asteroid/Comet > Estimate Options` so the visible-limit estimator can now persist a custom SNR threshold, start magnitude, sampled-star count, required visible-star count, and optional annotation of the faintest confirmed estimate stars.

- Styled the inline asteroid and HR progress bars with the current theme accent color and removed the extra status-bar progress widget from the lower-right corner.

- Fixed asteroid/comet image display-control changes and resets so clearing the fast rendered-frame cache no longer leaves a placeholder display rendered as a black image; `Reset Display` now restores the mode's current configured stretch, brightness, and contrast cleanly.

- Fixed the asteroid/comet visible-limit estimator progress message formatting so `Estimate` no longer fails with an `Invalid format specifier` error before the sampled search begins.

- Expanded asteroid/comet visible-limit Work Log output to narrate the background search by magnitude bin, including sampled-star match results, measured SNR values, offsets, and per-bin summaries.

- Reworked the asteroid/comet `Estimate` visible-limit action to use a quick Gaia-star sampling pass in 0.5-mag bins instead of a full detected-source cross-match, making `Mag Limit` estimation substantially faster while still checking for visible stars and their SNR.

- Moved the asteroid/comet `Blink` button into the `Asteroid / Comet Overlay` toolbar so playback controls now sit directly beside the blink interval and object-tracking controls.

- Added an asteroid/comet `Estimate` action beside `Mag Limit` that measures the dimmest Gaia-matched star visible in the current frame, updates the visible-limit readout, and copies that estimate into `Mag Limit` for reruns.

- Removed the last asteroid blink fallback that rebuilt full annotated-image displays when a rendered frame preview was already cached; asteroid frame switches now reuse lightweight placeholder display metadata plus the cached rendered image, reducing preload and playback overhead further.

- Optimized asteroid/comet blink playback to preload fully rendered frame previews for the current stretch settings, so blinking now swaps cached display images instead of rebuilding each frame view on demand.

- Asteroid blink mode now skips live per-frame measurement recomputation while the timer is active and resumes frame-specific measurements after playback stops, avoiding repeated FITS/XISF reads during blink.

- Added focused regression coverage for asteroid rendered-frame cache usage and for suppressing live frame measurement work during active blink playback.

- Asteroid/Comet Detection now turns its `Generate` action to the current accent color after a file or folder sequence is loaded, matching the ready-to-run attention styling used in the other modes.

- Added asteroid-mode `Center Object` and default-on `Track Object` controls so the selected prediction can be recentered on demand and kept centered while switching frames or blinking across a sequence.

- Blink playback now preloads asteroid frame previews in a multithreaded background worker behind a loading dialog before playback starts, avoiding the old frame-by-frame stall during the first blink pass.

- Added a configurable asteroid blink interval control so the user can set how long each frame remains visible during playback.

- Made asteroid/comet row selection persist independently of transient table focus so the selected prediction stays highlighted on the annotated image.

- Focused the asteroid/comet image view on the selected prediction or measured match during row changes and frame/blink updates so the marked object is immediately visible.

- Added focused asteroid-mode regression coverage for persistent selection and selected-object focusing in the annotated image view.

- Fixed Asteroid/Comet Detection blink playback so advancing frames always updates the active image through a shared frame-selection path, renamed the left log pane to `Work Log`, timestamped asteroid work-log messages, and now append an explicit completion summary after successful detections.

- Expanded `Asteroid/Comet Detection` so it can open a folder of solved subframes, populate a frame selector, blink through the sequence, reuse the differential image stretch/brightness/contrast controls, and recompute per-frame predicted positions plus current-frame SNR/flux values for the selected detection.

- Fixed Asteroid/Comet Detection altitude checks so stale or unavailable Astropy IERS data no longer aborts detection runs; the app now falls back gracefully when local Alt/Az conversion cannot be resolved offline.

- Fixed Asteroid/Comet Detection SkyBoT parsing so unit-bearing Astropy QTable columns like `RA`, `DEC`, `RA_rate`, and `posunc` are accepted directly instead of being rejected as missing numeric fields.

- Added a third `Asteroid/Comet Detection` app mode with a single-image MVP workflow that reads observation metadata from the source header, resolves or reuses the image WCS, queries known solar-system objects predicted in the field, estimates likely visible matches from local image peaks, and overlays both predicted and measured positions in the shared annotated-image viewer.

- HR Diagram mode now highlights the `Generate` button with the current theme accent after a source image is selected, matching the attention styling already used in differential-photometry mode.

- HR Source Image now re-annotates the currently selected star from either the filtered HR table or the HR plot, so switching from the plot/table to the image view keeps the same source visually marked.

- HR rows now carry Gaia parallax and proper-motion metadata, the filtered HR table includes a `Parallax` column, and the Source Image tab adds a lazy `Show Motion` vector overlay with customizable color and line width.

- HR plot markers now scale by Gaia G apparent magnitude so brighter stars appear larger, and the plot controls add lower and upper apparent-magnitude filters for constraining the visible brightness range.

- Clicking an HR plot point now shows a small star-info popup immediately and refreshes it with cached background SIMBAD details such as spectral type when available, without blocking the UI thread.

- Fixed HR RGB source previews so the `Source Image` tab keeps color data instead of collapsing solved RGB images to grayscale.

- Moved HR motion-vector color and line-width configuration into the menu bar under `Settings > HR Diagram` and persisted those settings across sessions.

- Expanded the menu-bar `Settings > HR Diagram` panel with persisted plot-presentation controls for `Require Parallax`, point color saturation, point opacity, and fixed-versus-brightness-scaled marker sizing.

- Renamed the HR left-side `Filtered HR Rows` panel to `Source Results`, renamed `Notes` to `Work Log`, reduced the work-log pane height, and added an explicit HR working-table finalization message between the last measurement-progress update and the completion summary.

- Made the HR `Source Results` and `Work Log` panes vertically resizable with a splitter, tuned their initial sizes to match the new HR layout, tinted rows that currently survive onto the plot, and added a right-click Source Results workflow for manually adding or removing stars from the plot as ROI overrides.

- Stabilized the high-density HR plot display subset so removing one star no longer reshuffles unrelated visible points, and added a `Show Selected On Top` Source Results action to pin chosen stars to the top of the table.

- Updated HR `Update Diagram` refreshes so currently plotted rows float above non-plotted rows in `Source Results`, and cached the HR splitter sizes in settings so custom panel sizing persists across reloads and relaunches.

- Changed HR ROI drawing so plain drag and mouse wheel stay in pan/zoom mode while `Shift` + left-drag drafts the selected circle or rectangle ROI, and limited `Show Motion` overlays to the currently plotted subset instead of the broader Source Results list.

- Replaced the HR Source Results `Parallax` column with `Proper Motion`, moved the ROI `Keep Inside` / `Exclude Inside` default into `Settings > HR Diagram`, and clarified the overlap between ROI Action and `Invert` for single-region ROI workflows.

- Simplified the HR ROI controls again by making `Circle ROI` the default shape, removing the separate ROI Action and `Undo Last` controls, moving the stretch/brightness/contrast controls into the top Source Image toolbar row beside `Clear All`, and dropping the extra preview-controls-only hint message.

- The File menu now swaps between `Open Folder` in Differential Photometry mode and `Open File` in HR Diagram mode, and HR `Source Image` now lets `Ctrl` + left-click toggle motion-vector stars in or out of the plot directly from the image.

- The HR action button now reuses the accent-attention styling for pending `Update Diagram` states, not just the initial `Generate` state.

- Added an optional HR age-guide overlay for `Gaia BP-RP` versus `Absolute Magnitude Proxy` views, with an adjustable age control aimed at quick globular-cluster-style inspection.

- Fixed HR working-table CSV export so the new proper-motion fields are included in the written schema instead of failing preparation with a `dict contains fields not in fieldnames` error.

- Added focused regression coverage for the HR proper-motion table column, source-image selection annotation, simplified ROI defaults, motion-vector toggle path, and HR generate-button attention styling.



## 2026-04-02



- Improved annotated-image preview scaling by deriving display limits from sigma-clipped background statistics and a bright-pixel percentile, which darkens background noise while keeping stars and highlights visible.

- Cached normalized and asinh preview transforms inside annotated-image display objects so brightness/contrast and stretch redraws reuse precomputed image data instead of recomputing the full preview each time.

- Image-view render-setting changes and frame switches now preserve the current zoom level and view center instead of snapping the inspection viewport back to the default fit.



## 2026-04-01



- Settings now support a persisted custom settings-file location in addition to the default AppData path, so the app can reopen the same external settings file even when no environment override is present.

- The Settings dialog now exposes the settings-file location choice directly, with a default-AppData toggle and a browseable custom path field.

- Saving settings now preserves a separate immutable `default_settings` snapshot, so reset/default values continue to reflect product defaults instead of drifting to the user's last-saved configuration.



## 2026-03-31



- Changed the Light Curve y-axis selector to default to Calibrated Magnitude, added Source Results copy-name support, removed the old Measurements-only search box and Unflagged Only filter, and made the shared Sources search box filter whichever results tab is active.

- Fixed the HR results row-limit control so users can reduce the displayed row cap below 10 again, matching the filtered-table behavior and tests.

- Moved `Hide excluded` into the Light Curve controls between `Fit Period` and `Fold` while keeping it responsible for both Measurements filtering and active light-curve visibility.

- Updated `Recompute Photometry` to operate on the currently selected object row instead of stale workspace context.

- Fixed manual photometry workflows so saving apertures and recomputing no longer persist `Manual` mode by default; manual recompute now uses a temporary manual override and `Clear Selection` also clears the saved per-object explicit source subset.

- Fixed a stale-light-curve case when `Hide excluded` is enabled by rebuilding the visible series selector before syncing Source Results row changes.

- Added a source-level SNR column to Source Results, using the median finite SNR across that source's measurements, and updated the column order accordingly.

- Updated Light Curve hover tooltips to show the y-value with two decimals and append the y-error as `Â±xx.xx` when available.

- Fixed Source Results numeric sorting so Mag, Measured, Flagged, SNR, Period (d), Calculated (d), Match Index, and Var Score sort numerically instead of lexicographically, and tightened the Filter and SNR column widths.

- Added a Light Curve `Filter` workflow with a `Filtering Settings` dialog plus an enable checkbox in the Light Curve toolbar; unchecking the checkbox now cancels the active filter set without discarding the saved criteria.

- Moved `Hide excluded` into the new Light Curve filtering dialog and added two additional optional filters: maximum magnitude error and outlier magnitude bounds.

- Light Curve filtering now drives the Measurements subset, visible light-curve points, image-frame selector, filtered exports, and `Fit Period`; newly fit filtered periods are written back into Source Results immediately.

- Removed the always-visible Browser pane from the upper-left sidebar, replaced it with a compact Workspace action strip for `Generate`, `CDS`, `Simbad`, `Gaia`, and `VSX`, and let the results/Measurements area extend upward without shrinking the Work Log region.

- Scans now open a `Loaded Results` summary dialog that summarizes discovered object folders and lets the user choose the active object for `Generate` and scan-file inspection now that the Browser pane is hidden.

- Moved the Light Curve filter enable checkbox to the left of the `Filter` button for a tighter control grouping.

- The Workspace strip now shows the current active object inline to the right of `Generate` so it is obvious which object that action will use.

- The `Loaded Results` popup now includes per-object invalid counts plus a live note preview that summarizes solved/unsolved readiness and the most common file notes for the selected object.

- After a scan loads results, the `Generate` button now adopts the current theme accent color to draw attention and returns to its normal style as soon as processing starts or fails/completes.

- Added an `Increase SNR` source action next to `Find Better Fit`; it processes one or many selected sources in the background and creates conservative higher-SNR derived light curves without discarding the original measurements.

- Added persisted Advanced settings for `Increase SNR`, including period-fraction and duration caps, target SNR, frame-per-bin limits, variability-type-aware thresholds, weighted flux binning, sigma clipping, dataset mode, selection scope, and optional periodless fallback.

- Source details and summary notes now report `Increase SNR` results, including original-to-derived row counts, chosen frames per bin, effective bin duration, and estimated SNR gain for each derived series.

- The `Increase SNR` button now switches to `Reset SNR` for selected sources that already have derived SNR-binned series, restoring their original measurements in place without rerunning photometry.

- SNR-derived measurements now reuse representative original frame paths for image inspection, and the image panel falls back to the closest original frame instead of trying to open synthetic `_snrbin_*.fit` files.

- The `Process Default` button in the source-selection preview dialog now uses the current theme accent color so the default recommended action is visually distinct.

- `Fit Period` now writes Work Log entries before and after the fit, showing the currently displayed period value and the updated fitted value for the active series.



## 2026-03-30



- Fixed `Find Better Fit` fallback reruns so magnitude-matched retries can still run when the target lacks a catalog magnitude but has calibrated photometry, and the work log now explains when fallback retries are unavailable, undersized, or redundant.

- Added a Light Curve `Export` button plus an Advanced setting that defaults exports to a scientific white-background plot while preserving an exact-view export mode when the setting is disabled.

- Light-curve exports now support `PNG`, `PDF`, and `SVG`; scientific exports keep the active fit, fold state, and visible span, while exact-view exports can write the live pyqtgraph plot directly to raster or document/vector formats.

- Corrected exact-view light-curve exports to capture the full on-screen plot widget instead of a clipped corner, and File -> Export Plots now follows the same scientific-vs-themed export setting as the per-plot Export button.

- Added scientific PDF export controls for light curves in Advanced Settings, including configurable PDF DPI and paper size used by scientific per-plot PDF exports.

- Added a compact light-curve export-format dropdown next to the Export button and moved both controls to the far right of the Light Curve toolbar so common PNG/PDF/SVG exports are quicker to reach.

- Added a `Keep Comparison Stars` option to `Edit Apertures`, defaulted it on, and now seed the manual aperture set from the current target/comparison measurements so editing target apertures no longer drops the active ensemble by default.

- Unchecking `Keep Comparison Stars` now clears the retained comparison set and re-enables manual comparison-star definition, while retained comparison stars stay visible without draggable handles.

- `Find Better Fit` now detects when `Apply to All Frames` saved a newer manual aperture configuration than the current processed report, reprocesses with that manual setup first, then resumes the comparison-fit search automatically.



## 2026-03-29



- Optimized science/report export by indexing comparison and check measurements once per export run, reusing annotated-image display data for repeated measurements from the same frame, and avoiding duplicate science-row recomputation for the summary payload.

- Moved science/report export work off the UI thread, restricted report export to the selected Source Results rows, and now write one bundle per selected source under the object's `science/<source>` folders instead of exporting everything into a manually chosen directory.

- Removed annotated-image generation from the science/report export bundle because it is not required for AAVSO submission; annotated images remain available through the separate annotated-image export action.

- Added configurable worker-count settings for `Calculate Period` and `Pull Period`, made those background jobs honor the configured limits, and changed per-source science exports to name every bundle file after the selected source folder while writing a README that tells users to upload the `*_aavso_extended.txt` file to AAVSO.

- Corrected the AAVSO Extended export so `CMAG` and `KMAG` now contain the instrumental comparison and check magnitudes required by the spec, switched exported JD timestamps to the exposure midpoint when `EXPTIME` is available, expanded filter-code handling, and changed the Science Export filter setting from a free-text box to an editable dropdown seeded with the AAVSO-recommended filter codes.

- Added an observation-timezone setting so users can choose the timezone for filename timestamps and naive `DATE-OBS` headers; scanning now converts those timestamps to UTC before processing/export, while headers that already include an explicit timezone offset keep that declared offset and measurement-cache signatures invalidate when the timezone setting changes.

- Changed the AAVSO file header to write `#DELIM=,` to match the official Extended-format examples, and the export now prefers the selected AAVSO export-filter setting over the source image filter so rows can be reported as `CV`/`CR` even when the original frame metadata says `L` or another acquisition filter.

- Differential photometry now stores a zero-point calibrated magnitude and uncertainty per measurement, exposes that calibrated magnitude as a light-curve y-axis option, and exports zero-point-calibrated CV-style rows as `MTYPE=STD` even when `TRANS=NO`.

- Science observation exports and the measurement detail pane now include explicit check-star diagnostics, including the check calibrated magnitude, per-frame residual, and per-series residual RMS when a catalog-backed check star is available.



## 2026-03-28



- Added background QThread workers for `Pull Period` and `Calculate Period` so those actions no longer run directly on the UI thread.

- Period actions now disable both period buttons while a job is active to prevent overlapping updates against the same report.

- Added regression coverage for the new period worker startup/completion behavior.

- Started the repository-level implementation log in `version.md` for tracking future changes.

- Added bounded thread-pool concurrency inside `Pull Period` so literature lookups can run in parallel without blocking the UI thread.

- Added process-pool execution for multi-series `Calculate Period` runs so period inference can use multiple CPU cores when more than one light curve is being analyzed.

- Updated literature-period completion handling to write pulled period values back onto the live catalog entry metadata before refreshing the source table.

- Added cooperative cancel support for active `Pull Period` and `Calculate Period` jobs.

- Period buttons now switch into cancel controls while a job is active and surface cancellation progress in the work log and status bar.

- Added explicit serial/thread/process execution-mode messaging so users can see whether a period job is running in serial mode or parallel mode and with how many workers.

- Reduced `Calculate Period` completion refresh work so it now updates the source table and active series state without rebuilding the full measurement and selector pipeline.

- Source Results selection now also highlights the comparison/reference source rows associated with the selected target so the ensemble stars are easier to identify in the table.

- Added a `Find Better Fit` action on Source Results that tries alternate comparison-star combinations for the selected target, logs every tested combination, and applies the best period-matching ensemble back into the live report.

- Parallelized multi-combination `Find Better Fit` searches across multiple CPU worker processes so large comparison-star searches complete much faster while preserving per-combination logging and cancellation behavior.

- Reduced `Find Better Fit` completion refresh work so Source Results no longer immediately rebuild the full Measurements table unless that tab is open; the Measurements tab now refreshes lazily when needed.

- Added a third `Advanced` tab to Settings with persisted `Find Better Fit` controls for stop-match threshold, comparison-fit CPU worker count, eclipsing-binary retry tolerance near match index 50, fallback candidate-pool size, and fallback magnitude tolerance.

- Extended `Find Better Fit` to stop scheduling new combinations once the configured match threshold is reached, retry near-50 matches with the eclipsing-binary period convention, and fall back to a magnitude-matched comparison-star pool when the desired threshold is not met.

- Enabled multi-target `Find Better Fit` from Source Results by queueing all eligible selected targets sequentially while keeping combination-level evaluation parallel inside each target job.

- Added regression coverage for the new advanced settings persistence, the Advanced settings tab/defaults, multi-target comparison-fit queueing, and fallback comparison-pool worker behavior.

- Removed the old hard 8-worker ceiling for `Find Better Fit` so an explicit worker count now honors the configured value up to the machine's available logical CPU count.

- Comparison-fit progress logs now explicitly report when a near-50 match triggered an eclipsing-binary retry, even if the retry was tested but the standard convention remained the selected result.

- After `Find Better Fit` updates a source, the Light Curve `Fit Period` button now recalls that cached best-fit result for the same series instead of running a fresh period inference again.

- Added an Advanced Settings checkbox to enable multiple-target `Find Better Fit` selection explicitly; it now defaults to off and keeps multi-row Source Results searches disabled unless the option is enabled.

- Cached computed airmass values so repeated Measurements-table loads and export passes reuse the same site/time/coordinate calculation instead of recomputing it for every refresh.

- Replaced automatic Measurements-tab rebuilds with an explicit `Measure` button that warns how many filtered rows will be loaded before rendering the table, keeping processing completion responsive on large reports.

- `Fit Period` now ignores cached `Find Better Fit` results when the user has Ctrl-dragged a narrower light-curve segment, and it promotes that manual segment fit into the remembered result only when its literature match index is better than the cached score.

- Fixed `Fit Period` so enabling the fit checkbox programmatically immediately redraws the fitted curve, and period-result cache persistence now remembers whether a restored value came from `Find Better Fit` or a manual override so relaunches keep the correct reuse behavior.

- Added compatibility for older saved period-cache entries that predate the new origin metadata, so `Fit Period` can still reuse legacy saved results after relaunch instead of recomputing them.

- Persisted the comparison-source ensemble used by `Find Better Fit` and restore that ensemble back onto measurements and light curves during reload, so post-relaunch `Fit Period` works against the same saved comparison-star solution.

- Suppressed pathological fitted light-curve overlays when the generated fit diverges wildly from the observed data range, preventing impossible thousand-magnitude curves from being drawn.

